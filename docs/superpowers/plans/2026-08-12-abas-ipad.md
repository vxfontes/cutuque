# Abas no iPad, novo terminal tmux e estado por agente — plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fazer o app de iPad navegar como PC (abas para sessões ao vivo, Board, máquinas e arquivo), consertar o `restoreSize()` que hoje deixa o pane do tmux preso na grade do iPad, criar sessões tmux pelo app escolhendo máquina/grupo/sessão/pasta/agente, e passar a inferir estado de pane para codex e opencode.

**Architecture:** Nada de estrutura muda na raiz — decisão #19 do projeto continua valendo: uma raiz de `NavigationSplitView` construída uma vez, nunca substituída, com N painéis montados num `ZStack` alternando por **opacidade**. O que hoje o `SessionDetailPane` faz com dois painéis, o `RootSplitView` passa a fazer com N, dirigido por um modelo puro (`OpenTabs`) testável sem simulador. No hub, o script Python que varre os panes sai de dentro do `tmux.go` para um arquivo próprio e ganha uma **tabela de marcadores por agente** no lugar do `if ag == 'claude'` fundido na função, o que torna a inferência testável de verdade (hoje não há um único teste do Python).

**Tech Stack:** Swift 5.9 / SwiftUI (iOS 17+, iPad e iPhone), XCTest, XcodeGen; Go 1.22 no hub (`net/http` `mux.Handle("POST /...")`), `go test`; Python 3 do sistema (script remoto via ssh/stdin); tmux.

**Spec:** `docs/superpowers/specs/2026-08-12-abas-ipad-design.md` — D1…D13, tabela de estados, tabela de marcadores.

**Divergência declarada da spec:** a pendência #7 da spec sugeria _um plano por fase_. A Vanessa pediu explicitamente **um plano só, dividido no máximo de tasks paralelizáveis**. É o que está aqui: 29 tasks em 8 ondas, com as dependências explícitas.

## Global Constraints

- **pt-BR em tudo**: nomes de teste, comentários, mensagens de erro, rótulos de UI. O código existente é assim; não misturar.
- **Decisão #19**: nunca montar/desmontar painel condicionalmente para trocar de aba. Alternância é `opacity` + `allowsHitTesting` + `accessibilityHidden`, como em `SessionDetailPane.swift:86`.
- **Regra de 3 arquivos** (regra da Vanessa): nenhuma task toca mais de 3 arquivos de produção. Se uma task crescer, ela está errada — pare e quebre.
- **`scripts/tmx.sh` é FORA DE ESCOPO.** É script da Vanessa, aparece como ` M` no `git status` e **não deve ser commitado por agente nenhum**. `git add` sempre com caminhos explícitos, nunca `git add -A` / `git add .`.
- **Nunca apagar comentário que documenta um bug.** O padrão do projeto é comentário load-bearing (ex.: `SessionListView.swift:372-375`, `tmux.go:95-97`). Quando a decisão muda, o comentário é **reescrito com a nova razão e a data**, não deletado.
- **`work_re` exige o parêntese.** `re.compile(r'\((?:\d+m )?\d+s')`. Quem "melhorar" o regex tirando o `\(` faz toda tela ociosa do opencode (`▣ Build · <modelo> · 3.2s`) virar `running` para sempre. Existe teste para isso — não afrouxe.
- **`esc interrupt` ≠ `esc to interrupt`.** O opencode escreve **sem** o "to". Marcador é por agente; não trocar por um `'interrupt' in low` genérico.
- **Validação de nome** (D12): grupo e sessão aceitam só `A-Za-z0-9-_`. Barrado no campo do app **e** rejeitado no hub (defesa em profundidade).
- **`✕` cinza não é kill.** Instrução literal da Vanessa preservada em `SessionDetailPane.swift:97`: _"nao é pra kill, é apenas pra fechar o terminal mas ele deve continuar trabalhando na sessao tmux"_.
- **Teto de abas vivas = 6**, escrito num lugar só (`OpenTabs.maxVivas`).
- **Convenção de nomes neste documento**: `D1`…`D13` sozinhos são sempre as **decisões travadas da spec**. Task deste plano vem sempre com a palavra na frente — **Task D1**, **Task G2**. As duas numerações colidem (existe decisão #1 e existe Task D1) e essa é a única forma de não confundir.

### Comandos

```bash
# Hub
cd hub && go test ./...
cd hub && go build ./...

# App — build
cd app && xcodegen generate
cd app && xcodebuild build -project CutuqueApp.xcodeproj -scheme CutuqueApp \
  -destination 'generic/platform=iOS' -quiet

# App — testes
cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp \
  -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet
```

**Arquivo novo no app precisa de `xcodegen generate` antes do build** — o projeto é gerado, não versionado.

## Pré-requisito de integração (bloqueia a onda 5)

Duas branches em voo, nenhuma mergeada nem pushada, hub não redeployado:

- `identidade-pane-ao-vivo` (`f3aef96` + `933e9db`, card `2136ff6c8e12ed22`) — separa `LiveEntry.paneTarget` de `LiveEntry.id`. **É o que torna grupo de mesmo nome em duas máquinas seguro**, e a chain E (lista agrupada) só é segura depois dela.
- `versao-ipad` — 132/132 testes, aceitação em iPad físico pendente.

A chain E **não começa** antes desse merge. As chains A/B/C/D/F/G podem começar já.

## File Structure

| arquivo | novo? | responsabilidade | chain |
|---|---|---|---|
| `hub/internal/adapter/claudecode/tmux_script.go` | **novo** | os consts Python: funções puras de inferência + driver de varredura | A |
| `hub/internal/adapter/claudecode/tmux_script_test.go` | **novo** | executa o Python puro contra telas de fixture, via `python3 -c` | A |
| `hub/internal/adapter/claudecode/tmux_screens_test.go` | **novo** | as telas reais capturadas em 12/08 como consts Go | A |
| `hub/internal/adapter/claudecode/tmux.go` | modifica | tipos, `Tmuxer`, parse, impls SSH/Local (perde os consts Python) | A/B/C |
| `hub/internal/adapter/claudecode/tmux_new.go` | **novo** | montagem e validação do `new-session`, impls SSH/Local | C |
| `hub/internal/adapter/claudecode/tmux_new_test.go` | **novo** | testes puros do `new-session` | C |
| `hub/internal/session/session.go` | modifica | `Discovered.Kind` | B |
| `hub/internal/launcher/launcher.go` | modifica | `Launcher.TmuxNewSession` | C |
| `hub/internal/server/launch.go` | modifica | handler HTTP de criar sessão | C |
| `hub/internal/server/server.go` | modifica | registro da rota | C |
| `app/CutuqueApp/TerminalPaneState.swift` | **novo** | o estado de três valores + a regra de quando devolver a largura | D |
| `app/CutuqueApp/TerminalMirrorView.swift` | modifica | consome o estado; `restoreSize()` no `liberado` e no background | D |
| `app/CutuqueApp/LivePaneLogic.swift` | **novo** | lógica pura das sessões ao vivo, tirada do `SessionListView.swift` | E |
| `app/CutuqueApp/SessionListView.swift` | modifica | lista agrupada por grupo, kill por máquina, botão "+" | E/F |
| `app/CutuqueApp/NomeTmux.swift` | **novo** | validador de nome de grupo/sessão (D12) | F |
| `app/CutuqueApp/APIClient.swift` | modifica | `tmuxNewSession(...)` | F |
| `app/CutuqueApp/NovoTerminalForm.swift` | **novo** | formulário de criação + sua lógica pura | F |
| `app/CutuqueApp/OpenTabs.swift` | **novo** | modelo das abas: abrir/fechar/fixar/dormir/persistir | G |
| `app/CutuqueApp/TabBar.swift` | **novo** | a barra de abas (view) + menu de toque longo | G |
| `app/CutuqueApp/RootSplitView.swift` | modifica | barra de abas na coluna de conteúdo, N painéis por opacidade | G |
| `app/CutuqueApp/NavigationState.swift` | modifica | `expandedVisibility` sem orientação (D4) | G |
| `app/CutuqueApp/LiveHub.swift` | **novo** | uma conexão `/ws` distribuída para N assinantes | H |
| `app/CutuqueApp/SessionDetailView.swift` | modifica | consome o `LiveHub` em vez de abrir conexão própria | H |

Arquivos de teste novos (a tabela **Testes** no fim do documento diz qual task escreve
cada um): `tmux_screens_test.go`, `tmux_script_test.go`, `tmux_state_opencode_test.go`,
`tmux_state_codex_test.go`, `tmux_shell_test.go`, `tmux_new_test.go`,
`TerminalPaneStateTests.swift`, `TerminalCenaLogicTests.swift`, `LivePaneLogicTests.swift`,
`NomeTmuxTests.swift`, `NovoTerminalFormTests.swift`, `OpenTabsTests.swift`,
`LiveHubTests.swift`. Três que **já existem** recebem casos novos: `tmux_test.go`
(Task B1), `launch_test.go` (Task C4) e `NavigationStateTests.swift` (Task G7).

**Por que a granularidade do arquivo de teste muda de chain para chain:** na chain A é um
por task, porque em Go arquivos diferentes do mesmo pacote nunca conflitam e cada task
tem um assunto próprio (opencode, codex, shell). Na chain G é um só para G1–G4, porque
essas quatro tasks são sequenciais de qualquer forma — mexem todas no mesmo
`OpenTabs.swift` — e dividir o teste não compraria paralelismo nenhum, só espalharia o
mesmo assunto por quatro arquivos.

## Grafo de dependências

```
onda 1   A1    C1    D1    E1*   F1    G1    G7    H1
          |     |     |     |           |
onda 2   A2    C2    D2    E2*   F2**  G2
          |     |     |     |           |
onda 3   A3    C3    D3    E3*         G3
          |     |           |           |
onda 4   A4    C4          F3          G4
          |                             |
onda 5   A5                            G5
          |                             |
onda 6   B1                            G6
                                        |
onda 7                                 H2
```

28 tasks: A1–A5, B1, C1–C4, D1–D3, E1–E3, F1–F3, G1–G7, H1–H2.
Chains: **A** estado por agente (hub/Python) · **B** tipos Go do shell ·
**C** criar sessão no hub · **D** `restoreSize()` · **E** lista agrupada ·
**F** formulário · **G** abas · **H** `LiveHub`.

- **A regra**: tasks que tocam o **mesmo arquivo de produção** são sequenciais; tasks em arquivos diferentes rodam em paralelo. Por isso o Python saiu do `tmux.go` (Task A1) — sem isso, as chains A, B e C brigariam pelo mesmo arquivo. É também por isso que dentro de uma chain nada é paralelo: G1→G2→G3→G4 são todas o mesmo `OpenTabs.swift`.
- **Onda 1 dispara 7 agentes ao mesmo tempo**: A1, C1, D1, F1, G1, G7, H1. E1 entra como oitavo assim que a `identidade-pane-ao-vivo` for mergeada.
- `*` = chain E travada pelo merge (ver Pré-requisito acima).
- `**` A Task F2 consome o tipo `GrupoAoVivo`, que a Task E1 cria. Se o merge atrasar, há saída sem esperar: `NovoTerminalFormLogic.gruposConhecidos` passa a receber `[String]` e quem chama (Task F3) faz o `map`.
- **Os dois cruzamentos entre chains, que é onde um merge dói:** `SessionListView.swift` é tocado por E2, E3, F3 **e** G6 — daí F3 estar na onda 4 (depois de E3) e G6 na 6. `CutuqueApp.swift` é tocado por G6 e H2 — daí H2 fechar a fila, mesmo sendo H1 independente de tudo e podendo sair já na onda 1.
- **Caminho crítico = chain G** (6 ondas) + H2. Todo o resto termina antes.
- Cada task termina com commit próprio. Branch por chain (`chain-a-estado-agente`, `chain-c-novo-terminal`, …) evita conflito de merge.

## Ordem de valor, se for uma pessoa só

Task D1→D2→D3 (o bug do `✕`, valor imediato) · A1→A5 (bolinha no codex e no opencode) ·
chain E (lista agrupada) · C→F (criar terminal) · G→H (abas, o caro).

---
## Chain A — estado do pane por agente (hub)

Contexto que o implementador não tem: a inferência de estado lê a **tela** do pane
(`capture-pane`), não os hooks — por isso sobrevive a restart do hub. Hoje ela vale
**só** para o Claude, e o código Python que a faz mora dentro de uma string Go
**sem um único teste**. A chain A resolve as duas coisas: primeiro torna o Python
testável, depois liga codex e opencode com marcadores calibrados por captura de tela
real em 12/08/2026.

### Task A1: Python testável — `classify` puro num arquivo próprio

Refactor sem mudança de comportamento. Objetivo: poder asserir a classificação a
partir de uma **tela de texto**, sem tmux e sem rede.

**Files:**
- Create: `hub/internal/adapter/claudecode/tmux_script.go`
- Create: `hub/internal/adapter/claudecode/tmux_screens_test.go`
- Create: `hub/internal/adapter/claudecode/tmux_script_test.go`
- Modify: `hub/internal/adapter/claudecode/tmux.go:15-101` (remove os consts, que passam para o arquivo novo)

**Interfaces:**
- Consumes: nada.
- Produces: `const tmuxStateFuncs string` (Python puro: `import re`, `work_re`, `classify(txt, agent)`), `const tmuxDriverScript string`, `const tmuxListScript = tmuxStateFuncs + tmuxDriverScript` (mesmo nome de hoje, então `TmuxList` de SSH e Local não muda), e o helper de teste `estadoDaTela(t *testing.T, tela, agente string) string`.

- [ ] **Step 1: Criar `tmux_screens_test.go` com as telas reais de 12/08**

````go
package claudecode

// Telas capturadas de verdade em 12/08/2026 com `tmux capture-pane -p`, uma por
// estado e por agente. São a fixture de toda a chain A: quando uma TUI mudar de
// string, é aqui que se recalibra (método gravado em
// memory/cutuque/backend/Backend — Estado do Pane por Agente (claude, codex, opencode).md).
//
// Versões calibradas: claude 2.1.228 · codex 0.147.0 · opencode 1.18.16.

const telaClaudeTrabalhando = `> resumir o arquivo

✻ Pondering… (8s · ↑ 1.2k tokens · esc to interrupt)
`

const telaClaudeEsperando = `Do you want to proceed?
❯ 1. Yes
  2. No, and tell Claude what to do differently
`

// Ocioso do Claude: o status é PASSADO ("Cogitated for 10s"), sem parênteses de
// timer vivo — é por isso que work_re exige o "\(".
const telaClaudeOciosa = `⏺ Pronto, resumi o arquivo.

  Cogitated for 10s
>
`

// codex trabalhando: timer vivo confirmado (29s → 31s → 33s em leituras de 2s).
// Casa nas DUAS regras do Claude: work_re em "(29s" e "esc to interrupt".
const telaCodexTrabalhando = `• Working (29s • esc to interrupt)

  ⋮ lendo o arquivo
`

const telaCodexOciosa = `› pergunte algo

  gpt-5-codex · ~/Desktop/coding/personal/cutuque
`

// O portão de confiança: sessão TRAVADA esperando tecla, para sempre. Nada aqui
// casa com as regras atuais, então hoje é classificada como "idle" (verde,
// "concluído") — o caso que o formulário de novo terminal vai criar toda hora.
const telaCodexPortaoDeConfianca = `Do you trust the contents of this directory?

› 1. Yes, continue
  2. No, quit
`

// opencode trabalhando: "esc interrupt" SEM o "to", mais o spinner de blocos.
const telaOpencodeTrabalhando = `⬝⬝⬝■■■  Build

  esc interrupt
`

// opencode ocioso DEPOIS de concluir: imprime a duração ("· 3.2s"). Sem o "\(" no
// work_re, esta tela viraria "running" para sempre. É a armadilha número 1.
const telaOpencodeOciosa = `▣ Build · claude-sonnet-4-5 · 3.2s

  Ask anything...
  tab agents  ctrl+p commands
`
````

- [ ] **Step 2: Escrever o teste que falha — `tmux_script_test.go`**

O helper é o coração da chain: executa **só** o Python puro, passando a tela pelo
stdin. Nada de tmux, nada de rede.

````go
package claudecode

import (
	"os/exec"
	"strconv"
	"strings"
	"testing"
)

// estadoDaTela roda a parte PURA do script (tmuxStateFuncs) contra uma tela de
// fixture. É o que permite testar a inferência sem tmux: o `classify` não captura
// nada, recebe texto. Se o python3 do sistema não existir, o teste pula — o script
// remoto também depende dele, então não há o que garantir aqui.
func estadoDaTela(t *testing.T, tela, agente string) string {
	t.Helper()
	if _, err := exec.LookPath("python3"); err != nil {
		t.Skip("python3 ausente")
	}
	prog := tmuxStateFuncs + "\nimport sys\nprint(classify(sys.stdin.read()," + strconv.Quote(agente) + "))\n"
	cmd := exec.Command("python3", "-c", prog)
	cmd.Stdin = strings.NewReader(tela)
	out, err := cmd.Output()
	if err != nil {
		t.Fatalf("python3 falhou: %v", err)
	}
	return strings.TrimSpace(string(out))
}

func TestClassifyClaude(t *testing.T) {
	casos := []struct {
		nome, tela, quer string
	}{
		{"trabalhando pelo timer vivo", telaClaudeTrabalhando, "running"},
		{"esperando no diálogo", telaClaudeEsperando, "waiting"},
		{"ociosa não casa com work_re", telaClaudeOciosa, "idle"},
	}
	for _, c := range casos {
		t.Run(c.nome, func(t *testing.T) {
			if got := estadoDaTela(t, c.tela, "claude"); got != c.quer {
				t.Fatalf("classify = %q, queria %q", got, c.quer)
			}
		})
	}
}
````

- [ ] **Step 3: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run TestClassifyClaude -v`
Expected: FAIL — `undefined: tmuxStateFuncs` (o const ainda não existe).

- [ ] **Step 4: Criar `tmux_script.go` com a parte pura**

````go
package claudecode

// tmuxStateFuncs é a parte PURA do script de varredura: recebe a tela do pane como
// texto e devolve o estado. Vive separada do driver por dois motivos — é a única
// parte que dá para testar sem tmux (ver tmux_script_test.go), e é a que muda
// quando uma TUI de agente muda de string.
//
// Estados:
//   - 'running' : trabalhando agora. Sinal principal = o TIMER VIVO do spinner
//                 entre parênteses ("(8s" / "(4m 18s"), que aparece enquanto gera
//                 ou roda ferramenta e some ao ficar ocioso. "esc to interrupt"
//                 NÃO aparece no modo bypass-permissions do Claude, então não dá
//                 para depender só dele; "agent(s) to finish" = espera subagente.
//   - 'waiting' : parado num diálogo de permissão/escolha ("Do you want to...").
//   - 'idle'    : ocioso no prompt = concluiu o turno (o "concluído" → verde).
//
// O "\(" do work_re NÃO é detalhe: o status ocioso do Claude é passado
// ("Cogitated for 10s") e o opencode imprime a duração DEPOIS de concluir
// ("▣ Build · <modelo> · 3.2s"). Sem o parêntese, as duas telas ociosas viram
// 'running' para sempre. Não afrouxe — há teste para isso.
const tmuxStateFuncs = `import re
work_re=re.compile(r'\((?:\d+m )?\d+s')
def classify(txt,agent):
    low=txt.lower()
    if work_re.search(txt) or 'esc to interrupt' in low or 'agent to finish' in low or 'agents to finish' in low:
        return 'running'
    if 'do you want to proceed' in low or 'do you want to make this edit' in low:
        return 'waiting'
    return 'idle'
`
````

O parâmetro `agent` já entra aqui, ainda sem uso: hoje o portão do driver
(`if ag=='claude'`) garante que só o Claude chega, e a task A2 troca o corpo pela
tabela por agente sem mexer na assinatura nem nestes testes.

- [ ] **Step 5: Mover o driver para `tmux_script.go`**

Recortar de `tmux.go` as linhas **20–101** (o comentário de `tmuxListScript` e o
const inteiro) e colar em `tmux_script.go`, **abaixo** de `tmuxStateFuncs`,
renomeando o const para `tmuxDriverScript` e aplicando exatamente duas mudanças no
corpo Python:

1. `pane_state` perde a classificação e ganha o agente — as 20 linhas de comentário
   que hoje moram nela (`tmux.go:61-72`) **já subiram** para o comentário Go do
   `tmuxStateFuncs`, então aqui fica só a leitura:

```python
def pane_state(sock,pane,agent):
    # Lê a tela visível do pane e delega a classificação para classify (parte pura).
    return classify(run('tmux','-S',sock,'capture-pane','-t',pane,'-p'),agent)
```

2. a chamada no laço (`tmux.go:98`) passa o agente:

```python
        st=pane_state(sock,f[0],ag) if ag=='claude' else ''
```

A linha `work_re=re.compile(...)` (`tmux.go:59`) **sai** do driver — agora vive em
`tmuxStateFuncs`. O `import re` duplicado nos dois consts é inofensivo.

Fechar o arquivo com a composição, que preserva o nome usado pelos dois `TmuxList`:

````go
// tmuxListScript é o que roda de fato na máquina remota (via `python3 -`): as
// funções puras mais o driver que varre os servidores. Concatenado, e não um
// arquivo único, porque só a primeira metade é testável isoladamente.
const tmuxListScript = tmuxStateFuncs + tmuxDriverScript
````

- [ ] **Step 6: Rodar tudo — o novo passa e nada quebra**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS, incluindo os 6 testes que já existiam em `tmux_test.go`
(`TestParseTmuxJSON`, `TestParseTmuxJSONState`, `TestParseTmuxJSONEmpty`,
`TestTmuxPaneAsDiscoveredTitle`, `TestParseTarget`, `TestValidKillSocket`).

- [ ] **Step 7: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_script.go \
        hub/internal/adapter/claudecode/tmux_script_test.go \
        hub/internal/adapter/claudecode/tmux_screens_test.go \
        hub/internal/adapter/claudecode/tmux.go
git commit -m "refactor(hub): separa a inferência de estado do pane num script testável"
```

### Task A2: tabela de marcadores por agente + opencode

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux_script.go` (o const `tmuxStateFuncs`)
- Test: `hub/internal/adapter/claudecode/tmux_state_opencode_test.go` (novo)

**Interfaces:**
- Consumes: `tmuxStateFuncs`, `classify(txt, agent)`, `estadoDaTela(t, tela, agente)` e as telas de fixture (Task A1).
- Produces: dentro do Python, `MARKERS` — dicionário `agente → {'running': [str], 'waiting': [str], 'timer': bool}`. Agente novo = **uma linha nova**, sem tocar em `classify`. Agente ausente da tabela → `classify` devolve `''` (neutro), que é o comportamento de hoje para quem não é Claude.

- [ ] **Step 1: Escrever o teste que falha**

````go
package claudecode

import "testing"

// O opencode 1.18.16 escreve "esc interrupt" — SEM o "to" do Claude. A checagem
// que existia (`'esc to interrupt' in low`) não o pegava, e trocar por um
// `'interrupt' in low` genérico seria frágil: por isso o marcador é POR AGENTE.
func TestClassifyOpencodeTrabalhando(t *testing.T) {
	if got := estadoDaTela(t, telaOpencodeTrabalhando, "opencode"); got != "running" {
		t.Fatalf("classify = %q, queria running (marcador 'esc interrupt', sem o 'to')", got)
	}
}

// A armadilha: o opencode imprime a duração DEPOIS de concluir ("· 3.2s"). Quem
// afrouxar o work_re tirando a exigência do "\(" faz esta tela virar running para
// sempre. Este teste é a guarda.
func TestClassifyOpencodeOciosaNaoViraRunning(t *testing.T) {
	if got := estadoDaTela(t, telaOpencodeOciosa, "opencode"); got != "idle" {
		t.Fatalf("classify = %q, queria idle — a duração pós-conclusão não é timer vivo", got)
	}
}

// Agente fora da tabela continua neutro, como hoje: melhor sem bolinha do que com
// a bolinha errada.
func TestClassifyAgenteDesconhecidoEhNeutro(t *testing.T) {
	if got := estadoDaTela(t, telaClaudeTrabalhando, "gemini"); got != "" {
		t.Fatalf("classify = %q, queria vazio", got)
	}
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run 'TestClassifyOpencode|TestClassifyAgenteDesconhecido' -v`
Expected: FAIL — `TestClassifyOpencodeTrabalhando` devolve `idle` (marcador do "to")
e `TestClassifyAgenteDesconhecidoEhNeutro` devolve `idle` (o `agent` é ignorado).

- [ ] **Step 3: Trocar o corpo de `classify` pela tabela**

Substituir o `const tmuxStateFuncs` inteiro (mantendo o comentário Go acima dele,
que continua valendo) por:

````go
const tmuxStateFuncs = `import re
work_re=re.compile(r'\((?:\d+m )?\d+s')
# Marcadores por agente, calibrados por captura de tela em 12/08/2026
# (claude 2.1.228 · codex 0.147.0 · opencode 1.18.16). Agente novo = linha nova.
# 'timer' liga a regra do work_re (parêntese + Ns), que é o sinal mais forte de
# "trabalhando agora" nas TUIs que mostram spinner com cronômetro.
MARKERS={
 'claude':{'timer':True,
           'running':['esc to interrupt','agent to finish','agents to finish'],
           'waiting':['do you want to proceed','do you want to make this edit']},
 'codex':{'timer':True,
          'running':['esc to interrupt'],
          'waiting':[]},
 'opencode':{'timer':False,
             'running':['esc interrupt'],
             'waiting':[]},
}
def classify(txt,agent):
    m=MARKERS.get(agent)
    if not m: return ''
    low=txt.lower()
    if m['timer'] and work_re.search(txt): return 'running'
    for s in m['running']:
        if s in low: return 'running'
    for s in m['waiting']:
        if s in low: return 'waiting'
    return 'idle'
`
````

Notas que o implementador precisa entender antes de "simplificar":

- **codex reusa os marcadores do Claude e isso é evidência, não preguiça.** A tela
  real de uma sessão trabalhando é `• Working (29s • esc to interrupt)`: casa em
  `work_re` **e** em `esc to interrupt`.
- **`opencode` tem `timer:False`** de propósito. Ele nunca mostra timer vivo entre
  parênteses e **mostra** `· 3.2s` ao concluir; desligar a regra do timer para ele é
  a segunda guarda contra o falso positivo. `esc interrupt` já cobre o `running`.
- **`waiting` vazio em codex e opencode** reflete a configuração da Vanessa:
  `opencode.json` com `"permission": {"*": "allow", …}` e `tmx cx` rodando
  `--sandbox danger-full-access`. Nenhum dos dois para para pedir aprovação de
  ferramenta. (A linha do codex ganha um marcador na Task A3 — mas de **portão de
  confiança**, que é outra coisa.)

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS — os três novos e os três de `TestClassifyClaude` (a linha do claude
na tabela reproduz exatamente a lógica anterior).

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_script.go \
        hub/internal/adapter/claudecode/tmux_state_opencode_test.go
git commit -m "feat(hub): tabela de marcadores por agente e deteccao do opencode"
```

### Task A3: portão de confiança do codex vira `waiting`

O caso que a feature nova cria: sessão de codex aberta numa pasta que ele não
conhece para em `Do you trust the contents of this directory?` e fica **travada
esperando uma tecla, para sempre**. As regras atuais chamam isso de `idle` — bolinha
verde, "concluído". O formulário de criação escolhe pasta arbitrária, então **isso
vai acontecer**.

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux_script.go` (linha `'codex'` de `MARKERS`)
- Test: `hub/internal/adapter/claudecode/tmux_state_codex_test.go` (novo)

**Interfaces:**
- Consumes: `MARKERS`, `classify` (A2), `telaCodexPortaoDeConfianca`, `telaCodexTrabalhando`, `telaCodexOciosa` (A1).
- Produces: nenhuma assinatura nova — só uma string a mais na tabela.

- [ ] **Step 1: Escrever o teste que falha**

````go
package claudecode

import "testing"

// O codex 0.147.0 funciona com os marcadores do Claude: a tela real de trabalho é
// "• Working (29s • esc to interrupt)" — timer vivo confirmado (29s → 31s → 33s em
// leituras de 2s). Estes dois travam a linha do codex na tabela.
func TestClassifyCodexTrabalhandoEOciosa(t *testing.T) {
	if got := estadoDaTela(t, telaCodexTrabalhando, "codex"); got != "running" {
		t.Fatalf("trabalhando: classify = %q, queria running", got)
	}
	if got := estadoDaTela(t, telaCodexOciosa, "codex"); got != "idle" {
		t.Fatalf("ociosa: classify = %q, queria idle", got)
	}
}

// Travado no portão de confiança NÃO é concluído. Sem isto, a Vanessa vê bolinha
// verde numa sessão que não vai andar até alguém apertar 1.
func TestClassifyCodexPortaoDeConfiancaEhWaiting(t *testing.T) {
	if got := estadoDaTela(t, telaCodexPortaoDeConfianca, "codex"); got != "waiting" {
		t.Fatalf("classify = %q, queria waiting — a sessão está travada esperando tecla", got)
	}
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run TestClassifyCodex -v`
Expected: `TestClassifyCodexTrabalhandoEOciosa` PASS,
`TestClassifyCodexPortaoDeConfiancaEhWaiting` FAIL com `classify = "idle"`.

- [ ] **Step 3: Acrescentar o marcador na linha do codex**

````go
 'codex':{'timer':True,
          'running':['esc to interrupt'],
          # Portão de confiança de pasta nova: a sessão fica travada esperando
          # tecla, então é 'waiting', não 'idle'. O formulário de novo terminal
          # escolhe pasta arbitrária — este caso é rotina, não exceção.
          'waiting':['do you trust the contents of this directory']},
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS em tudo.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_script.go \
        hub/internal/adapter/claudecode/tmux_state_codex_test.go
git commit -m "feat(hub): portao de confianca do codex reportado como waiting"
```

### Task A4: tirar o portão `if ag=='claude'` do driver

É a task que **liga** o que A2 e A3 construíram: hoje, mesmo com a tabela pronta,
codex e opencode chegam ao app sem bolinha, porque o driver nunca chama `pane_state`
para eles.

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux_script.go` (`tmuxDriverScript`)
- Test: `hub/internal/adapter/claudecode/tmux_state_codex_test.go` (acrescenta um teste)

**Interfaces:**
- Consumes: `tmuxDriverScript` (A1), `MARKERS` (A2/A3).
- Produces: nada novo — muda o dado do driver.

- [ ] **Step 1: Escrever o teste que falha**

O driver não roda em teste unitário (varre sockets de verdade e chama `tmux`), então
a asserção é sobre o **conteúdo do const**, que é dado, não comportamento. É uma
guarda contra alguém reintroduzir o portão sem perceber.

````go
// O driver precisa inferir estado para TODO agente detectado. O portão original
// existia porque os marcadores do codex/opencode não eram conhecidos — passaram a
// ser, calibrados por captura em 12/08/2026 (ver tmux_screens_test.go).
func TestDriverNaoRestringeEstadoAoClaude(t *testing.T) {
	if strings.Contains(tmuxDriverScript, "if ag=='claude'") {
		t.Fatal("o portão que limitava o estado ao Claude voltou ao driver")
	}
	if !strings.Contains(tmuxDriverScript, "st=pane_state(sock,f[0],ag)") {
		t.Fatal("o driver precisa passar o agente para pane_state")
	}
}
````

Acrescentar `"strings"` ao import do arquivo.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run TestDriverNaoRestringe -v`
Expected: FAIL — "o portão que limitava o estado ao Claude voltou ao driver".

- [ ] **Step 3: Trocar a linha e REESCREVER o comentário**

O comentário de três linhas que hoje justifica o portão não é apagado — é reescrito
com a razão nova e a data, que é o padrão do projeto:

```python
        ag=agent_of(pid)
        if not ag: continue
        # Estado inferido para os três agentes. Os marcadores de cada TUI foram
        # calibrados por captura de tela em 12/08/2026 (claude 2.1.228,
        # codex 0.147.0, opencode 1.18.16) e vivem em MARKERS, não aqui —
        # agente novo é uma linha na tabela. Antes disto só o Claude tinha
        # estado, porque os marcadores dos outros dois eram desconhecidos.
        st=pane_state(sock,f[0],ag)
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS.

- [ ] **Step 5: Verificar contra o tmux de verdade**

Run: `cd hub && go build ./... && python3 -c "$(printf '%s' "$(go run ./cmd/tmuxscript 2>/dev/null || true)")" 2>/dev/null; echo ok`

Se não existir um `cmd/tmuxscript`, valide à mão numa sessão real: com uma sessão de
codex ou opencode aberta, o `GET /machines/{m}/live` do hub local passa a devolver
`state` não vazio para ela. **Não invente um comando novo no plano** — se a
verificação manual não for possível no momento, registre isso no commit e siga; a
Task A5 e a chain B não dependem dela.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_script.go \
        hub/internal/adapter/claudecode/tmux_state_codex_test.go
git commit -m "feat(hub): estado do pane para codex e opencode, nao so claude"
```

### Task A5: shells sem agente aparecem na lista, marcados (D11)

Sem isto, o terminal vazio criado pelo formulário **desaparece no refresh seguinte**
e não há como voltar nele.

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux_script.go` (`tmuxDriverScript`)
- Test: `hub/internal/adapter/claudecode/tmux_shell_test.go` (novo)

**Interfaces:**
- Consumes: `tmuxDriverScript` (A1).
- Produces: cada item do JSON ganha `kind`: `"agent"` quando `agent_of` achou um dos três, `"shell"` quando não achou. Pane de shell sai com `cmd:""` e `state:""`. Consumido pela Task B1.

- [ ] **Step 1: Escrever o teste que falha**

````go
package claudecode

import (
	"strings"
	"testing"
)

// D11: o pane sem agente deixa de ser descartado e passa a sair marcado como
// "shell". Guarda no conteúdo do driver — ele não roda em teste unitário.
func TestDriverMantemShellMarcado(t *testing.T) {
	if strings.Contains(tmuxDriverScript, "if not ag: continue") {
		t.Fatal("o driver ainda descarta pane sem agente; D11 pede que ele apareça marcado")
	}
	for _, s := range []string{"'kind':kind", "kind='agent' if ag else 'shell'"} {
		if !strings.Contains(tmuxDriverScript, s) {
			t.Fatalf("driver sem %q", s)
		}
	}
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run TestDriverMantemShell -v`
Expected: FAIL — "o driver ainda descarta pane sem agente".

- [ ] **Step 3: Implementar no driver**

Trocar as linhas do laço (as que a Task A4 deixou) por:

```python
        ag=agent_of(pid)
        # D11: pane sem agente NÃO é descartado — é um shell, e precisa aparecer,
        # senão o terminal vazio criado pelo app desaparece no refresh seguinte.
        kind='agent' if ag else 'shell'
        # Estado inferido para os três agentes. Os marcadores de cada TUI foram
        # calibrados por captura de tela em 12/08/2026 (claude 2.1.228,
        # codex 0.147.0, opencode 1.18.16) e vivem em MARKERS, não aqui —
        # agente novo é uma linha na tabela. Shell não tem TUI para ler: sai sem
        # estado, sem nem tentar capturar.
        st=pane_state(sock,f[0],ag) if ag else ''
        out.append({'id':sock+'\t'+f[0],'socket':sock,'pane':f[0],'cmd':ag,'kind':kind,'cwd':f[2],'session':f[3],'window':f[4],'state':st})
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS. `TestDriverNaoRestringeEstadoAoClaude` continua passando — a
substring `st=pane_state(sock,f[0],ag)` segue presente.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_script.go \
        hub/internal/adapter/claudecode/tmux_shell_test.go
git commit -m "feat(hub): pane de shell aparece na varredura marcado como shell"
```

## Chain B — o `kind` do lado Go

### Task B1: `TmuxPane.Kind` e `Discovered.Kind`

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux.go:111-118` (struct `TmuxPane`) e `:382-392` (`TmuxPaneAsDiscovered`)
- Modify: `hub/internal/session/session.go:94-108` (struct `Discovered`)
- Test: `hub/internal/adapter/claudecode/tmux_test.go` (acrescenta dois testes)

**Interfaces:**
- Consumes: o JSON com `kind` da Task A5.
- Produces: `TmuxPane.Kind string` (json `kind`), `session.Discovered.Kind string` (json `kind,omitempty`), e `TmuxPaneAsDiscovered` repassando o campo. Consumido pelo app na Task F3 (`DiscoveredSession.kind`) — o app precisa decodificar tolerante, porque `omitempty` faz o hub antigo não mandar nada.

- [ ] **Step 1: Escrever os testes que falham**

````go
// O kind vem do script (D11) e precisa atravessar até o Discovered — é o que
// permite o app mostrar o terminal vazio marcado em vez de escondê-lo.
func TestParseTmuxJSONKind(t *testing.T) {
	out := []byte(`[{"id":"/tmp/tmux-501/main\tpane1","cmd":"","kind":"shell","cwd":"/x","session":"s","window":"w","state":""}]`)
	panes := parseTmuxJSON(out)
	if len(panes) != 1 {
		t.Fatalf("panes = %d, queria 1", len(panes))
	}
	if panes[0].Kind != "shell" {
		t.Fatalf("Kind = %q, queria shell", panes[0].Kind)
	}
}

// Resposta de um script antigo (sem kind) não pode virar erro nem string aleatória:
// campo ausente é string vazia, e quem consome trata vazio como "agent".
func TestTmuxPaneAsDiscoveredKind(t *testing.T) {
	d := TmuxPaneAsDiscovered(TmuxPane{ID: "s\t%1", Cwd: "/x", Session: "sess", Kind: "shell"})
	if d.Kind != "shell" {
		t.Fatalf("Discovered.Kind = %q, queria shell", d.Kind)
	}
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run 'Kind' -v`
Expected: FAIL na compilação — `unknown field Kind in struct literal`.

- [ ] **Step 3: Implementar**

Em `tmux.go`, dentro de `TmuxPane` (depois de `Cmd`):

```go
	Kind    string `json:"kind"`    // "agent" (um dos três) | "shell" (pane sem agente)
```

**E dentro de `parseTmuxJSON` (`tmux.go:145-161`), nos dois lugares** — a struct
anônima do `Unmarshal` não tem tags, casa por nome (case-insensitive), então sem isto
o campo é silenciosamente descartado e o teste falha sem dizer por quê:

```go
	var raw []struct {
		ID, Cmd, Kind, Cwd, Session, Window, State string
	}
	...
		panes = append(panes, TmuxPane{ID: r.ID, Cmd: r.Cmd, Kind: r.Kind, Cwd: r.Cwd, Session: r.Session, Window: r.Window, State: r.State})
```

Em `TmuxPaneAsDiscovered`, no literal de retorno:

```go
	return session.Discovered{ID: p.ID, Cwd: p.Cwd, Title: title, State: p.State, Agent: p.Cmd, Kind: p.Kind}
```

Em `session.go`, dentro de `Discovered`:

```go
	// Kind separa pane de agente de pane de shell puro ("agent"|"shell"). Vazio
	// em tudo que não vem do tmux — o app trata vazio como "agent".
	Kind string `json:"kind,omitempty"`
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./... `
Expected: PASS em todos os pacotes.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux.go \
        hub/internal/adapter/claudecode/tmux_test.go \
        hub/internal/session/session.go
git commit -m "feat(hub): kind do pane atravessa ate o Discovered"
```

## Chain C — criar sessão tmux no hub

Contexto: o **grupo é o socket**. `tmux -L <nome>` cria o servidor na hora, sem
registro nenhum — é por isso que D13 diz que grupo novo não tem cerimônia. O código
que já existe fala em `-S <caminho>` porque a varredura acha os sockets pelo caminho;
na **criação** usamos `-L <grupo>`, que é o que o `tmx.sh` faz, e devolvemos o
caminho real do socket para o app poder falar com o pane depois.

Nada aqui depende das chains A/B: `tmux_new.go` é arquivo novo e o único ponto de
contato com `tmux.go` é **uma linha** na interface `Tmuxer`.

### Task C1: montagem e validação do `new-session` (puro)

**Files:**
- Create: `hub/internal/adapter/claudecode/tmux_new.go`
- Create: `hub/internal/adapter/claudecode/tmux_new_test.go`

**Interfaces:**
- Consumes: `tmuxPaneIDPattern`, `tmuxSocketPattern`, `parseTarget`, `singleQuote` (todos já em `tmux.go`).
- Produces:
  - `var tmuxNewSessionAgents map[string]string` — agente → comando do pane.
  - `type tmuxNewSpec struct { Group, Session, Cwd, Command string }`
  - `func validarNovaSessao(group, session, cwd, agent string) (tmuxNewSpec, error)`
  - `func (n tmuxNewSpec) localArgs() []string`
  - `func (n tmuxNewSpec) sshInner() string`
  - `func parseNovaSessao(out []byte) (target string, err error)`

- [ ] **Step 1: Escrever o teste que falha**

````go
package claudecode

import (
	"strings"
	"testing"
)

func TestValidarNovaSessaoAgentes(t *testing.T) {
	casos := []struct{ agente, querCmd string }{
		{"claude", "claude"},
		// D10: o botão do codex copia o `tmx cx` — mesma flag, para app e terminal
		// darem a mesma coisa.
		{"codex", "codex --sandbox danger-full-access"},
		{"opencode", "opencode"},
		// D8: o terminal livre nasce sem agente nenhum, só o shell.
		{"terminal", ""},
	}
	for _, c := range casos {
		spec, err := validarNovaSessao("defender", "mike", "/Users/vanessa/x", c.agente)
		if err != nil {
			t.Fatalf("%s: erro inesperado: %v", c.agente, err)
		}
		if spec.Command != c.querCmd {
			t.Fatalf("%s: Command = %q, queria %q", c.agente, spec.Command, c.querCmd)
		}
	}
}

func TestValidarNovaSessaoRecusaNomeInvalido(t *testing.T) {
	// D12 é a camada de UX; esta é a defesa em profundidade. O tmux usa ":" e "."
	// como separador de alvo, e o socket viaja como caminho — nome com esses
	// caracteres viraria alvo ambíguo, não erro bonito.
	casos := [][2]string{
		{"defender:1", "mike"},
		{"defender", "mike.aux"},
		{"", "mike"},
		{"defender", ""},
		{"../etc", "mike"},
		{"defender", "mike aux"},
	}
	for _, c := range casos {
		if _, err := validarNovaSessao(c[0], c[1], "/Users/vanessa/x", "claude"); err == nil {
			t.Fatalf("grupo=%q sessão=%q: queria erro", c[0], c[1])
		}
	}
}

func TestValidarNovaSessaoRecusaPastaEAgente(t *testing.T) {
	if _, err := validarNovaSessao("defender", "mike", "relativo/x", "claude"); err == nil {
		t.Fatal("pasta relativa: queria erro")
	}
	if _, err := validarNovaSessao("defender", "mike", "/x", "gemini"); err == nil {
		t.Fatal("agente desconhecido: queria erro")
	}
}

func TestLocalArgsESshInner(t *testing.T) {
	spec, err := validarNovaSessao("defender", "mike", "/Users/vanessa/x", "codex")
	if err != nil {
		t.Fatal(err)
	}
	args := spec.localArgs()
	junto := strings.Join(args, " ")
	// -A anexa se a sessão já existir (não é erro: a spec diz que o app abre a aba
	// nela). -d não anexa o terminal do hub. -P -F devolve socket e pane.
	for _, s := range []string{"-L defender", "new-session -A -d", "-s mike", "-c /Users/vanessa/x", "codex --sandbox danger-full-access"} {
		if !strings.Contains(junto, s) {
			t.Fatalf("localArgs sem %q: %v", s, args)
		}
	}
	if !strings.Contains(spec.sshInner(), "'codex --sandbox danger-full-access'") {
		t.Fatalf("sshInner sem o comando entre aspas: %s", spec.sshInner())
	}
}

func TestParseNovaSessao(t *testing.T) {
	// O socket vem como caminho; /private/tmp e /tmp são o MESMO dir no macOS, e a
	// varredura normaliza tirando o /private. Se a criação não normalizar igual, o
	// alvo devolvido não casa com nenhuma linha da lista ao vivo.
	got, err := parseNovaSessao([]byte("/private/tmp/tmux-501/defender\t%42\n"))
	if err != nil {
		t.Fatal(err)
	}
	if got != "/tmp/tmux-501/defender\t%42" {
		t.Fatalf("target = %q", got)
	}
	if _, err := parseNovaSessao([]byte("lixo")); err == nil {
		t.Fatal("saída sem tab: queria erro")
	}
	if _, err := parseNovaSessao([]byte("/tmp/tmux-501/x\tpane1")); err == nil {
		t.Fatal("pane fora do formato %N: queria erro")
	}
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run 'NovaSessao|LocalArgs' -v`
Expected: FAIL na compilação — `undefined: validarNovaSessao`.

- [ ] **Step 3: Implementar `tmux_new.go`**

````go
package claudecode

import (
	"fmt"
	"regexp"
	"strings"
)

// tmuxNewSessionAgents mapeia o agente escolhido no app para o comando que roda no
// pane. Copiado do scripts/tmx.sh da Vanessa (claude :153, codex :164,
// opencode :175, shell puro :59) para que app e terminal dêem a MESMA coisa — D10.
// Comando vazio = shell, que é o terminal livre da D8.
var tmuxNewSessionAgents = map[string]string{
	"claude":   "claude",
	"codex":    "codex --sandbox danger-full-access",
	"opencode": "opencode",
	"terminal": "",
}

// tmuxNamePattern valida nome de grupo e de sessão. O tmux usa ":" e "." como
// separador de alvo (sessão:janela.pane) e o grupo virá um dia num caminho de
// socket — nome fora deste alfabeto produz alvo ambíguo, não erro legível. É a
// mesma regra que o formulário do app aplica no teclado (D12).
var tmuxNamePattern = regexp.MustCompile(`^[A-Za-z0-9_-]+$`)

// tmuxAbsPathPattern exige pasta absoluta e de alfabeto seguro (mesmo conjunto do
// tmuxSocketPattern) antes de interpolar num comando remoto.
var tmuxAbsPathPattern = regexp.MustCompile(`^/[A-Za-z0-9._/ -]+$`)

// tmuxNewSpec é uma criação já validada: nada além disto chega a virar comando.
type tmuxNewSpec struct {
	Group   string // = o socket do tmux (-L), e o mesmo identificador do escopo do board
	Session string
	Cwd     string
	Command string // vazio = shell puro
}

// tmuxNewFormat pede de volta o caminho do socket e o pane criado, que juntos são o
// alvo composto que o resto do hub usa ("<socket>\t<pane>").
const tmuxNewFormat = "#{socket_path}\t#{pane_id}"

func validarNovaSessao(group, session, cwd, agent string) (tmuxNewSpec, error) {
	if !tmuxNamePattern.MatchString(group) {
		return tmuxNewSpec{}, fmt.Errorf("claudecode: grupo inválido: %q", group)
	}
	if !tmuxNamePattern.MatchString(session) {
		return tmuxNewSpec{}, fmt.Errorf("claudecode: sessão inválida: %q", session)
	}
	if !tmuxAbsPathPattern.MatchString(cwd) {
		return tmuxNewSpec{}, fmt.Errorf("claudecode: pasta inválida: %q", cwd)
	}
	cmd, ok := tmuxNewSessionAgents[agent]
	if !ok {
		return tmuxNewSpec{}, fmt.Errorf("claudecode: agente desconhecido: %q", agent)
	}
	return tmuxNewSpec{Group: group, Session: session, Cwd: cwd, Command: cmd}, nil
}

// localArgs monta os args do tmux local. -A anexa se a sessão já existir (a spec
// trata "já existe" como sucesso, não erro) e -d evita anexar o terminal do hub.
func (n tmuxNewSpec) localArgs() []string {
	args := []string{"-L", n.Group, "new-session", "-A", "-d", "-s", n.Session,
		"-c", n.Cwd, "-P", "-F", tmuxNewFormat}
	if n.Command != "" {
		args = append(args, n.Command)
	}
	return args
}

func (n tmuxNewSpec) sshInner() string {
	inner := "tmux -L " + singleQuote(n.Group) + " new-session -A -d -s " + singleQuote(n.Session) +
		" -c " + singleQuote(n.Cwd) + " -P -F " + singleQuote(tmuxNewFormat)
	if n.Command != "" {
		inner += " " + singleQuote(n.Command)
	}
	return inner
}

// parseNovaSessao converte a saída do -P -F no alvo composto. Normaliza o
// /private/tmp para /tmp igual ao `norm` do script de varredura — senão o alvo
// devolvido aqui não casaria com nenhuma linha da lista ao vivo.
func parseNovaSessao(out []byte) (string, error) {
	linha := strings.TrimSpace(string(out))
	if i := strings.IndexByte(linha, '\n'); i >= 0 {
		linha = strings.TrimSpace(linha[:i])
	}
	partes := strings.SplitN(linha, "\t", 2)
	if len(partes) != 2 {
		return "", fmt.Errorf("claudecode: saída inesperada do new-session: %q", linha)
	}
	socket := strings.TrimPrefix(partes[0], "/private")
	target := socket + "\t" + partes[1]
	if _, _, err := parseTarget(target); err != nil {
		return "", err
	}
	return target, nil
}
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux_new.go \
        hub/internal/adapter/claudecode/tmux_new_test.go
git commit -m "feat(hub): validacao e montagem do new-session do tmux"
```

### Task C2: `TmuxNewSession` no `Tmuxer` (SSH e Local)

**Files:**
- Modify: `hub/internal/adapter/claudecode/tmux.go:124-132` (uma linha na interface)
- Modify: `hub/internal/adapter/claudecode/tmux_new.go` (os dois métodos, no fim do arquivo)
- Test: `hub/internal/adapter/claudecode/tmux_new_test.go` (acrescenta um teste)

**Interfaces:**
- Consumes: `validarNovaSessao`, `parseNovaSessao`, `tmuxNewSpec.localArgs`, `tmuxNewSpec.sshInner` (C1); `runSSHTmux` (`tmux.go:212`), `childEnv`.
- Produces: `TmuxNewSession(ctx context.Context, group, session, cwd, agent string) (target string, err error)` na interface `Tmuxer` e nos dois targets. Consumido pela Task C3.

- [ ] **Step 1: Escrever o teste que falha**

````go
// Guarda de contrato: os dois targets satisfazem o Tmuxer com o método novo. Sem
// isto, só o handler quebraria — e bem mais tarde.
func TestTargetsImplementamTmuxNewSession(t *testing.T) {
	var _ Tmuxer = (*SSHTarget)(nil)
	var _ Tmuxer = (*LocalTarget)(nil)
}

// Nome inválido nem chega a virar comando: falha antes de qualquer I/O.
func TestTmuxNewSessionValidaAntesDeExecutar(t *testing.T) {
	var alvo LocalTarget
	if _, err := alvo.TmuxNewSession(context.Background(), "grupo:ruim", "s", "/x", "claude"); err == nil {
		t.Fatal("queria erro de validação")
	}
}
````

Acrescentar `"context"` ao import do arquivo de teste.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/adapter/claudecode/ -run TmuxNewSession -v`
Expected: FAIL na compilação — `alvo.TmuxNewSession undefined`.

- [ ] **Step 3: Implementar**

Na interface `Tmuxer` (`tmux.go`), depois de `TmuxList`:

```go
	TmuxNewSession(ctx context.Context, group, session, cwd, agent string) (string, error)
```

No fim de `tmux_new.go` (com `"context"`, `"os/exec"` e `"strings"` no import):

````go
// TmuxNewSession cria (ou anexa, pelo -A) uma sessão no servidor do grupo e devolve
// o alvo composto do pane. O grupo é o socket: `tmux -L <grupo>` cria o servidor na
// hora, sem registro nenhum — é por isso que grupo novo não tem cerimônia (D13).
func (t *SSHTarget) TmuxNewSession(ctx context.Context, group, session, cwd, agent string) (string, error) {
	spec, err := validarNovaSessao(group, session, cwd, agent)
	if err != nil {
		return "", err
	}
	out, err := t.runSSHTmux(ctx, spec.sshInner())
	if err != nil {
		return "", err
	}
	return parseNovaSessao(out)
}

func (t *LocalTarget) TmuxNewSession(ctx context.Context, group, session, cwd, agent string) (string, error) {
	spec, err := validarNovaSessao(group, session, cwd, agent)
	if err != nil {
		return "", err
	}
	cmd := exec.CommandContext(ctx, "tmux", spec.localArgs()...)
	cmd.Env = childEnv()
	out, err := cmd.Output()
	if err != nil {
		return "", err
	}
	return parseNovaSessao(out)
}
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./... && go build ./...`
Expected: PASS. Se outro pacote tiver um dublê que implementa `Tmuxer`, ele quebra
aqui — implemente o método no dublê devolvendo `("", nil)`.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/adapter/claudecode/tmux.go \
        hub/internal/adapter/claudecode/tmux_new.go \
        hub/internal/adapter/claudecode/tmux_new_test.go
git commit -m "feat(hub): TmuxNewSession nos targets ssh e local"
```

### Task C3: `Launcher.TmuxNewSession`

**Files:**
- Modify: `hub/internal/launcher/launcher.go` (novo método logo depois de `TmuxList`, `:324`)
- Test: `hub/internal/launcher/launcher_test.go` (acrescenta um teste)

**Interfaces:**
- Consumes: `claudecode.Tmuxer.TmuxNewSession` (C2), `l.anyTarget`, `l.baseCtx`, `discoverTimeout`, `ErrUnknownMachine`, `ErrDiscoverFailed`.
- Produces: `func (l *Launcher) TmuxNewSession(machine, group, session, cwd, agent string) (string, error)`. Consumido pela Task C4.

- [ ] **Step 1: Escrever o teste que falha**

````go
// Máquina que não existe não pode virar erro genérico: o handler traduz
// ErrUnknownMachine em 404 e qualquer outra coisa em 502.
func TestTmuxNewSessionMaquinaDesconhecida(t *testing.T) {
	l := &Launcher{}
	if _, err := l.TmuxNewSession("naoexiste", "defender", "mike", "/x", "claude"); !errors.Is(err, ErrUnknownMachine) {
		t.Fatalf("err = %v, queria ErrUnknownMachine", err)
	}
}
````

Se `launcher_test.go` ainda não importa `errors`, acrescente.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/launcher/ -run TmuxNewSession -v`
Expected: FAIL na compilação — `l.TmuxNewSession undefined`.

- [ ] **Step 3: Implementar (copiando o padrão do `TmuxList`, `:304`)**

````go
// TmuxNewSession cria uma sessão tmux na máquina e devolve o alvo composto do pane
// criado ("<socket>\t<pane>"), pronto para capture/send-keys. Grupo = servidor tmux
// (-L), que é o mesmo identificador do escopo do board.
func (l *Launcher) TmuxNewSession(machine, group, sess, cwd, agent string) (string, error) {
	tgt, ok := l.anyTarget(machine)
	if !ok {
		return "", ErrUnknownMachine
	}
	tm, ok := tgt.(claudecode.Tmuxer)
	if !ok {
		return "", ErrUnknownMachine
	}
	ctx, cancel := context.WithTimeout(l.baseCtx, discoverTimeout)
	defer cancel()
	target, err := tm.TmuxNewSession(ctx, group, sess, cwd, agent)
	if err != nil {
		return "", fmt.Errorf("%w: %v", ErrDiscoverFailed, err)
	}
	return target, nil
}
````

O parâmetro chama-se `sess` e não `session` de propósito: `session` é o **pacote**
importado nesse arquivo, e sombreá-lo faria o resto do arquivo parar de compilar de
um jeito confuso.

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./internal/launcher/ -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/launcher/launcher.go hub/internal/launcher/launcher_test.go
git commit -m "feat(hub): Launcher.TmuxNewSession resolvendo a maquina"
```

### Task C4: rota `POST /machines/{machine}/tmux/new`

**Files:**
- Modify: `hub/internal/server/launch.go:41-48` (interface `Launcher`) e depois de `:173` (o handler)
- Modify: `hub/internal/server/server.go:170` (registro da rota)
- Test: `hub/internal/server/launch_test.go` (dublê + teste)

**Interfaces:**
- Consumes: `Launcher.TmuxNewSession` (C3), `writeJSONError`, `writeJSONResp` (`settings.go:114`), `maxLaunchBody`, `requireAuth`.
- Produces: rota `POST /machines/{machine}/tmux/new` com corpo `{"group","session","cwd","agent"}` → `200 {"target":"<socket>\t<pane>"}` · `400 bad_request` · `404 unknown_machine` · `502 tmux_failed`. Consumido pela Task F1 (cliente do app).

- [ ] **Step 1: Escrever o teste que falha**

````go
func TestTmuxNewSession(t *testing.T) {
	f := &fakeLauncher{tmuxNewTarget: "/tmp/tmux-501/defender\t%42"}
	rec := do(t, TmuxNewSessionHandler(f), http.MethodPost, "/machines/macbook/tmux/new",
		`{"group":"defender","session":"mike","cwd":"/Users/vanessa/x","agent":"codex"}`)
	if rec.Code != http.StatusOK {
		t.Fatalf("status = %d, queria 200", rec.Code)
	}
	if !strings.Contains(rec.Body.String(), `%42`) {
		t.Fatalf("corpo sem o alvo criado: %s", rec.Body.String())
	}
	if f.gotTmuxAgent != "codex" {
		t.Fatalf("agente = %q, queria codex", f.gotTmuxAgent)
	}
}

// Campo faltando é 400 e NÃO chega ao launcher — criar sessão é efeito colateral
// numa máquina de verdade.
func TestTmuxNewSessionCorpoIncompleto(t *testing.T) {
	f := &fakeLauncher{}
	rec := do(t, TmuxNewSessionHandler(f), http.MethodPost, "/machines/macbook/tmux/new",
		`{"group":"defender","session":"","cwd":"/x","agent":"codex"}`)
	if rec.Code != http.StatusBadRequest {
		t.Fatalf("status = %d, queria 400", rec.Code)
	}
	if f.gotTmuxAgent != "" {
		t.Fatal("o launcher foi chamado com corpo inválido")
	}
}
````

No `fakeLauncher` (`launch_test.go:137` em diante), acrescentar os campos e o método:

````go
	tmuxNewTarget string
	gotTmuxAgent  string
````

````go
func (f *fakeLauncher) TmuxNewSession(machine, group, sess, cwd, agent string) (string, error) {
	f.gotTmuxAgent = agent
	if f.err != nil {
		return "", f.err
	}
	return f.tmuxNewTarget, nil
}
````

(Use o nome do campo de erro que o `fakeLauncher` já tiver; se ele não tiver um,
devolva `f.tmuxNewTarget, nil` e pronto.)

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd hub && go test ./internal/server/ -run TmuxNewSession -v`
Expected: FAIL na compilação — `undefined: TmuxNewSessionHandler`.

- [ ] **Step 3: Implementar**

Na interface `Launcher` (`launch.go`), depois de `TmuxKillServer`:

```go
	TmuxNewSession(machine, group, sess, cwd, agent string) (string, error)
```

Depois do `TmuxKillServerHandler` (`launch.go:173`):

````go
// tmuxNewSessionRequest é o corpo de POST /machines/{machine}/tmux/new. group é o
// servidor tmux (-L) e também o escopo do board; agent é um de
// claude|codex|opencode|terminal ("terminal" = shell puro, D8).
type tmuxNewSessionRequest struct {
	Group   string `json:"group"`
	Session string `json:"session"`
	Cwd     string `json:"cwd"`
	Agent   string `json:"agent"`
}

// TmuxNewSessionHandler cria uma sessão tmux na máquina e devolve o alvo do pane.
// POST {"group","session","cwd","agent"} → 200 {"target"} | 400 | 404 | 502.
// Sessão que já existe NÃO é erro: o -A anexa e o app abre a aba nela.
func TmuxNewSessionHandler(lch Launcher) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		machine := r.PathValue("machine")
		r.Body = http.MaxBytesReader(w, r.Body, maxLaunchBody)
		var req tmuxNewSessionRequest
		if err := json.NewDecoder(r.Body).Decode(&req); err != nil ||
			req.Group == "" || req.Session == "" || req.Cwd == "" || req.Agent == "" {
			writeJSONError(w, http.StatusBadRequest, "bad_request")
			return
		}
		target, err := lch.TmuxNewSession(machine, req.Group, req.Session, req.Cwd, req.Agent)
		switch {
		case errors.Is(err, launcher.ErrUnknownMachine):
			writeJSONError(w, http.StatusNotFound, "unknown_machine")
		case err != nil:
			writeJSONError(w, http.StatusBadGateway, "tmux_failed")
		default:
			writeJSONResp(w, http.StatusOK, map[string]string{"target": target})
		}
	}
}
````

Em `server.go`, ao lado da linha `:170`:

```go
		mux.Handle("POST /machines/{machine}/tmux/new", requireAuth(cfg.Token, TmuxNewSessionHandler(lch)))
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd hub && go test ./... && go build ./...`
Expected: PASS.

- [ ] **Step 5: Verificar contra o hub local**

```bash
cd hub && go run ./cmd/hub &
curl -s -X POST localhost:8787/machines/local/tmux/new \
  -H "Authorization: Bearer $CUTUQUE_TOKEN" -H 'Content-Type: application/json' \
  -d '{"group":"cutuque-teste","session":"t1","cwd":"/tmp","agent":"terminal"}'
tmux -L cutuque-teste kill-server
```

Ajuste o nome da máquina ao que o hub local expõe. Se o hub local não subir sem
configuração, pule este passo e registre no commit — os testes cobrem o contrato.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add hub/internal/server/launch.go hub/internal/server/server.go \
        hub/internal/server/launch_test.go
git commit -m "feat(hub): rota POST /machines/{m}/tmux/new"
```

## Chain D — `restoreSize()` no `✕` e no background (F0 da spec)

O bug, para quem chega agora: `restoreSize()` (que manda `cols=0,rows=0` e devolve o
pane ao `window-size latest`) só é chamado no `onDisappear`
(`TerminalMirrorView.swift:305`). No **iPhone** o terminal é um `NavigationLink`
empurrado, então voltar sempre dispara. No **iPad** a view fica montada para sempre e
o `✕` só troca `nav.paneMode` — o `onDisappear` nunca roda e o pane fica preso em
`window-size manual`, na grade do iPad, no PC da Vanessa. **Não é intermitente: é
determinístico.**

Armadilha do conserto, verificada no código: `.task(id: resizeKey)` (`:262`) **não
reentra** quando só o `isActive` muda, porque `resizeKey` é `"\(cols)x\(rows)"`
(`:236`).

### Task D1: o estado de três valores (puro)

Hoje `isActive` é booleano, e booleano não distingue "troquei de aba" (mantém a
largura) de "fechei o terminal" (devolve a largura). Sem o estado do meio, cada troca
de aba viraria dois POSTs de resize.

**Files:**
- Create: `app/CutuqueApp/TerminalPaneState.swift`
- Create: `app/CutuqueAppTests/TerminalPaneStateTests.swift`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `enum TerminalPaneState: String, Codable, Equatable { case ativo, suspenso, liberado }`
  - `var fazPolling: Bool`
  - `static func devolveLargura(de: TerminalPaneState, para: TerminalPaneState) -> Bool`
  - `enum TerminalResizeKey { static func chave(cols: Int?, rows: Int?, estado: TerminalPaneState) -> String }`
  - Consumido pelas Tasks D2, D3, G2 e G6.

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

/// O estado de três valores existe porque booleano não distinguia "troquei de aba"
/// de "fechei o terminal": o primeiro MANTÉM a largura fixada (voltar para a aba não
/// precisa de dois POSTs de resize), o segundo DEVOLVE.
final class TerminalPaneStateTests: XCTestCase {

    func testSoOAtivoFazPolling() {
        XCTAssertTrue(TerminalPaneState.ativo.fazPolling)
        XCTAssertFalse(TerminalPaneState.suspenso.fazPolling)
        XCTAssertFalse(TerminalPaneState.liberado.fazPolling)
    }

    func testDevolveLarguraSoAoEntrarEmLiberado() {
        XCTAssertTrue(TerminalPaneState.devolveLargura(de: .ativo, para: .liberado))
        XCTAssertTrue(TerminalPaneState.devolveLargura(de: .suspenso, para: .liberado))
        // Suspender é trocar de aba: a largura FICA, senão voltar para a aba
        // remediria e reenviaria o resize a cada ida e volta.
        XCTAssertFalse(TerminalPaneState.devolveLargura(de: .ativo, para: .suspenso))
        XCTAssertFalse(TerminalPaneState.devolveLargura(de: .suspenso, para: .ativo))
        // Já liberado não devolve de novo.
        XCTAssertFalse(TerminalPaneState.devolveLargura(de: .liberado, para: .liberado))
    }

    /// A armadilha do conserto: o `.task(id:)` do TerminalMirrorView só reentra
    /// quando a chave muda, e a chave de hoje é só "colunas x linhas". Trocar de
    /// estado sem trocar de medida não reentrava — o estado tem de estar na chave.
    func testAChaveDeResizeMudaComOEstado() {
        let ativo = TerminalResizeKey.chave(cols: 80, rows: 24, estado: .ativo)
        let liberado = TerminalResizeKey.chave(cols: 80, rows: 24, estado: .liberado)
        XCTAssertNotEqual(ativo, liberado)
        XCTAssertEqual(ativo, TerminalResizeKey.chave(cols: 80, rows: 24, estado: .ativo))
        XCTAssertNotEqual(ativo, TerminalResizeKey.chave(cols: 100, rows: 24, estado: .ativo))
    }

    func testChaveSemMedidaAindaDistingueEstado() {
        XCTAssertNotEqual(TerminalResizeKey.chave(cols: nil, rows: nil, estado: .ativo),
                          TerminalResizeKey.chave(cols: nil, rows: nil, estado: .liberado))
    }
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/TerminalPaneStateTests -quiet`
Expected: FAIL na compilação — `cannot find 'TerminalPaneState' in scope`.

- [ ] **Step 3: Implementar `TerminalPaneState.swift`**

````swift
import Foundation

/// O estado de um painel de terminal. Três valores, não dois: o booleano `isActive`
/// que existia antes não distinguia trocar de aba de fechar o terminal, e a
/// diferença é a largura do pane no tmux do PC.
///
/// | estado      | polling | largura do tmux |
/// |-------------|---------|-----------------|
/// | `ativo`     | roda    | aplicada        |
/// | `suspenso`  | para    | **mantida**     |
/// | `liberado`  | para    | **devolvida**   |
///
/// `suspenso` é a aba de trás: o pane continua trabalhando na grade que já tem, e
/// voltar para a aba não precisa reenviar resize. `liberado` é o `✕`, a aba fechada,
/// a aba que dormiu por causa do teto de 6 (D3) e o app no background (D7).
enum TerminalPaneState: String, Codable, Equatable {
    case ativo
    case suspenso
    case liberado

    /// Só o painel visível espelha a tela — os de trás ficam montados e calados
    /// (decisão #19: nunca desmontar para trocar de painel).
    var fazPolling: Bool { self == .ativo }

    /// Devolver a largura é transição, não estado: só ao ENTRAR em `liberado`.
    /// Chamar duas vezes não faria mal ao tmux, mas faria um POST por medida de
    /// layout, e a medida muda em toda rotação.
    static func devolveLargura(de anterior: TerminalPaneState, para novo: TerminalPaneState) -> Bool {
        novo == .liberado && anterior != .liberado
    }
}

/// A chave do `.task(id:)` que dispara resize e polling no TerminalMirrorView.
/// O estado entra na chave DE PROPÓSITO: sem ele o `.task` não reentra quando só o
/// estado muda (era `"\(cols)x\(rows)"`), e era esse o motivo do `✕` do iPad não
/// devolver a largura.
enum TerminalResizeKey {
    static func chave(cols: Int?, rows: Int?, estado: TerminalPaneState) -> String {
        guard let cols, let rows else { return "sem-medida·\(estado.rawValue)" }
        return "\(cols)x\(rows)·\(estado.rawValue)"
    }
}
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/TerminalPaneStateTests -quiet`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/TerminalPaneState.swift app/CutuqueAppTests/TerminalPaneStateTests.swift
git commit -m "feat(app): estado de tres valores do painel de terminal"
```

### Task D2: o `✕` devolve a largura

**Files:**
- Modify: `app/CutuqueApp/TerminalMirrorView.swift:179` (a propriedade), `:236` (`resizeKey`), `:262-275` (`.task` e `.onChange`), `:291` (toolbar), `:305` (`onDisappear`)
- Modify: `app/CutuqueApp/SessionDetailPane.swift:86` (o call site do iPad)
- Test: `app/CutuqueAppTests/TerminalMirrorViewTests.swift` (atualiza `:22` e acrescenta um teste)

**Interfaces:**
- Consumes: `TerminalPaneState`, `TerminalResizeKey` (Task D1); `TerminalMirrorModel.stop()`, `.start()`, `.resize(cols:rows:)`, `.restoreSize()` (`:104-116`).
- Produces: `TerminalMirrorView.paneState: TerminalPaneState` **no lugar de** `isActive: Bool`. Todo call site novo (Tasks G6) passa `paneState:`.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// O `✕` do iPad não desmonta a view (ela fica montada para sempre) — só troca o
    /// modo do painel. Antes disso o `restoreSize()` vivia só no `onDisappear`, que
    /// nunca rodava ali: o pane ficava preso em `window-size manual` no PC.
    func testPainelFechadoNasceLiberado() {
        let view = TerminalMirrorView(machine: "mac", target: "sess:0.0", title: "t",
                                      paneState: .liberado)
        XCTAssertEqual(view.paneState, .liberado)
        XCTAssertFalse(view.paneState.fazPolling)
    }

    func testPainelDeTrasSuspendeSemDevolverLargura() {
        let view = TerminalMirrorView(machine: "mac", target: "sess:0.0", title: "t",
                                      paneState: .suspenso)
        XCTAssertFalse(view.paneState.fazPolling)
        XCTAssertFalse(TerminalPaneState.devolveLargura(de: .ativo, para: view.paneState))
    }
````

E trocar a linha `:22`, que hoje é
`TerminalMirrorView(machine: "mac", target: "sess:0.0", title: "t", isActive: false)`,
por `..., paneState: .liberado)`.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/TerminalMirrorViewTests -quiet`
Expected: FAIL na compilação — `extraneous argument label 'paneState:'`.

- [ ] **Step 3: Implementar em `TerminalMirrorView.swift`**

Trocar a propriedade (`:179`), preservando o comentário que estiver acima dela e
acrescentando a razão nova:

````swift
    /// O estado do painel. Três valores porque o `✕` do iPad e a troca de aba são
    /// coisas diferentes: um devolve a largura ao tmux, o outro a mantém. Era um
    /// `Bool isActive` — o booleano é o que deixava o pane preso na grade do iPad.
    var paneState: TerminalPaneState = .ativo
````

`resizeKey` (`:236`) passa a incluir o estado:

````swift
    private var resizeKey: String {
        TerminalResizeKey.chave(cols: grid?.cols, rows: grid?.rows, estado: paneState)
    }
````

O `.task(id: resizeKey)` (`:262`) e o `.onChange(of:)` (`:270`):

````swift
        .task(id: resizeKey) {
            // Painel liberado não fixa largura nenhuma: agenda o contrário, devolver.
            if paneState == .liberado {
                resizeDebouncer.cancel()
                model.stop()
                model.restoreSize()
                return
            }
            if let grid {
                resizeDebouncer.schedule(cols: grid.cols, rows: grid.rows) { c, r in
                    model.resize(cols: c, rows: r)
                }
            }
            if paneState.fazPolling { model.start() } else { model.stop() }
        }
        .onChange(of: paneState) { anterior, novo in
            // A largura é devolvida na TRANSIÇÃO para liberado, não no estado — ver
            // TerminalPaneState.devolveLargura. Suspender (aba de trás) mantém.
            if TerminalPaneState.devolveLargura(de: anterior, para: novo) {
                resizeDebouncer.cancel()
                model.restoreSize()
            }
            if novo.fazPolling { model.start() } else { model.stop() }
        }
````

O `.task` **e** o `.onChange` tratam o caso de propósito: o `.task` cobre a montagem
já liberada (aba restaurada dormindo) e o `.onChange` cobre a transição em vida.
Chamar `restoreSize()` duas vezes é inofensivo — é idempotente do lado do tmux.

Trocar as duas leituras restantes de `isActive`: a da toolbar (`:291`) passa a ser
`if paneState == .ativo`, e o `onDisappear` (`:305`) **fica como está** — é o caminho
correto no iPhone e a rede de segurança no iPad.

Em `SessionDetailPane.swift:86`, o call site:

````swift
                TerminalMirrorView(machine: terminal.machine, target: terminal.target,
                                   title: terminal.title,
                                   // `✕` (paneMode = .info) é fechar o terminal: devolve a
                                   // largura ao tmux. NÃO é kill — a sessão segue
                                   // trabalhando, é a instrução literal da Vanessa acima.
                                   paneState: showsTerminal ? .ativo : .liberado,
                                   ownsNavigationTitle: false)
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS na suíte inteira. Se algum outro arquivo passar `isActive:`, o
compilador aponta — troque por `paneState:`.

- [ ] **Step 5: Verificar no PC (é o teste que importa)**

Com uma sessão ao vivo aberta no iPad, apertar o `✕` e, no Mac, conferir:

```bash
tmux -L <grupo> show-window-options -t <sessão> window-size
```

Expected: `window-size latest`. Antes do conserto ficava `manual`.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/TerminalMirrorView.swift app/CutuqueApp/SessionDetailPane.swift \
        app/CutuqueAppTests/TerminalMirrorViewTests.swift
git commit -m "fix(app): fechar o terminal no ipad devolve a largura do pane ao tmux"
```

### Task D3: background devolve a largura (D7)

**Files:**
- Modify: `app/CutuqueApp/TerminalPaneState.swift` (acrescenta `TerminalCenaLogic`)
- Modify: `app/CutuqueApp/TerminalMirrorView.swift` (um `.onChange(of: scenePhase)`)
- Test: `app/CutuqueAppTests/TerminalPaneStateTests.swift` (acrescenta um teste)

**Interfaces:**
- Consumes: `TerminalPaneState` (Task D1).
- Produces: `enum AcaoDeLargura { case devolver, reaplicar, nada }` e `enum TerminalCenaLogic { static func acao(fase: ScenePhase, estado: TerminalPaneState) -> AcaoDeLargura }`.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// D7: sair do app devolve o pane ao normal no PC. `.inactive` NÃO conta — ele
    /// dispara em troca de app, Split View e central de notificações, e devolver
    /// largura ali faria o terminal remediar toda hora.
    func testCenaDeBackgroundDevolveLargura() {
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .background, estado: .ativo), .devolver)
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .background, estado: .suspenso), .devolver)
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .background, estado: .liberado), .nada)
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .active, estado: .ativo), .reaplicar)
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .active, estado: .suspenso), .nada)
        XCTAssertEqual(TerminalCenaLogic.acao(fase: .inactive, estado: .ativo), .nada)
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/TerminalPaneStateTests -quiet`
Expected: FAIL — `cannot find 'TerminalCenaLogic' in scope`.

- [ ] **Step 3: Implementar**

No fim de `TerminalPaneState.swift` (com `import SwiftUI` no topo, por causa do
`ScenePhase`):

````swift
/// O que fazer com a largura do pane quando a cena do app muda.
enum AcaoDeLargura: Equatable {
    case devolver
    case reaplicar
    case nada
}

/// D7: sair do app devolve o pane ao normal no PC — ninguém está olhando, e deixar
/// `window-size manual` fixado na grade do iPad estraga o terminal de quem está no
/// Mac. Voltar reaplica, mas só no painel que estava ativo.
///
/// `.inactive` é deliberadamente ignorado: ele dispara em troca de app, Split View e
/// central de notificações, e devolver largura ali faria o terminal remediar a cada
/// toque fora do app.
enum TerminalCenaLogic {
    static func acao(fase: ScenePhase, estado: TerminalPaneState) -> AcaoDeLargura {
        switch fase {
        case .background:
            return estado == .liberado ? .nada : .devolver
        case .active:
            return estado == .ativo ? .reaplicar : .nada
        default:
            return .nada
        }
    }
}
````

Em `TerminalMirrorView`, ao lado do `.onChange(of: paneState)`, com
`@Environment(\.scenePhase) private var scenePhase` entre as propriedades:

````swift
        .onChange(of: scenePhase) { _, fase in
            switch TerminalCenaLogic.acao(fase: fase, estado: paneState) {
            case .devolver:
                resizeDebouncer.cancel()
                model.restoreSize()
            case .reaplicar:
                if let grid { model.resize(cols: grid.cols, rows: grid.rows) }
            case .nada:
                break
            }
        }
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 5: Verificar no aparelho**

Com o terminal ao vivo aberto no iPad, ir para a tela inicial e conferir no Mac que
`window-size` voltou para `latest`; reabrir o app e conferir que volta a `manual` com
a grade do iPad.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/TerminalPaneState.swift app/CutuqueApp/TerminalMirrorView.swift \
        app/CutuqueAppTests/TerminalPaneStateTests.swift
git commit -m "fix(app): app em background devolve a largura do pane ao tmux"
```

## Chain E — lista agrupada por grupo (D9)

⚠️ **Não comece antes do merge de `identidade-pane-ao-vivo`.** Sem a separação de
`LiveEntry.paneTarget` e `LiveEntry.id`, duas máquinas com um grupo de mesmo nome
produzem `Identifiable` idênticos no `ForEach`, e é justamente o caso que a D9 torna
comum. Ver "Pré-requisito de integração".

O que a D9 pede: grupo `defender` reúne `mike` (macbook) e `mikeaux` (windows) numa
seção só, cada linha com ícone da máquina e a pasta. Grupo é **projeto/contexto**;
máquina é detalhe de execução.

**Achado que encurta a chain:** `liveRowLabel` (`SessionListView.swift:799-812`) **já**
mostra o ícone da máquina e a pasta, e `machineSymbol(_:)` já existe
(`SessionNames.swift:42-51`). Não há nada a construir para os ícones — a chain E é
reagrupar e resolver a ação destrutiva.

**O custo real:** o cabeçalho da seção tem uma ação destrutiva amarrada a
`(group.machine, group.socket)` (`:414-420`), e o comentário de `:372-375` já avisava
que agrupar só por socket teria _"um 'encerrar server' que mataria a errada"_. Com
máquinas misturadas, `group.machine` deixa de ser único: a ação passa a ser **uma
entrada por máquina presente no grupo**.

### Task E1: agrupamento por grupo (puro), em arquivo próprio

**Files:**
- Create: `app/CutuqueApp/LivePaneLogic.swift` (recebe o que hoje mora em `SessionListView.swift:24-50`, mais o agrupamento novo)
- Create: `app/CutuqueAppTests/LivePaneAgrupamentoTests.swift`
- Modify: `app/CutuqueApp/SessionListView.swift:24-50` (remove o `enum LivePaneLogic`, que muda de arquivo) e `:394-401` (as duas funções de socket)

**Interfaces:**
- Consumes: `LiveEntry` (`SessionListView.swift:7`), `DiscoveredSession`.
- Produces:
  - `struct ServidorDoGrupo: Identifiable, Equatable { let machine: String; let socket: String; var id: String }`
  - `struct GrupoAoVivo: Identifiable, Equatable { let grupo: String; let servers: [ServidorDoGrupo]; let entries: [LiveEntry]; var id: String { grupo } }`
  - `LivePaneLogic.agrupadoPorGrupo(_ entries: [LiveEntry]) -> [GrupoAoVivo]`
  - `LivePaneLogic.socket(of paneTarget: String) -> String` e `LivePaneLogic.nomeDoGrupo(_ socket: String) -> String` (mudam de casa: eram `SessionListView.socket(of:)` e `.serverName(_:)`)
  - `LivePaneLogic.removendoServer(_:machine:socket:)` continua igual, só muda de arquivo.
  - Consumido pela Task E2.

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

/// D9: um grupo é uma seção só, com as máquinas misturadas dentro. O caso que
/// motivou tudo: grupo "defender" com a sessão "mike" no macbook e "mikeaux" no
/// windows — antes eram duas seções ("defender · macbook", "defender · windows").
final class LivePaneAgrupamentoTests: XCTestCase {

    private func entrada(machine: String, socket: String, pane: String,
                         session: String) -> LiveEntry {
        LiveEntry(machine: machine,
                  session: DiscoveredSession(id: socket + "\t" + pane,
                                             cwd: "/Users/vanessa/dev/" + session,
                                             title: session))
    }

    func testGrupoDeMesmoNomeEmDuasMaquinasCaiNumaSecaoSo() {
        let grupos = LivePaneLogic.agrupadoPorGrupo([
            entrada(machine: "macbook", socket: "/tmp/tmux-501/defender", pane: "%1", session: "mike"),
            entrada(machine: "windows", socket: "/tmp/tmux-501/defender", pane: "%1", session: "mikeaux"),
        ])
        XCTAssertEqual(grupos.count, 1)
        XCTAssertEqual(grupos[0].grupo, "defender")
        XCTAssertEqual(grupos[0].entries.count, 2)
    }

    /// A parte que não é estética: "Encerrar server" é destrutivo e, num grupo com
    /// duas máquinas, uma entrada só seria ambígua — mataria a errada.
    func testUmaEntradaDeEncerrarServerPorMaquina() {
        let grupos = LivePaneLogic.agrupadoPorGrupo([
            entrada(machine: "macbook", socket: "/tmp/tmux-501/defender", pane: "%1", session: "mike"),
            entrada(machine: "macbook", socket: "/tmp/tmux-501/defender", pane: "%2", session: "mike2"),
            entrada(machine: "windows", socket: "/tmp/tmux-501/defender", pane: "%1", session: "mikeaux"),
        ])
        XCTAssertEqual(grupos[0].servers.map(\.machine), ["macbook", "windows"])
        XCTAssertEqual(grupos[0].servers.count, 2, "uma entrada por MÁQUINA, não por pane")
    }

    func testGruposDiferentesSeparamEOrdemEhEstavel() {
        let grupos = LivePaneLogic.agrupadoPorGrupo([
            entrada(machine: "macbook", socket: "/tmp/tmux-501/zima", pane: "%1", session: "a"),
            entrada(machine: "macbook", socket: "/tmp/tmux-501/atlas", pane: "%1", session: "b"),
        ])
        XCTAssertEqual(grupos.map(\.grupo), ["atlas", "zima"])
    }

    func testSocketEGrupoSaemDoAlvoComposto() {
        XCTAssertEqual(LivePaneLogic.socket(of: "/tmp/tmux-501/defender\t%3"), "/tmp/tmux-501/defender")
        XCTAssertEqual(LivePaneLogic.nomeDoGrupo("/tmp/tmux-501/defender"), "defender")
        // Alvo sem socket (servidor default) não pode derrubar o agrupamento.
        XCTAssertEqual(LivePaneLogic.socket(of: "%3"), "")
    }
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/LivePaneAgrupamentoTests -quiet`
Expected: FAIL — `type 'LivePaneLogic' has no member 'agrupadoPorGrupo'`.

- [ ] **Step 3: Criar `LivePaneLogic.swift`**

Mover o `enum LivePaneLogic` de `SessionListView.swift:24-50` **como está** (com os
comentários, que documentam bugs de verdade), mover para dentro dele as duas funções
de `:394-401` renomeando `serverName` para `nomeDoGrupo`, e acrescentar:

````swift
/// Um servidor tmux concreto dentro de um grupo: máquina + socket. Existe porque
/// "Encerrar server" é destrutivo e precisa dos dois — o mesmo caminho de socket
/// existe em duas máquinas de mesmo uid.
struct ServidorDoGrupo: Identifiable, Equatable {
    let machine: String
    let socket: String
    var id: String { machine + "\t" + socket }
}

/// Um grupo do tmux na lista ao vivo: uma seção, com as máquinas misturadas dentro
/// (D9). Grupo é projeto/contexto — o mesmo identificador que o board usa como
/// escopo (CUTUQUE_GROUP = socket); máquina é detalhe de execução, e vive no ícone
/// de cada linha.
struct GrupoAoVivo: Identifiable, Equatable {
    let grupo: String
    let servers: [ServidorDoGrupo]
    let entries: [LiveEntry]
    var id: String { grupo }
}

extension LivePaneLogic {
    /// Agrupa por NOME do grupo, não por máquina + socket. Antes o agrupamento
    /// incluía a máquina para o "Encerrar server" do cabeçalho não ficar ambíguo;
    /// agora a ambiguidade é resolvida na ação (uma entrada por máquina) em vez de
    /// na seção, que é o que a D9 pede.
    ///
    /// Ordem: grupos por nome e, dentro do grupo, por máquina e depois pelo alvo —
    /// determinística, para a lista não dançar entre polls.
    static func agrupadoPorGrupo(_ entries: [LiveEntry]) -> [GrupoAoVivo] {
        let porGrupo = Dictionary(grouping: entries) { nomeDoGrupo(socket(of: $0.paneTarget)) }
        return porGrupo.keys.sorted().map { grupo in
            let doGrupo = porGrupo[grupo] ?? []
            let servidores = Set(doGrupo.map { ServidorDoGrupo(machine: $0.machine, socket: socket(of: $0.paneTarget)) })
            return GrupoAoVivo(
                grupo: grupo,
                servers: servidores.sorted { $0.id < $1.id },
                entries: doGrupo.sorted { ($0.machine, $0.paneTarget) < ($1.machine, $1.paneTarget) }
            )
        }
    }
}
````

`ServidorDoGrupo` precisa de `Hashable` para o `Set` — acrescente à declaração
(`Identifiable, Equatable, Hashable`).

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS. `LivePaneIdentityTests` continua passando (o `LivePaneLogic` só mudou
de arquivo). Onde o `SessionListView` chamava `Self.socket(of:)` / `Self.serverName`,
trocar por `LivePaneLogic.socket(of:)` / `LivePaneLogic.nomeDoGrupo`.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/LivePaneLogic.swift app/CutuqueApp/SessionListView.swift \
        app/CutuqueAppTests/LivePaneAgrupamentoTests.swift
git commit -m "feat(app): agrupamento das sessoes ao vivo por grupo do tmux"
```

### Task E2: a lista usa o agrupamento e o kill vira por máquina

**Files:**
- Modify: `app/CutuqueApp/SessionListView.swift:376-392` (`liveByServer` → `gruposAoVivo`), `:403-425` (`liveServerSections`)
- Test: `app/CutuqueAppTests/LivePaneAgrupamentoTests.swift` (acrescenta um teste de rótulo)

**Interfaces:**
- Consumes: `LivePaneLogic.agrupadoPorGrupo`, `GrupoAoVivo`, `ServidorDoGrupo` (E1); `ServerKill` (`:53-60`), `model.killServer(machine:socket:)` (`:230`).
- Produces: `SessionListView.gruposAoVivo: [GrupoAoVivo]` e `LivePaneLogic.rotuloDeEncerrar(_ servidor: ServidorDoGrupo) -> String`.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// O menu do cabeçalho passa a ter uma entrada por máquina, e o rótulo tem de
    /// dizer QUAL — é ação destrutiva, e ambiguidade aqui mata o server errado.
    func testRotuloDeEncerrarNomeiaAMaquina() {
        let s = ServidorDoGrupo(machine: "macbook", socket: "/tmp/tmux-501/defender")
        XCTAssertEqual(LivePaneLogic.rotuloDeEncerrar(s), "Encerrar server no macbook")
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/LivePaneAgrupamentoTests -quiet`
Expected: FAIL — `has no member 'rotuloDeEncerrar'`.

- [ ] **Step 3: Implementar**

Em `LivePaneLogic.swift`:

````swift
    /// Rótulo da ação destrutiva. A máquina entra SEMPRE, mesmo com uma só no grupo:
    /// aqui não vale a economia de ruído do rótulo de seção antigo, porque o custo do
    /// erro é encerrar o server da máquina errada.
    static func rotuloDeEncerrar(_ servidor: ServidorDoGrupo) -> String {
        "Encerrar server no \(servidor.machine)"
    }
````

Em `SessionListView.swift`, substituir `liveByServer` (`:376-392`) inteiro por:

````swift
    /// As sessões ao vivo agrupadas por grupo do tmux (D9). O agrupamento e a ordem
    /// moram em LivePaneLogic — aqui fica só a leitura do estado da view.
    private var gruposAoVivo: [GrupoAoVivo] {
        LivePaneLogic.agrupadoPorGrupo(liveNotTracked)
    }
````

E `liveServerSections` (`:403-425`) passa a iterar os grupos, com o menu de encerrar
por servidor:

````swift
    @ViewBuilder private var liveServerSections: some View {
        ForEach(gruposAoVivo) { grupo in
            Section {
                ForEach(grupo.entries) { liveRow($0) }
            } header: {
                HStack {
                    Label("Ao vivo · \(grupo.grupo)", systemImage: "bolt.horizontal.circle")
                    Spacer()
                    Menu {
                        // Uma entrada por MÁQUINA presente no grupo: com máquinas
                        // misturadas numa seção só, uma ação única seria ambígua —
                        // e ambiguidade em ação destrutiva é inaceitável.
                        ForEach(grupo.servers) { servidor in
                            Button(role: .destructive) {
                                serverToKill = ServerKill(machine: servidor.machine,
                                                          socket: servidor.socket,
                                                          name: grupo.grupo)
                            } label: {
                                Label(LivePaneLogic.rotuloDeEncerrar(servidor),
                                      systemImage: "xmark.octagon")
                            }
                        }
                    } label: {
                        Image(systemName: "ellipsis.circle")
                    }
                }
            }
        }
    }
````

Preserve o resto do cabeçalho como está hoje (fonte, `textCase`, `foregroundStyle`
etc.) — a mudança é o `ForEach` interno, não o estilo.

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS. Os testes de `serversAmbiguos`/`rotulo` que existirem continuam
passando — as funções ainda estão lá, e é a Task E3 que as aposenta.

- [ ] **Step 5: Verificar na tela**

Com duas máquinas rodando um grupo de mesmo nome: uma seção só, cada linha com o
ícone da sua máquina, e o menu `⋯` com duas entradas nomeando as máquinas. Confirmar
que a confirmação (`:547-558`) mostra a máquina certa.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/SessionListView.swift app/CutuqueApp/LivePaneLogic.swift \
        app/CutuqueAppTests/LivePaneAgrupamentoTests.swift
git commit -m "feat(app): lista ao vivo por grupo com encerrar server por maquina"
```

### Task E3: aposentar `serversAmbiguos()` e `rotulo()`

**Files:**
- Modify: `app/CutuqueApp/LivePaneLogic.swift` (remove as duas funções)
- Modify: `app/CutuqueAppTests/LivePaneIdentityTests.swift` (remove os testes das duas)

**Interfaces:**
- Consumes: nada.
- Produces: nada. É remoção — e só é segura **depois** da E2, que retirou o último uso.

- [ ] **Step 1: Confirmar que ninguém mais usa**

Run: `cd /Users/vanessa/Desktop/coding/personal/cutuque && grep -rn "serversAmbiguos\|rotulo(server" app/`
Expected: só as definições e os testes delas. Se aparecer outro uso, **pare** — a
Task E2 não terminou.

- [ ] **Step 2: Remover**

Apagar `serversAmbiguos(_:)` e `rotulo(server:machine:ambiguo:)` de
`LivePaneLogic.swift`, e os testes correspondentes de `LivePaneIdentityTests.swift`.

**Não apagar** o comentário de `removendoServer` nem o de `LiveEntry.id`: eles
documentam o bug de identidade que a branch `identidade-pane-ao-vivo` consertou e
continuam valendo. Em `agrupadoPorGrupo`, deixe registrado por que o desempate por
máquina saiu — o comentário que a Task E1 já pede cobre isso.

- [ ] **Step 3: Rodar a suíte**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS, com dois testes a menos.

- [ ] **Step 4: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/LivePaneLogic.swift app/CutuqueAppTests/LivePaneIdentityTests.swift
git commit -m "refactor(app): aposenta o desempate de server ambiguo"
```

## Chain F — formulário de novo terminal (F4 da spec)

Cinco campos, cinco fontes que já existem: máquina (`targets()`, `APIClient.swift:83`),
grupo (os grupos que a lista ao vivo já conhece, com nome novo permitido — D13),
sessão (texto), pasta (`listDirs(machine:path:)`, `:412`), agente (os quatro do
`tmx.sh`).

### Task F1: validador de nome + cliente da rota

**Files:**
- Create: `app/CutuqueApp/NomeTmux.swift`
- Create: `app/CutuqueAppTests/NomeTmuxTests.swift`
- Modify: `app/CutuqueApp/APIClient.swift` (o método novo, ao lado de `tmuxKillServer`, `:969`)

**Interfaces:**
- Consumes: a rota `POST /machines/{machine}/tmux/new` (Task C4) — pode ser escrita em paralelo, o contrato está fechado.
- Produces:
  - `NomeTmux.valido(_ nome: String) -> Bool`
  - `NomeTmux.filtrando(_ texto: String) -> String`
  - `NomeTmux.aviso` (a explicação para a UI)
  - `CutuqueAPI.tmuxNewSession(machine:group:session:cwd:agent:) async throws -> String` (devolve o alvo composto do pane)
  - Consumido pelas Tasks F2 e F3.

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

/// D12: nome de grupo e de sessão é barrado NO TECLADO, não num erro criptográfico
/// depois. O tmux usa ":" e "." como separador de alvo (sessão:janela.pane) e o grupo
/// viaja num caminho de socket — nome fora do alfabeto produziria alvo ambíguo.
final class NomeTmuxTests: XCTestCase {

    func testAceitaLetrasNumerosHifenEUnderscore() {
        XCTAssertTrue(NomeTmux.valido("defender"))
        XCTAssertTrue(NomeTmux.valido("mike-aux_2"))
        XCTAssertTrue(NomeTmux.valido("A1"))
    }

    func testRecusaVazioESeparadoresDoTmux() {
        XCTAssertFalse(NomeTmux.valido(""))
        XCTAssertFalse(NomeTmux.valido("mike.aux"))
        XCTAssertFalse(NomeTmux.valido("defender:1"))
        XCTAssertFalse(NomeTmux.valido("mike aux"))
        XCTAssertFalse(NomeTmux.valido("../etc"))
        XCTAssertFalse(NomeTmux.valido("açaí"))
    }

    /// O campo filtra enquanto digita — é isso que "barrado no teclado" quer dizer.
    func testFiltrandoRemoveOInvalidoEPreservaOResto() {
        XCTAssertEqual(NomeTmux.filtrando("mike.aux"), "mikeaux")
        XCTAssertEqual(NomeTmux.filtrando("de fen:der"), "defender")
        XCTAssertEqual(NomeTmux.filtrando("mike-aux_2"), "mike-aux_2")
        XCTAssertEqual(NomeTmux.filtrando(""), "")
    }
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/NomeTmuxTests -quiet`
Expected: FAIL — `cannot find 'NomeTmux' in scope`.

- [ ] **Step 3: Implementar `NomeTmux.swift`**

````swift
import Foundation

/// Nome de grupo e de sessão do tmux (D12). O alfabeto é `A-Za-z0-9-_`, e a razão
/// não é estética: o tmux usa ":" e "." como separador de alvo (sessão:janela.pane) e
/// o grupo vira caminho de socket, validado no hub por `^/[A-Za-z0-9._/ -]+$`. Nome
/// fora disso não daria erro bonito — daria alvo ambíguo.
///
/// O hub valida de novo (defesa em profundidade); esta é a camada de UX, que barra
/// enquanto a Vanessa digita.
enum NomeTmux {
    /// O que a UI explica embaixo do campo.
    static let aviso = "Letras, números, hífen e sublinhado"

    private static let permitidos: Set<Character> = {
        var s = Set<Character>("-_")
        for c in "abcdefghijklmnopqrstuvwxyz" { s.insert(c) }
        for c in "ABCDEFGHIJKLMNOPQRSTUVWXYZ" { s.insert(c) }
        for c in "0123456789" { s.insert(c) }
        return s
    }()

    static func valido(_ nome: String) -> Bool {
        !nome.isEmpty && nome.allSatisfy { permitidos.contains($0) }
    }

    /// Filtra em vez de rejeitar: colar "mike.aux" no campo dá "mikeaux", e não um
    /// campo que se recusa a mudar sem dizer por quê.
    static func filtrando(_ texto: String) -> String {
        String(texto.filter { permitidos.contains($0) })
    }
}
````

- [ ] **Step 4: Acrescentar o método no `APIClient.swift`**

Ao lado de `tmuxKillServer` (depois de `:969`):

````swift
    /// Corpo e resposta de `POST /machines/{machine}/tmux/new`.
    private struct TmuxNewBody: Encodable {
        let group: String
        let session: String
        let cwd: String
        let agent: String
    }
    private struct TmuxNewResponse: Decodable {
        let target: String
    }

    /// Cria (ou anexa, pelo `-A` do tmux) uma sessão na máquina e devolve o alvo do
    /// pane ("<socket>\t<pane>"), pronto para abrir o espelho. `group` é o servidor
    /// tmux, que é também o escopo do board — grupo novo não tem cerimônia (D13).
    /// `agent` é um de claude|codex|opencode|terminal.
    /// Sessão que já existe NÃO é erro: o hub anexa e devolve o alvo dela.
    func tmuxNewSession(machine: String, group: String, session: String,
                        cwd: String, agent: String) async throws -> String {
        let url = baseURL
            .appendingPathComponent("machines").appendingPathComponent(machine)
            .appendingPathComponent("tmux").appendingPathComponent("new")
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        request.httpBody = try JSONEncoder().encode(
            TmuxNewBody(group: group, session: session, cwd: cwd, agent: agent))

        let (data, response) = try await URLSession.shared.data(for: request)
        guard let http = response as? HTTPURLResponse else { throw URLError(.badServerResponse) }
        switch http.statusCode {
        case 200:
            return try JSONDecoder.cutuque.decode(TmuxNewResponse.self, from: data).target
        case 404:
            throw CutuqueError.notFound
        default:
            throw CutuqueError.unexpected(status: http.statusCode)
        }
    }
````

- [ ] **Step 5: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/NomeTmux.swift app/CutuqueApp/APIClient.swift \
        app/CutuqueAppTests/NomeTmuxTests.swift
git commit -m "feat(app): validacao de nome do tmux e cliente da rota de criar sessao"
```

### Task F2: o formulário

**Files:**
- Create: `app/CutuqueApp/NovoTerminalForm.swift` (a view **e** o `NovoTerminalFormLogic`)
- Create: `app/CutuqueAppTests/NovoTerminalFormTests.swift`

**Interfaces:**
- Consumes: `NomeTmux` e `CutuqueAPI.tmuxNewSession` (F1); `api.targets()` (`APIClient.swift:83`), `api.listDirs(machine:path:)` (`:412`), `machineSymbol(_:)` (`SessionNames.swift:42`).
- Produces:
  - `enum AgenteNovoTerminal: String, CaseIterable { case claude, codex, opencode, terminal }` com `var rotulo: String` e `var simbolo: String`
  - `struct NovoTerminalCampos: Equatable { var machine, grupo, sessao, pasta: String; var agente: AgenteNovoTerminal }`
  - `enum NovoTerminalFormLogic { static func podeCriar(_ campos: NovoTerminalCampos) -> Bool; static func gruposConhecidos(_ grupos: [GrupoAoVivo]) -> [String] }`
  - `struct NovoTerminalForm: View` com `let aoCriar: (String, String) -> Void` (máquina, alvo)
  - Consumido pela Task F3.

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

final class NovoTerminalFormTests: XCTestCase {

    private var completos: NovoTerminalCampos {
        NovoTerminalCampos(machine: "macbook", grupo: "defender", sessao: "mike",
                           pasta: "/Users/vanessa/dev/defender", agente: .claude)
    }

    func testCriarSoComTodosOsCamposValidos() {
        XCTAssertTrue(NovoTerminalFormLogic.podeCriar(completos))

        var semMaquina = completos; semMaquina.machine = ""
        XCTAssertFalse(NovoTerminalFormLogic.podeCriar(semMaquina))

        var grupoInvalido = completos; grupoInvalido.grupo = "defender:1"
        XCTAssertFalse(NovoTerminalFormLogic.podeCriar(grupoInvalido))

        var sessaoVazia = completos; sessaoVazia.sessao = ""
        XCTAssertFalse(NovoTerminalFormLogic.podeCriar(sessaoVazia))

        // Pasta tem de ser absoluta: é o que o hub aceita, e escolher pela lista de
        // dirs sempre dá absoluta — o caso relativo é digitação à mão.
        var pastaRelativa = completos; pastaRelativa.pasta = "dev/defender"
        XCTAssertFalse(NovoTerminalFormLogic.podeCriar(pastaRelativa))
    }

    /// D13: grupo novo não tem cerimônia. Os grupos conhecidos são só uma sugestão —
    /// digitar um nome que não existe É criar o grupo.
    func testGruposConhecidosSaoSugestaoOrdenadaESemRepeticao() {
        let grupos = [
            GrupoAoVivo(grupo: "zima", servers: [], entries: []),
            GrupoAoVivo(grupo: "atlas", servers: [], entries: []),
        ]
        XCTAssertEqual(NovoTerminalFormLogic.gruposConhecidos(grupos), ["atlas", "zima"])
        XCTAssertTrue(NovoTerminalFormLogic.podeCriar(
            NovoTerminalCampos(machine: "macbook", grupo: "grupo-que-nao-existe",
                               sessao: "s", pasta: "/tmp", agente: .terminal)))
    }

    /// D8 e D10: o terminal livre nasce sem agente; o codex é o `tmx cx`, com a flag.
    /// O valor bruto é o que vai no corpo do POST — o hub o usa como chave.
    func testValoresBrutosDosAgentes() {
        XCTAssertEqual(AgenteNovoTerminal.allCases.map(\.rawValue),
                       ["claude", "codex", "opencode", "terminal"])
    }
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/NovoTerminalFormTests -quiet`
Expected: FAIL — `cannot find 'NovoTerminalCampos' in scope`.

- [ ] **Step 3: Implementar a lógica e a view**

````swift
import SwiftUI

/// Os quatro do `scripts/tmx.sh`. "terminal" é o shell puro — o terminal livre nasce
/// sem agente nenhum (D8). O `rawValue` é o que viaja no POST: o hub usa essa string
/// como chave da tabela de comandos.
enum AgenteNovoTerminal: String, CaseIterable, Identifiable {
    case claude, codex, opencode, terminal

    var id: String { rawValue }

    var rotulo: String {
        switch self {
        case .claude: return "Claude"
        case .codex: return "Codex"
        case .opencode: return "OpenCode"
        case .terminal: return "Terminal vazio"
        }
    }

    var simbolo: String {
        switch self {
        case .claude: return "sparkles"
        case .codex: return "chevron.left.forwardslash.chevron.right"
        case .opencode: return "cube"
        case .terminal: return "terminal"
        }
    }
}

struct NovoTerminalCampos: Equatable {
    var machine: String = ""
    var grupo: String = ""
    var sessao: String = ""
    var pasta: String = ""
    var agente: AgenteNovoTerminal = .claude
}

/// Lógica pura do formulário, fora da View para dar teste sem simulador.
enum NovoTerminalFormLogic {
    static func podeCriar(_ campos: NovoTerminalCampos) -> Bool {
        !campos.machine.isEmpty
            && NomeTmux.valido(campos.grupo)
            && NomeTmux.valido(campos.sessao)
            && campos.pasta.hasPrefix("/")
    }

    /// Sugestões de grupo, tiradas do que já está ao vivo. Sugestão, não menu
    /// fechado: digitar um nome que não existe é criar o grupo (D13) — e criar grupo
    /// novo cria escopo novo no board, que é consequência declarada.
    static func gruposConhecidos(_ grupos: [GrupoAoVivo]) -> [String] {
        Array(Set(grupos.map(\.grupo))).sorted()
    }
}

/// Formulário de novo terminal tmux. Cinco campos, cinco fontes que já existiam.
struct NovoTerminalForm: View {
    let maquinas: [String]
    let gruposSugeridos: [String]
    /// Chamado com (máquina, alvo do pane) quando a criação dá certo.
    let aoCriar: (String, String) -> Void

    @Environment(\.dismiss) private var dismiss
    @EnvironmentObject private var api: CutuqueAPI

    @State private var campos = NovoTerminalCampos()
    @State private var criando = false
    @State private var erro: String?

    var body: some View {
        NavigationStack {
            Form {
                Section("Máquina") {
                    Picker("Máquina", selection: $campos.machine) {
                        ForEach(maquinas, id: \.self) { m in
                            Label(m, systemImage: machineSymbol(m)).tag(m)
                        }
                    }
                }

                Section {
                    TextField("Grupo", text: $campos.grupo)
                        .autocorrectionDisabled()
                        .textInputAutocapitalization(.never)
                        .onChange(of: campos.grupo) { _, novo in
                            // D12: filtra enquanto digita, em vez de reclamar depois.
                            let limpo = NomeTmux.filtrando(novo)
                            if limpo != novo { campos.grupo = limpo }
                        }
                    if !gruposSugeridos.isEmpty {
                        ScrollView(.horizontal, showsIndicators: false) {
                            HStack {
                                ForEach(gruposSugeridos, id: \.self) { g in
                                    Button(g) { campos.grupo = g }
                                        .buttonStyle(.bordered)
                                }
                            }
                        }
                    }
                } header: {
                    Text("Grupo")
                } footer: {
                    Text("\(NomeTmux.aviso). Nome novo cria o grupo.")
                }

                Section {
                    TextField("Sessão", text: $campos.sessao)
                        .autocorrectionDisabled()
                        .textInputAutocapitalization(.never)
                        .onChange(of: campos.sessao) { _, novo in
                            let limpo = NomeTmux.filtrando(novo)
                            if limpo != novo { campos.sessao = limpo }
                        }
                } header: {
                    Text("Sessão")
                } footer: {
                    Text("Sessão que já existe é reaproveitada, não duplicada.")
                }

                Section("Pasta") {
                    NavigationLink {
                        SeletorDePasta(machine: campos.machine, escolhida: $campos.pasta)
                    } label: {
                        HStack {
                            Text("Pasta")
                            Spacer()
                            Text(campos.pasta.isEmpty ? "escolher" : campos.pasta)
                                .foregroundStyle(.secondary)
                                .lineLimit(1)
                                .truncationMode(.head)
                        }
                    }
                    .disabled(campos.machine.isEmpty)
                }

                Section("Agente") {
                    Picker("Agente", selection: $campos.agente) {
                        ForEach(AgenteNovoTerminal.allCases) { a in
                            Label(a.rotulo, systemImage: a.simbolo).tag(a)
                        }
                    }
                    .pickerStyle(.inline)
                }

                if let erro {
                    Section { Text(erro).foregroundStyle(.red) }
                }
            }
            .navigationTitle("Novo terminal")
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancelar") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Criar") { criar() }
                        .disabled(!NovoTerminalFormLogic.podeCriar(campos) || criando)
                }
            }
        }
        .onAppear {
            if campos.machine.isEmpty, let primeira = maquinas.first { campos.machine = primeira }
        }
    }

    private func criar() {
        criando = true
        erro = nil
        Task {
            do {
                let alvo = try await api.tmuxNewSession(machine: campos.machine,
                                                        group: campos.grupo,
                                                        session: campos.sessao,
                                                        cwd: campos.pasta,
                                                        agent: campos.agente.rawValue)
                aoCriar(campos.machine, alvo)
                dismiss()
            } catch {
                // Erro nomeia a máquina: "não deu" sem dizer onde é inútil quando
                // são três máquinas.
                erro = "não deu para criar em \(campos.machine): \(error.localizedDescription)"
            }
            criando = false
        }
    }
}
````

- [ ] **Step 4: Implementar o `SeletorDePasta` no mesmo arquivo**

````swift
/// Navegação de pastas pela mesma fonte da aba Arquivos (`GET /machines/{m}/dirs`).
/// Só pastas: o alvo é o `-c` do `new-session`.
private struct SeletorDePasta: View {
    let machine: String
    @Binding var escolhida: String

    @EnvironmentObject private var api: CutuqueAPI
    @Environment(\.dismiss) private var dismiss
    @State private var atual = ""
    @State private var dirs: [String] = []
    @State private var carregando = false

    var body: some View {
        List {
            if !atual.isEmpty {
                Button {
                    atual = (atual as NSString).deletingLastPathComponent
                    Task { await carregar() }
                } label: {
                    Label("..", systemImage: "arrow.up.left")
                }
            }
            ForEach(dirs, id: \.self) { d in
                Button {
                    atual = d
                    Task { await carregar() }
                } label: {
                    Label((d as NSString).lastPathComponent, systemImage: "folder")
                }
            }
        }
        .overlay { if carregando { ProgressView() } }
        .navigationTitle(atual.isEmpty ? "Home" : (atual as NSString).lastPathComponent)
        .toolbar {
            ToolbarItem(placement: .confirmationAction) {
                Button("Usar esta") {
                    escolhida = atual
                    dismiss()
                }
                .disabled(atual.isEmpty)
            }
        }
        .task { await carregar() }
    }

    private func carregar() async {
        carregando = true
        // O listing do hub já devolve caminhos; se a chamada falhar, a lista fica
        // vazia e a Vanessa volta — sem alerta, porque navegar não é destrutivo.
        if let listing = try? await api.listDirs(machine: machine, path: atual) {
            atual = listing.path
            dirs = listing.dirs.map { listing.path + "/" + $0 }.sorted()
        }
        carregando = false
    }
}
````

**Confirme os nomes de campo de `DirListing`** (`Models.swift`) antes de rodar: se ele
expõe as subpastas com outro nome (`entries`, `names`) ou já em caminho absoluto,
ajuste as duas linhas de `carregar()`. É o único ponto do plano que depende de um tipo
que não foi lido linha a linha.

- [ ] **Step 5: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/NovoTerminalForm.swift app/CutuqueAppTests/NovoTerminalFormTests.swift
git commit -m "feat(app): formulario de novo terminal tmux"
```

### Task F3: o botão `+` na lista e o shell marcado (D11)

**Files:**
- Modify: `app/CutuqueApp/SessionListView.swift` (toolbar da lista + apresentação do formulário + marca de shell na linha)
- Modify: `app/CutuqueApp/Models.swift:192-236` (`DiscoveredSession.kind`)
- Test: `app/CutuqueAppTests/NovoTerminalFormTests.swift` (acrescenta um teste de decodificação)

**Interfaces:**
- Consumes: `NovoTerminalForm`, `NovoTerminalFormLogic.gruposConhecidos` (F2); `gruposAoVivo` (E2); `Discovered.Kind` do hub (B1).
- Produces: `DiscoveredSession.kind: String` (decodificação tolerante) e `DiscoveredSession.ehShell: Bool`.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// O `kind` vem com `omitempty` do hub: um hub antigo não manda o campo, e isso
    /// NÃO pode virar erro de decodificação — é o padrão que `state` e `agent` já
    /// seguem em DiscoveredSession.
    func testKindAusenteNaoQuebraADecodificacao() throws {
        let json = Data(#"{"id":"s\t%1","cwd":"/x","title":"t"}"#.utf8)
        let s = try JSONDecoder.cutuque.decode(DiscoveredSession.self, from: json)
        XCTAssertEqual(s.kind, "")
        XCTAssertFalse(s.ehShell)
    }

    func testShellVemMarcado() throws {
        let json = Data(#"{"id":"s\t%1","cwd":"/x","title":"t","kind":"shell"}"#.utf8)
        let s = try JSONDecoder.cutuque.decode(DiscoveredSession.self, from: json)
        XCTAssertTrue(s.ehShell)
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/NovoTerminalFormTests -quiet`
Expected: FAIL — `value of type 'DiscoveredSession' has no member 'kind'`.

- [ ] **Step 3: Implementar em `Models.swift`**

Seguindo exatamente o padrão que já está lá (`:192-236`): acrescentar `case kind` ao
`CodingKeys`, a propriedade, a linha tolerante no `init(from:)` e o parâmetro com
default no `init` de conveniência.

````swift
    /// "shell" = pane sem agente (D11). Vazio = pane de agente ou sessão que não vem
    /// do tmux. Campo novo → hub antigo não manda → default seguro.
    let kind: String

    /// Terminal livre: aparece na lista marcado, senão o que o formulário criou
    /// desaparece no refresh seguinte.
    var ehShell: Bool { kind == "shell" }
````

No `init(from:)`: `kind = (try? c.decode(String.self, forKey: .kind)) ?? ""`
No `init(...)`: `kind: String = ""` (mantendo a ordem dos parâmetros existentes).

- [ ] **Step 4: Marcar o shell na linha e pôr o botão `+`**

Em `liveRowLabel` (`SessionListView.swift:799-812`), depois do `Text(entry.session.folderName)`
que já existe no `HStack` de `:810-811`:

````swift
                    if entry.session.ehShell {
                        // D11: sem esta marca, o terminal livre criado pelo app fica
                        // indistinguível de uma sessão de agente sem estado.
                        Label("shell", systemImage: "terminal")
                            .labelStyle(.titleAndIcon)
                    }
````

E na toolbar da lista de sessões, um `+` que abre o formulário:

````swift
    @State private var mostrandoNovoTerminal = false
````

````swift
                ToolbarItem(placement: .primaryAction) {
                    Button {
                        mostrandoNovoTerminal = true
                    } label: {
                        Label("Novo terminal", systemImage: "plus")
                    }
                }
````

````swift
        .sheet(isPresented: $mostrandoNovoTerminal) {
            NovoTerminalForm(maquinas: model.machineNames,
                             gruposSugeridos: NovoTerminalFormLogic.gruposConhecidos(gruposAoVivo)) { maquina, alvo in
                // Criou → atualiza a lista e abre o espelho no pane novo. O alvo já
                // vem no formato composto que o resto do app usa.
                Task { await model.refreshLive() }
                abrirAoVivo(machine: maquina, target: alvo)
            }
        }
````

`model.machineNames` e `abrirAoVivo(machine:target:)` são os nomes esperados: use o
que o `SessionListView` já tem para (a) a lista de máquinas e (b) abrir uma linha ao
vivo — a função que o `liveRow` chama ao ser tocada. **Não crie um caminho novo de
abertura**; reuse o que existe, senão a Task G1 vai ter dois pontos de entrada para
reconciliar.

- [ ] **Step 5: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 6: Verificar de ponta a ponta**

Criar um terminal vazio num grupo novo pelo app e conferir: (a) aparece na lista, numa
seção nova, marcado como shell; (b) `tmux -L <grupo> ls` no Mac mostra a sessão; (c)
criar de novo com o mesmo nome **não** dá erro nem duplica.

- [ ] **Step 7: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/SessionListView.swift app/CutuqueApp/Models.swift \
        app/CutuqueAppTests/NovoTerminalFormTests.swift
git commit -m "feat(app): botao de novo terminal e shell marcado na lista"
```

## Chain G — as abas (F6 da spec)

O risco estrutural do plano mora aqui. A ordem é deliberada: **o modelo primeiro,
inteiro e testado, sem uma linha de View** (G1–G4), depois a barra (G5), depois a raiz
(G6), depois a largura (G7). Quem inverter isso vai depurar modelo dentro de
`RootSplitView`, que é o pior lugar do app para depurar qualquer coisa.

`OpenTabs` é um **struct de valor** com métodos `mutating`, não um `ObservableObject`.
É o que torna G1–G4 testáveis sem simulador nem hosting de View; o `ObservableObject`
é uma casca de três linhas em G5.

### Task G1: o modelo de abas

**Files:**
- Create: `app/CutuqueApp/OpenTabs.swift`
- Create: `app/CutuqueAppTests/OpenTabsTests.swift`

**Interfaces:**
- Consumes: `DetailSelection` (`NavigationState.swift:36`), `Session` (`Models.swift`), `Machine`, `BoardTask`, `LiveEntry` (`SessionListView.swift:1-22`).
- Produces (usado por G2–G7):
  - `enum TipoDeAba: String, Codable { case live, chat, board, maquina, arquivado }`
  - `struct ChaveDeAba: Hashable, Codable { let tipo: TipoDeAba; let machine: String; let alvo: String }` + `static func para(_ selection: DetailSelection) -> ChaveDeAba`, `.board`, `static func maquina(_ nome: String)`, `static func arquivado(_ id: String)`
  - `enum TabConteudo { case sessao(DetailSelection), board, maquina(Machine), arquivado(BoardTask), pendente, morta }`
  - `enum EstiloDeAba { case passagem, normal }` (+ `fixa: Bool` separado)
  - `struct AbaAberta: Identifiable, Equatable { let chave; var titulo; var estilo; var fixa; var conteudo; var ordemDeFoco }`
  - `struct OpenTabs: Equatable` com `abas`, `selecionada`, `abrir(_:titulo:estilo:)`, `selecionar(_:)`

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

/// O modelo de abas do iPad. Três regras que a Vanessa travou (D1, D2):
/// aba de passagem é SUBSTITUÍDA ao tocar noutra coisa (modelo VS Code);
/// abrir um alvo que já está aberto FOCA a aba existente; nunca há duas abas
/// para o mesmo alvo.
final class OpenTabsTests: XCTestCase {

    private let a = ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/s\t%1")
    private let b = ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/s\t%2")

    func testAbaDePassagemEhSubstituidaPelaProxima() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente)          // passagem
        t.abrir(chave: b, titulo: "aux", conteudo: .pendente)           // passagem
        XCTAssertEqual(t.abas.map(\.chave), [b], "a de passagem some no lugar da nova")
        XCTAssertEqual(t.selecionada, b)
    }

    func testAbaNormalNaoEhSubstituida() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.abrir(chave: b, titulo: "aux", conteudo: .pendente)
        XCTAssertEqual(t.abas.map(\.chave), [a, b])
    }

    /// Reabrir promove: é o equivalente do duplo clique do VS Code, e é o gesto
    /// que a Vanessa vai usar sem pensar quando quiser guardar a aba.
    func testReabrirUmaAbaDePassagemAPromoveENaoDuplica() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente)
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente)
        XCTAssertEqual(t.abas.count, 1)
        XCTAssertEqual(t.abas[0].estilo, .normal)
        XCTAssertEqual(t.selecionada, a)
    }

    /// Abrir de novo NÃO troca o conteúdo vivo por um `.pendente` — senão focar
    /// uma aba restaurada e já reconciliada a jogaria de volta pro limbo.
    func testAbrirDeNovoPreservaOConteudoJaResolvido() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .board, estilo: .normal)
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente)
        XCTAssertEqual(t.abas[0].conteudo, .board)
    }

    func testTituloEhAtualizadoQuandoAAbaJaExiste() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.abrir(chave: a, titulo: "mike renomeada", conteudo: .pendente)
        XCTAssertEqual(t.abas[0].titulo, "mike renomeada")
    }

    func testSelecionarSobeAOrdemDeFoco() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.abrir(chave: b, titulo: "aux", conteudo: .pendente, estilo: .normal)
        t.selecionar(a)
        XCTAssertEqual(t.selecionada, a)
        XCTAssertGreaterThan(t.abas[0].ordemDeFoco, t.abas[1].ordemDeFoco)
    }

    func testSelecionarChaveInexistenteNaoMudaNada() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        let antes = t
        t.selecionar(b)
        XCTAssertEqual(t, antes)
    }

    /// Uma aba por destino singular: Board aberto duas vezes é uma aba.
    func testDestinosSingularesNaoDuplicam() {
        var t = OpenTabs()
        t.abrir(chave: .board, titulo: "Board", conteudo: .board, estilo: .normal)
        t.abrir(chave: .board, titulo: "Board", conteudo: .board, estilo: .normal)
        XCTAssertEqual(t.abas.count, 1)
    }

    func testChaveDeLiveSeparaMaquinasComMesmoNomeDeGrupo() {
        // Por que isto tem teste: é a razão de `identidade-pane-ao-vivo` ser
        // pré-requisito. Dois grupos "defender", um em cada máquina, com o mesmo
        // socket — se a chave não levasse a máquina, seriam a MESMA aba.
        let noMacbook = ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/tmp/defender\t%1")
        let noWindows = ChaveDeAba(tipo: .live, machine: "windows", alvo: "/tmp/defender\t%1")
        XCTAssertNotEqual(noMacbook, noWindows)
    }
}
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: FAIL — `cannot find 'OpenTabs' in scope`.

- [ ] **Step 3: Implementar `OpenTabs.swift`**

````swift
import Foundation

enum TipoDeAba: String, Codable {
    case live, chat, board, maquina, arquivado
}

/// Identidade da aba, e a única coisa que vai pro disco (G4). Deliberadamente
/// pequena e `Codable`: guardar `Session`/`Machine` inteiros no `@AppStorage`
/// congelaria dados que envelhecem, e a reconciliação da G4 depende de a chave
/// ser só um endereço.
///
/// `machine` faz parte da identidade porque grupo de tmux com o mesmo nome pode
/// existir em duas máquinas — é o que `identidade-pane-ao-vivo` separou.
struct ChaveDeAba: Hashable, Codable {
    let tipo: TipoDeAba
    let machine: String
    let alvo: String

    init(tipo: TipoDeAba, machine: String = "", alvo: String = "") {
        self.tipo = tipo
        self.machine = machine
        self.alvo = alvo
    }

    static let board = ChaveDeAba(tipo: .board)
    static func maquina(_ nome: String) -> ChaveDeAba { .init(tipo: .maquina, alvo: nome) }
    static func arquivado(_ id: String) -> ChaveDeAba { .init(tipo: .arquivado, alvo: id) }

    static func para(_ selection: DetailSelection) -> ChaveDeAba {
        switch selection {
        case .live(let e):    return .init(tipo: .live, machine: e.machine, alvo: e.paneTarget)
        case .session(let s): return .init(tipo: .chat, machine: s.machine, alvo: s.id)
        }
    }
}

/// O que a aba mostra. `pendente` e `morta` existem por causa da persistência
/// (D2): uma aba restaurada do disco não recria pane nenhum — ela nasce
/// `pendente` e a reconciliação a resolve em conteúdo de verdade ou em `morta`,
/// que é um **aviso**, nunca uma tentativa de ressuscitar.
enum TabConteudo: Equatable {
    case sessao(DetailSelection)
    case board
    case maquina(Machine)
    case arquivado(BoardTask)
    case pendente
    case morta
}

/// `passagem` é a aba de preview do VS Code: existe no máximo uma, e a próxima
/// coisa aberta toma o lugar dela. `fixa` é ortogonal (G3) — protege de "fechar
/// outras"/"fechar todas".
enum EstiloDeAba {
    case passagem, normal
}

struct AbaAberta: Identifiable, Equatable {
    let chave: ChaveDeAba
    var titulo: String
    var estilo: EstiloDeAba
    var fixa: Bool
    var conteudo: TabConteudo
    /// Contador monotônico de foco, não relógio: dá MRU testável sem injetar
    /// tempo (G2 usa isto para decidir quem dorme).
    var ordemDeFoco: Int

    var id: ChaveDeAba { chave }
}

struct OpenTabs: Equatable {
    private(set) var abas: [AbaAberta] = []
    private(set) var selecionada: ChaveDeAba?
    private var contadorDeFoco = 0

    // MARK: abrir e selecionar

    mutating func abrir(chave: ChaveDeAba, titulo: String, conteudo: TabConteudo,
                        estilo: EstiloDeAba = .passagem) {
        contadorDeFoco += 1

        if let i = abas.firstIndex(where: { $0.chave == chave }) {
            // Já aberta: foca, atualiza o título e PROMOVE. Nunca duplica, e
            // nunca rebaixa um conteúdo resolvido de volta a `pendente`.
            abas[i].titulo = titulo
            abas[i].estilo = .normal
            abas[i].ordemDeFoco = contadorDeFoco
            if conteudo != .pendente { abas[i].conteudo = conteudo }
            selecionada = chave
            return
        }

        if estilo == .passagem, let i = abas.firstIndex(where: { $0.estilo == .passagem && !$0.fixa }) {
            abas.remove(at: i)
        }

        abas.append(AbaAberta(chave: chave, titulo: titulo, estilo: estilo,
                              fixa: false, conteudo: conteudo, ordemDeFoco: contadorDeFoco))
        selecionada = chave
    }

    mutating func selecionar(_ chave: ChaveDeAba) {
        guard let i = abas.firstIndex(where: { $0.chave == chave }) else { return }
        contadorDeFoco += 1
        abas[i].ordemDeFoco = contadorDeFoco
        selecionada = chave
    }

    func aba(_ chave: ChaveDeAba) -> AbaAberta? {
        abas.first { $0.chave == chave }
    }
}
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/OpenTabs.swift app/CutuqueAppTests/OpenTabsTests.swift
git commit -m "feat(app): modelo de abas com aba de passagem e foco sem duplicar"
```

### Task G2: teto de 6 vivas e quem dorme

**Files:**
- Modify: `app/CutuqueApp/OpenTabs.swift` (só acrescenta; nada do que a G1 escreveu muda)
- Test: `app/CutuqueAppTests/OpenTabsTests.swift`

**Interfaces:**
- Consumes: `TerminalPaneState` (Task D1) — **dependência real**: esta task não começa antes da Task D1 estar mergeada.
- Produces: `OpenTabs.maxVivas`, `OpenTabs.estado(de:) -> TerminalPaneState`, `OpenTabs.vivas: [ChaveDeAba]`.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// D3: teto de 6 vivas, o resto dorme, e dormir = DEVOLVER A LARGURA. O
    /// mapeamento é exatamente o modelo de três estados da Task D1:
    ///   selecionada          → .ativo     (poll + largura aplicada)
    ///   viva, não escolhida  → .suspenso  (sem poll, largura mantida)
    ///   dormindo             → .liberado  (sem poll, largura devolvida)
    func testSeisVivasEOSetimoDorme() {
        var t = OpenTabs()
        let chaves = (1...7).map { ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/s\t%\($0)") }
        for c in chaves { t.abrir(chave: c, titulo: c.alvo, conteudo: .pendente, estilo: .normal) }

        XCTAssertEqual(t.selecionada, chaves[6])
        XCTAssertEqual(t.estado(de: chaves[6]), .ativo)
        // Os 5 seguintes mais recentes seguem vivos, mas suspensos.
        for c in chaves[2...5] { XCTAssertEqual(t.estado(de: c), .suspenso, "\(c.alvo)") }
        // O mais antigo dorme: 7 abertas, teto de 6.
        XCTAssertEqual(t.estado(de: chaves[0]), .liberado)
        XCTAssertEqual(t.vivas.count, OpenTabs.maxVivas)
    }

    /// Quem dorme é o menos usado, não o mais antigo na barra: focar acorda.
    func testFocarAcordaEEmpurraOMenosUsadoParaDormir() {
        var t = OpenTabs()
        let chaves = (1...7).map { ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/s\t%\($0)") }
        for c in chaves { t.abrir(chave: c, titulo: c.alvo, conteudo: .pendente, estilo: .normal) }
        XCTAssertEqual(t.estado(de: chaves[0]), .liberado)

        t.selecionar(chaves[0])
        XCTAssertEqual(t.estado(de: chaves[0]), .ativo)
        XCTAssertEqual(t.estado(de: chaves[1]), .liberado, "o que sobrou de menos usado passa a dormir")
    }

    /// Fixar protege de fechar, NÃO de dormir. São eixos diferentes de propósito:
    /// dormir é custo de tmux (largura + poll), fixar é intenção de navegação.
    /// Quem "melhorar" isto acordando as fixas fura o teto de 6.
    func testFixarNaoImpedeDeDormir() {
        var t = OpenTabs()
        let chaves = (1...7).map { ChaveDeAba(tipo: .live, machine: "macbook", alvo: "/s\t%\($0)") }
        for c in chaves { t.abrir(chave: c, titulo: c.alvo, conteudo: .pendente, estilo: .normal) }
        t.fixar(chaves[0])
        XCTAssertEqual(t.estado(de: chaves[0]), .liberado)
    }

    func testEstadoDeAbaQueNaoExisteEhLiberado() {
        let t = OpenTabs()
        XCTAssertEqual(t.estado(de: a), .liberado)
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: FAIL — `has no member 'estado'`.

- [ ] **Step 3: Implementar**

Acrescentar em `OpenTabs`:

````swift
    /// Teto de painéis vivos ao mesmo tempo (D3). O número saiu da conversa —
    /// "rola, 6 vivas + dormindo" — e existe porque cada painel vivo custa um
    /// `window-size manual` no tmux do outro lado, não só memória do app.
    static let maxVivas = 6

    /// As `maxVivas` abas mais recentemente focadas. MRU, não ordem da barra:
    /// dormir tem de seguir o uso, senão a aba da esquerda morreria sempre.
    var vivas: [ChaveDeAba] {
        abas.sorted { $0.ordemDeFoco > $1.ordemDeFoco }
            .prefix(Self.maxVivas)
            .map(\.chave)
    }

    /// O estado do painel desta aba. Este método é a ÚNICA fonte do estado que
    /// a `TerminalMirrorView` recebe (Task D1) — nada de View decidindo por conta.
    func estado(de chave: ChaveDeAba) -> TerminalPaneState {
        guard abas.contains(where: { $0.chave == chave }) else { return .liberado }
        if chave == selecionada { return .ativo }
        return vivas.contains(chave) ? .suspenso : .liberado
    }
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: PASS. (O teste de `fixar` só passa depois da G3 — se preferir, escreva-o na G3; se escrever agora, ele falha por falta do método, o que é o comportamento esperado de TDD e não um erro do plano.)

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/OpenTabs.swift app/CutuqueAppTests/OpenTabsTests.swift
git commit -m "feat(app): teto de 6 painéis vivos e quem dorme devolve a largura"
```

### Task G3: fixar, fechar, fechar outras, fechar todas

**Files:**
- Modify: `app/CutuqueApp/OpenTabs.swift`
- Test: `app/CutuqueAppTests/OpenTabsTests.swift`

**Interfaces:**
- Produces: `fixar(_:)`, `desafixar(_:)`, `fechar(_:)`, `fecharOutras(_:)`, `fecharTodas()` — todos `mutating`. Consumidos pela G5.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    func testFecharEscolheAVizinhaDaEsquerda() {
        var t = OpenTabs()
        let c = (1...3).map { ChaveDeAba(tipo: .live, machine: "m", alvo: "/s\t%\($0)") }
        for k in c { t.abrir(chave: k, titulo: k.alvo, conteudo: .pendente, estilo: .normal) }
        t.selecionar(c[1])
        t.fechar(c[1])
        XCTAssertEqual(t.abas.map(\.chave), [c[0], c[2]])
        XCTAssertEqual(t.selecionada, c[0], "a vizinha da esquerda; sem esquerda, a da direita")
    }

    func testFecharAPrimeiraEscolheADireita() {
        var t = OpenTabs()
        let c = (1...2).map { ChaveDeAba(tipo: .live, machine: "m", alvo: "/s\t%\($0)") }
        for k in c { t.abrir(chave: k, titulo: k.alvo, conteudo: .pendente, estilo: .normal) }
        t.selecionar(c[0])
        t.fechar(c[0])
        XCTAssertEqual(t.selecionada, c[1])
    }

    func testFecharUmaQueNaoEstaEmFocoNaoMudaOFoco() {
        var t = OpenTabs()
        let c = (1...2).map { ChaveDeAba(tipo: .live, machine: "m", alvo: "/s\t%\($0)") }
        for k in c { t.abrir(chave: k, titulo: k.alvo, conteudo: .pendente, estilo: .normal) }
        t.selecionar(c[1])
        t.fechar(c[0])
        XCTAssertEqual(t.selecionada, c[1])
    }

    /// D2: fechar a última é permitido e deixa o painel vazio — o estado sem
    /// nenhuma aba é legítimo, não um caso de erro. Quem "consertar" isso
    /// recusando o fechamento tira da Vanessa a única forma de zerar a tela.
    func testFecharAUltimaDeixaNadaSelecionado() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.fechar(a)
        XCTAssertTrue(t.abas.isEmpty)
        XCTAssertNil(t.selecionada)
    }

    func testFecharOutrasPoupaAFixaEAPropria() {
        var t = OpenTabs()
        let c = (1...4).map { ChaveDeAba(tipo: .live, machine: "m", alvo: "/s\t%\($0)") }
        for k in c { t.abrir(chave: k, titulo: k.alvo, conteudo: .pendente, estilo: .normal) }
        t.fixar(c[0])
        t.fecharOutras(c[2])
        XCTAssertEqual(Set(t.abas.map(\.chave)), Set([c[0], c[2]]))
        XCTAssertEqual(t.selecionada, c[2])
    }

    func testFecharTodasPoupaAsFixasEEscolheAPrimeiraQueSobrou() {
        var t = OpenTabs()
        let c = (1...3).map { ChaveDeAba(tipo: .live, machine: "m", alvo: "/s\t%\($0)") }
        for k in c { t.abrir(chave: k, titulo: k.alvo, conteudo: .pendente, estilo: .normal) }
        t.fixar(c[1])
        t.fecharTodas()
        XCTAssertEqual(t.abas.map(\.chave), [c[1]])
        XCTAssertEqual(t.selecionada, c[1])
    }

    func testFecharTodasSemNenhumaFixaZeraTudo() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.fecharTodas()
        XCTAssertTrue(t.abas.isEmpty)
        XCTAssertNil(t.selecionada)
    }

    /// Fixar uma aba de passagem também a promove: senão a próxima coisa aberta
    /// substituiria a aba que a Vanessa acabou de mandar ficar.
    func testFixarPromoveAAbaDePassagem() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente)
        t.fixar(a)
        XCTAssertEqual(t.abas[0].estilo, .normal)
        XCTAssertTrue(t.abas[0].fixa)

        t.abrir(chave: b, titulo: "aux", conteudo: .pendente)
        XCTAssertEqual(t.abas.count, 2)
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: FAIL — `has no member 'fechar'`.

- [ ] **Step 3: Implementar**

````swift
    mutating func fixar(_ chave: ChaveDeAba) {
        guard let i = abas.firstIndex(where: { $0.chave == chave }) else { return }
        abas[i].fixa = true
        abas[i].estilo = .normal
    }

    mutating func desafixar(_ chave: ChaveDeAba) {
        guard let i = abas.firstIndex(where: { $0.chave == chave }) else { return }
        abas[i].fixa = false
    }

    mutating func fechar(_ chave: ChaveDeAba) {
        guard let i = abas.firstIndex(where: { $0.chave == chave }) else { return }
        let eraAEscolhida = selecionada == chave
        abas.remove(at: i)
        guard eraAEscolhida else { return }
        // Vizinha da esquerda; sem esquerda, a da direita; sem nenhuma, nada
        // escolhido (painel vazio é estado legítimo).
        let vizinha = i > 0 ? abas[i - 1] : (i < abas.count ? abas[i] : nil)
        if let vizinha {
            selecionar(vizinha.chave)
        } else {
            selecionada = nil
        }
    }

    mutating func fecharOutras(_ chave: ChaveDeAba) {
        abas.removeAll { $0.chave != chave && !$0.fixa }
        if abas.contains(where: { $0.chave == chave }) { selecionar(chave) }
    }

    mutating func fecharTodas() {
        abas.removeAll { !$0.fixa }
        if let primeira = abas.first {
            selecionar(primeira.chave)
        } else {
            selecionada = nil
        }
    }
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: PASS (agora inclusive o `testFixarNaoImpedeDeDormir` da G2).

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/OpenTabs.swift app/CutuqueAppTests/OpenTabsTests.swift
git commit -m "feat(app): fixar e fechar abas com escolha da vizinha"
```

### Task G4: persistência e reconciliação (aba morta = aviso)

**Files:**
- Modify: `app/CutuqueApp/OpenTabs.swift`
- Test: `app/CutuqueAppTests/OpenTabsTests.swift`

**Interfaces:**
- Produces:
  - `struct AbaPersistida: Codable { let chave: ChaveDeAba; let titulo: String; let fixa: Bool }`
  - `OpenTabs.paraPersistir: [AbaPersistida]`
  - `static func restaurando(_ salvas: [AbaPersistida]) -> OpenTabs`
  - `mutating func reconciliar(vivas: [ChaveDeAba: TabConteudo])`
- Consumido pela G5 (que faz o `@AppStorage` e chama a reconciliação quando a lista ao vivo chega).

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// D2: as abas voltam ao reabrir o app, e uma aba cujo pane morreu vira
    /// AVISO — nunca uma tentativa de recriar o pane. Recriar seria abrir sessão
    /// de tmux sem a Vanessa pedir, no boot do app.
    func testRoundTripDaPersistencia() {
        var t = OpenTabs()
        t.abrir(chave: a, titulo: "mike", conteudo: .pendente, estilo: .normal)
        t.abrir(chave: .board, titulo: "Board", conteudo: .board, estilo: .normal)
        t.fixar(a)

        let dados = try! JSONEncoder().encode(t.paraPersistir)
        let salvas = try! JSONDecoder().decode([AbaPersistida].self, from: dados)
        let voltou = OpenTabs.restaurando(salvas)

        XCTAssertEqual(voltou.abas.map(\.chave), [a, .board])
        XCTAssertEqual(voltou.abas.map(\.titulo), ["mike", "Board"])
        XCTAssertTrue(voltou.abas[0].fixa)
        XCTAssertEqual(voltou.selecionada, a, "restaura escolhendo a primeira")
        // Nada de conteúdo antes da reconciliação: o pane não é recriado no boot.
        XCTAssertEqual(voltou.abas.map(\.conteudo), [.pendente, .pendente])
        // E nada de passagem: aba restaurada é aba que a Vanessa quis guardar.
        XCTAssertTrue(voltou.abas.allSatisfy { $0.estilo == .normal })
    }

    func testReconciliarResolveAsVivasEMarcaAsMortas() {
        var t = OpenTabs.restaurando([
            AbaPersistida(chave: a, titulo: "mike", fixa: false),
            AbaPersistida(chave: b, titulo: "aux", fixa: false),
        ])
        t.reconciliar(vivas: [a: .board])   // `.board` aqui é só um conteúdo qualquer não-pendente
        XCTAssertEqual(t.aba(a)?.conteudo, .board)
        XCTAssertEqual(t.aba(b)?.conteudo, .morta, "existia, não existe mais → aviso")
        XCTAssertEqual(t.abas.count, 2, "a aba morta FICA na barra; ela é o aviso")
    }

    /// Reconciliar de novo depois de a sessão voltar (hub reiniciou, ssh caiu e
    /// voltou) tem de RESSUSCITAR a aba: `morta` é um estado da vez, não uma
    /// sentença. Sem isto, um blip de rede deixa a barra cheia de avisos até a
    /// Vanessa fechar cada um à mão.
    func testAbaMortaVoltaAVidaSeOAlvoReaparecer() {
        var t = OpenTabs.restaurando([AbaPersistida(chave: a, titulo: "mike", fixa: false)])
        t.reconciliar(vivas: [:])
        XCTAssertEqual(t.aba(a)?.conteudo, .morta)
        t.reconciliar(vivas: [a: .board])
        XCTAssertEqual(t.aba(a)?.conteudo, .board)
    }

    /// Board e Arquivo não dependem de nada vivo: reconciliar não pode matá-los.
    func testDestinosQueNaoDependemDeTmuxNaoMorrem() {
        var t = OpenTabs()
        t.abrir(chave: .board, titulo: "Board", conteudo: .board, estilo: .normal)
        t.reconciliar(vivas: [:])
        XCTAssertEqual(t.aba(.board)?.conteudo, .board)
    }

    func testRestaurarNadaDaUmModeloVazio() {
        let t = OpenTabs.restaurando([])
        XCTAssertTrue(t.abas.isEmpty)
        XCTAssertNil(t.selecionada)
    }
````

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: FAIL — `cannot find 'AbaPersistida' in scope`.

- [ ] **Step 3: Implementar**

````swift
/// O que vai pro disco: chave, título e se é fixa. Conteúdo NÃO vai — é ele que
/// envelhece, e recriá-lo do disco significaria abrir pane de tmux no boot.
struct AbaPersistida: Codable, Equatable {
    let chave: ChaveDeAba
    let titulo: String
    let fixa: Bool
}
````

E em `OpenTabs`:

````swift
    var paraPersistir: [AbaPersistida] {
        abas.map { AbaPersistida(chave: $0.chave, titulo: $0.titulo, fixa: $0.fixa) }
    }

    /// Abas do disco nascem `pendente` e `normal`. A ordem de foco é a da barra
    /// (a primeira é a mais "recente"), o que faz o teto de 6 da G2 dormir as da
    /// direita até a Vanessa tocar nelas.
    static func restaurando(_ salvas: [AbaPersistida]) -> OpenTabs {
        var t = OpenTabs()
        for (i, s) in salvas.enumerated() {
            t.abas.append(AbaAberta(chave: s.chave, titulo: s.titulo, estilo: .normal,
                                    fixa: s.fixa, conteudo: .pendente,
                                    ordemDeFoco: salvas.count - i))
        }
        t.contadorDeFoco = salvas.count
        t.selecionada = t.abas.first?.chave
        return t
    }

    /// Casa as abas com o que está vivo agora. Quem não aparece vira `.morta` —
    /// um aviso na barra, e nada mais: sem recriar pane, sem fechar sozinho.
    /// Reversível de propósito (`morta` → viva de novo) porque hub reiniciado e
    /// ssh que caiu e voltou são o caso comum, não o raro.
    mutating func reconciliar(vivas: [ChaveDeAba: TabConteudo]) {
        for i in abas.indices {
            let chave = abas[i].chave
            if let conteudo = vivas[chave] {
                abas[i].conteudo = conteudo
            } else if Self.dependeDeAlgoVivo(chave.tipo) {
                abas[i].conteudo = .morta
            }
        }
    }

    /// Board e Arquivo são telas do hub, não panes: não morrem por ausência na
    /// lista ao vivo. Máquina também não — o host existe no registro mesmo com
    /// o ssh caído, e quem mostra "não conectei" é a própria tela da máquina.
    private static func dependeDeAlgoVivo(_ tipo: TipoDeAba) -> Bool {
        switch tipo {
        case .live, .chat:              return true
        case .board, .maquina, .arquivado: return false
        }
    }
````

`contadorDeFoco` é `private`; como `restaurando` é `static` no mesmo tipo, o acesso é
legal. Se o compilador reclamar, torne-o `private(set)` — **não** o torne público.

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/OpenTabsTests -quiet`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/OpenTabs.swift app/CutuqueAppTests/OpenTabsTests.swift
git commit -m "feat(app): persistencia de abas com aba morta virando aviso"
```

### Task G5: a barra de abas

**Files:**
- Create: `app/CutuqueApp/TabBar.swift` (a `View` **e** o `OpenTabsStore`)
- Test: nenhum novo — a lógica toda já tem teste em `OpenTabsTests`; o que sobra aqui é View, e View se verifica na tela (Step 5).

**Interfaces:**
- Consumes: `OpenTabs` inteiro (G1–G4).
- Produces:
  - `final class OpenTabsStore: ObservableObject` com `@Published var tabs: OpenTabs`, `salvar()`, `restaurar()`
  - `struct TabBar: View`
  - Consumido pela G6.

- [ ] **Step 1: Escrever o store**

````swift
import SwiftUI

/// Casca observável em volta do `OpenTabs` (que é struct de valor, e é por isso
/// que ele todo tem teste sem simulador). Só duas responsabilidades: publicar as
/// mudanças e falar com o disco.
@MainActor
final class OpenTabsStore: ObservableObject {
    @Published var tabs = OpenTabs()

    private let chaveNoDisco = "abasAbertas.v1"

    init() { restaurar() }

    func restaurar() {
        guard let dados = UserDefaults.standard.data(forKey: chaveNoDisco),
              let salvas = try? JSONDecoder().decode([AbaPersistida].self, from: dados)
        else { return }
        tabs = OpenTabs.restaurando(salvas)
    }

    /// Chamado depois de cada mutação. `UserDefaults` direto em vez de
    /// `@AppStorage` porque o que se guarda é uma LISTA — `@AppStorage` só sabe
    /// tipos escalares, e embrulhar JSON nele daria a mesma coisa com mais cerimônia.
    func salvar() {
        guard let dados = try? JSONEncoder().encode(tabs.paraPersistir) else { return }
        UserDefaults.standard.set(dados, forKey: chaveNoDisco)
    }

    /// Toda mutação passa por aqui: muda e salva, sem dois caminhos.
    func mutar(_ bloco: (inout OpenTabs) -> Void) {
        bloco(&tabs)
        salvar()
    }
}
````

- [ ] **Step 2: Escrever a barra**

````swift
/// Barra de abas. D5: mora DENTRO da coluna de conteúdo, nunca na raiz — a
/// decisão #19 do projeto diz que a split view é construída uma vez e nunca
/// substituída, e uma barra por cima da raiz obrigaria justamente a trocar a
/// raiz. Quem for "melhorar" isto pondo a barra num `VStack` em volta da
/// `NavigationSplitView` reintroduz o bug que a #19 existe para impedir.
struct TabBar: View {
    @ObservedObject var store: OpenTabsStore

    var body: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 2) {
                ForEach(store.tabs.abas) { aba in
                    botao(aba)
                }
            }
            .padding(.horizontal, 8)
            .padding(.vertical, 4)
        }
        .background(.bar)
        .overlay(alignment: .bottom) { Divider() }
    }

    private func botao(_ aba: AbaAberta) -> some View {
        let escolhida = store.tabs.selecionada == aba.chave
        return Button {
            store.mutar { $0.selecionar(aba.chave) }
        } label: {
            HStack(spacing: 6) {
                if aba.fixa {
                    Image(systemName: "pin.fill").font(.caption2)
                }
                if aba.conteudo == .morta {
                    // O aviso de D2, na própria aba: a sessão não existe mais e
                    // NADA vai ser recriado. A aba fica até a Vanessa fechar.
                    Image(systemName: "exclamationmark.triangle.fill")
                        .font(.caption2)
                        .foregroundStyle(.orange)
                }
                Text(aba.titulo)
                    .lineLimit(1)
                    // Itálico é a marca da aba de passagem, como no VS Code.
                    .italic(aba.estilo == .passagem)
                Button {
                    store.mutar { $0.fechar(aba.chave) }
                } label: {
                    Image(systemName: "xmark").font(.caption2)
                }
                .buttonStyle(.plain)
                .accessibilityLabel("Fechar \(aba.titulo)")
            }
            .padding(.horizontal, 10)
            .padding(.vertical, 6)
            .frame(maxWidth: 200)
            .background(escolhida ? AnyShapeStyle(.selection) : AnyShapeStyle(.clear),
                        in: RoundedRectangle(cornerRadius: 6))
            .contentShape(Rectangle())
        }
        .buttonStyle(.plain)
        .foregroundStyle(escolhida ? .primary : .secondary)
        // D6: toque longo abre o menu. `contextMenu` é o toque longo do iPadOS —
        // não inventar gesto próprio, que brigaria com a rolagem da barra.
        .contextMenu {
            if aba.fixa {
                Button("Desafixar", systemImage: "pin.slash") {
                    store.mutar { $0.desafixar(aba.chave) }
                }
            } else {
                Button("Fixar", systemImage: "pin") {
                    store.mutar { $0.fixar(aba.chave) }
                }
            }
            Button("Fechar", systemImage: "xmark") {
                store.mutar { $0.fechar(aba.chave) }
            }
            Button("Fechar outras", systemImage: "xmark.square") {
                store.mutar { $0.fecharOutras(aba.chave) }
            }
            Button("Fechar todas", systemImage: "xmark.square.fill") {
                store.mutar { $0.fecharTodas() }
            }
        }
    }
}
````

- [ ] **Step 3: Rodar a suíte inteira**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS — nada quebrou; a barra ainda não está montada em lugar nenhum.

- [ ] **Step 4: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/TabBar.swift
git commit -m "feat(app): barra de abas e store observavel"
```

### Task G6: N painéis na coluna de detalhe

**Files:**
- Modify: `app/CutuqueApp/RootSplitView.swift:241-294` (a `detailColumn`)
- Modify: `app/CutuqueApp/CutuqueApp.swift` (injetar o `OpenTabsStore`)
- Modify: `app/CutuqueApp/SessionListView.swift` (tocar numa sessão abre aba, em vez de trocar `nav.selection`)

**Interfaces:**
- Consumes: `OpenTabsStore`, `TabBar` (G5), `OpenTabs.estado(de:)` (Task G2), `TerminalPaneState` (Task D1).
- Produces: nada novo — é a montagem.

**Esta é a task mais perigosa do plano.** Leia os três parágrafos antes de tocar em
código.

- [ ] **Step 1: Entender o que sai e por quê**

`RootSplitView.swift:247` é hoje:

````swift
                SessionDetailPane(selection: selection).id(selection)
````

com o comentário: _".id força a troca de sessão a destruir o painel anterior — é aí, e
só aí, que o `restoreSize()` do terminal deve rodar."_

O `.id` **sai**. Com abas, trocar de aba não pode destruir nada — é exatamente o que a
decisão #19 proíbe e o que faria o espelho perder a rolagem a cada toque. O que
substitui a garantia que o `.id` dava é o par **G2 + D1**: o painel que não está em foco
recebe `.suspenso` (para o poll, mantém a largura) e o que dorme recebe `.liberado`
(devolve a largura). Ou seja: a devolução de largura deixa de ser efeito de
desmontagem e passa a ser **estado explícito**.

**Reescreva o comentário com data em vez de apagá-lo** — ele documenta um bug real
(constraint global). Algo como: _"até 08/2026 este `.id` era o que devolvia a largura ao
tmux, por desmontagem. Com abas, quem devolve é `TerminalPaneState.liberado` (ver
`OpenTabs.estado(de:)`) — desmontar por troca de aba mataria a rolagem do espelho."_

- [ ] **Step 2: Trocar a `detailColumn` por abas**

````swift
    @ViewBuilder private var detailColumn: some View {
        VStack(spacing: 0) {
            if !tabsStore.tabs.abas.isEmpty {
                TabBar(store: tabsStore)
            }
            ZStack {
                // TODOS os painéis abertos ficam montados, alternando por
                // opacidade (decisão #19). Trocar de aba não remonta nada — é
                // o mesmo desenho que o `SessionDetailPane` já usa entre chat,
                // terminal e informações, um nível acima.
                ForEach(tabsStore.tabs.abas) { aba in
                    let escolhida = tabsStore.tabs.selecionada == aba.chave
                    painel(aba)
                        .opacity(escolhida ? 1 : 0)
                        .allowsHitTesting(escolhida)
                        .accessibilityHidden(!escolhida)
                }
                if tabsStore.tabs.abas.isEmpty {
                    ContentUnavailableView("Nada aberto", systemImage: "square.on.square",
                                           description: Text("Toque numa sessão, no Board ou numa máquina."))
                }
            }
        }
    }

    @ViewBuilder private func painel(_ aba: AbaAberta) -> some View {
        switch aba.conteudo {
        case .sessao(let selection):
            SessionDetailPane(selection: selection,
                              paneState: tabsStore.tabs.estado(de: aba.chave))
        case .board:
            BoardView(embedded: true)
        case .maquina(let machine):
            NavigationStack { MachineDetailView(machine: machine) }
                .id(machine.name)   // este `.id` FICA: ver o comentário de :270-279
        case .arquivado(let task):
            ArchivedTaskPane(task: task)
        case .pendente:
            ProgressView()
        case .morta:
            // D2: aviso, nunca recriação. Fechar é decisão da Vanessa.
            ContentUnavailableView("Sessão encerrada", systemImage: "exclamationmark.triangle",
                                   description: Text("Essa sessão não está mais viva. A aba fica aqui até você fechá-la."))
        }
    }
````

`SessionDetailPane` ganha um parâmetro: `let paneState: TerminalPaneState`, repassado
para a `TerminalMirrorView` no lugar do `showsTerminal ? .ativo : .liberado` que a Task
D2 pôs em `:86`. A regra composta é: **o painel só faz poll se a aba está em foco E o
seletor está no terminal**:

````swift
        // A aba pode estar viva com o seletor no chat: aí o espelho mantém a
        // largura (`.suspenso`) mas não faz poll.
        let estadoDoEspelho: TerminalPaneState = showsTerminal ? paneState
                                                              : (paneState == .liberado ? .liberado : .suspenso)
````

- [ ] **Step 3: Injetar o store e trocar o toque na lista**

Em `CutuqueApp.swift`, ao lado dos outros `@StateObject`/`.environmentObject`:

````swift
    @StateObject private var tabsStore = OpenTabsStore()
````
…e `.environmentObject(tabsStore)` na cena.

Em `RootSplitView`: `@EnvironmentObject private var tabsStore: OpenTabsStore`.

Em `SessionListView`, onde hoje se faz `nav.selection = .live(entry)` (ou equivalente),
passe a abrir aba **de passagem**:

````swift
        tabsStore.mutar {
            $0.abrir(chave: .para(selection), titulo: titulo,
                     conteudo: .sessao(selection))   // estilo .passagem é o default
        }
        nav.selection = selection   // continua existindo: é o que o iPhone usa
````

`nav.selection` **não é aposentado**: no iPhone a navegação segue sendo pilha, e
`sessionListLivesInDetail` (`:248`) depende dele. As abas são um mecanismo do iPad
montado por cima, não uma substituição do modelo de navegação.

- [ ] **Step 4: Reconciliar quando a lista ao vivo chega**

Onde a lista ao vivo é recarregada (o mesmo lugar que alimenta `gruposAoVivo`, Task E2),
chame:

````swift
        // As abas restauradas do disco só ganham conteúdo aqui: é a primeira vez
        // que o app sabe o que ainda está vivo (G4).
        tabsStore.mutar { $0.reconciliar(vivas: vivasPorChave) }
````

com `vivasPorChave: [ChaveDeAba: TabConteudo]` montado das entradas ao vivo:
`[ChaveDeAba.para(.live(entry)): .sessao(.live(entry))]`.

- [ ] **Step 5: Rodar os testes**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 6: Verificar na tela (obrigatório — aqui teste não alcança)**

1. Abrir três sessões ao vivo. As três viram abas; trocar entre elas **não** reinicia o espelho (a rolagem fica onde estava).
2. Com o iPad em duas colunas, `tmux -L <grupo> show-window-options -t <sessão> window-size` na aba **em foco** → `manual`; nas abas vivas não focadas → `manual` (largura mantida); numa aba dormindo (abra sete) → `latest`.
3. Fechar uma aba → largura volta a `latest` naquela sessão.
4. Matar a sessão pelo Mac (`tmux -L <g> kill-session -t <s>`), recarregar a lista: a aba fica, com o triângulo, e **nenhuma** sessão nova aparece no `tmux ls`.
5. Fechar o app e reabrir: as abas voltam.

- [ ] **Step 7: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/RootSplitView.swift app/CutuqueApp/CutuqueApp.swift \
        app/CutuqueApp/SessionListView.swift app/CutuqueApp/SessionDetailPane.swift
git commit -m "feat(app): coluna de detalhe com N paineis por aba"
```

### Task G7: duas colunas sempre no iPad

**Files:**
- Modify: `app/CutuqueApp/NavigationState.swift:208-259` (`expandedVisibility`)
- Test: `app/CutuqueAppTests/NavigationStateTests.swift` (o arquivo que já testa `layoutVisibility`)

**Interfaces:**
- Consumes: nada.
- Produces: nada — muda comportamento de uma função que já tem teste.

- [ ] **Step 1: Escrever o teste que falha**

````swift
    /// D4: a barra lateral unificada é de DUAS colunas sempre — é ela que faz o
    /// app "parecer PC" e é o que devolve largura no 11". Antes disto, retrato
    /// dava `.doubleColumn` e paisagem dava `.all` (três colunas), e a coluna do
    /// meio comia a largura que o terminal precisava.
    func testExpandidoEhSempreDuasColunasNasSessoes() {
        let nav = NavigationState()
        nav.destination = .sessions
        nav.lastIsPortrait = false
        XCTAssertEqual(nav.expandedVisibility, .doubleColumn)
        nav.lastIsPortrait = true
        XCTAssertEqual(nav.expandedVisibility, .doubleColumn)
    }

    /// O Board é a exceção declarada: `contentColumn` (`RootSplitView.swift:228`)
    /// depende de `columnVisibility == .all` para NÃO desenhar a lista de destinos
    /// duas vezes lado a lado. Mexer nisto sem ler aquele comentário duplica a
    /// sidebar na tela.
    func testBoardSegueComOArranjoDeleProprio() {
        let nav = NavigationState()
        nav.destination = .board
        XCTAssertEqual(nav.expandedVisibility, .doubleColumn)
    }
````

Confirme o nome real de `lastIsPortrait` (é `private` hoje?) antes de escrever o teste:
se estiver privado, teste por `layoutVisibility(isPortrait:)`, que é a função pública
que já tem cobertura — **não** afrouxe o acesso de um campo só para testar.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/NavigationStateTests -quiet`
Expected: FAIL — paisagem devolve `.all`.

- [ ] **Step 3: Implementar**

Em `expandedVisibility`, o caso `.sessions, .machines` passa a devolver `.doubleColumn`
sem olhar a orientação. Trocar o comentário existente por um que diga o porquê, com
data: a coluna do meio deixou de ser necessária quando a barra de abas passou a dar a
navegação dentro do detalhe.

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 5: Verificar no iPad**

O ☰ continua existindo e ainda esconde a lateral, deixando só as abas + o painel; o
botão de tela cheia do terminal continua funcionando. Foi pedido explicitamente: _"o
menu hamburguer ainda vai existir pra caso queira minimizar o menu lateral, ficando
apenas as abas e o terminal/info, certo? e o tela cheia do terminal tb"_ — a resposta
tem de continuar sendo sim depois desta task.

- [ ] **Step 6: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/NavigationState.swift app/CutuqueAppTests/NavigationStateTests.swift
git commit -m "feat(app): barra lateral unificada de duas colunas no ipad"
```

## Chain H — `LiveHub`: uma conexão para N abas (F7 da spec)

Consequência direta do teto de 6. Hoje **cada** `SessionDetailViewModel` abre o próprio
WebSocket (`SessionDetailView.swift:56-88`) e filtra do lado do cliente
(`where updated.id == self.session.id`). Com seis painéis montados são seis conexões ao
`/ws` fazendo o mesmo trabalho seis vezes, cada uma com o próprio backoff de reconexão.

Esta chain é a única que **melhora algo que já funciona** em vez de entregar função nova.
Por isso ela é a última: se o tempo acabar, o app funciona sem ela — só gasta mais
conexão do que devia.

### Task H1: o hub de distribuição

**Files:**
- Create: `app/CutuqueApp/LiveHub.swift`
- Create: `app/CutuqueAppTests/LiveHubTests.swift`

**Interfaces:**
- Consumes: `CutuqueAPI.liveUpdates() -> AsyncStream<WSMessage>` (`APIClient.swift:1205`) e `WSMessage`.
- Produces:
  - `enum LiveHubRouting { static func destinatarios(de mensagem: WSMessage, inscritos: [String]) -> [String] }`
  - `@MainActor final class LiveHub: ObservableObject` com `inscrever(sessionID:) -> AsyncStream<WSMessage>` e `cancelar(sessionID:)`
  - Consumido pela Task H2.

- [ ] **Step 1: Escrever o teste que falha**

````swift
import XCTest
@testable import CutuqueApp

/// Roteamento da conexão única. É a parte que decide QUEM recebe o quê, extraída
/// como função pura porque a conexão em si não se testa sem hub de verdade.
final class LiveHubTests: XCTestCase {

    private func sessao(_ id: String) -> Session {
        Session(id: id, machine: "macbook", agent: "claude", title: id,
                state: .idle, createdAt: Date(timeIntervalSince1970: 0),
                updatedAt: Date(timeIntervalSince1970: 0))
    }

    func testAtualizacaoDeSessaoVaiSoParaQuemEhDaquelaSessao() {
        let destinos = LiveHubRouting.destinatarios(de: .sessionUpdated(sessao("s1")),
                                                    inscritos: ["s1", "s2"])
        XCTAssertEqual(destinos, ["s1"])
    }

    func testChunkVaiSoParaASessaoDele() {
        let destinos = LiveHubRouting.destinatarios(de: .outputChunk("s2", "stdout", "oi"),
                                                    inscritos: ["s1", "s2"])
        XCTAssertEqual(destinos, ["s2"])
    }

    func testMensagemDeSessaoQueNinguemAssinaNaoVaiParaNinguem() {
        let destinos = LiveHubRouting.destinatarios(de: .outputChunk("s9", "stdout", "oi"),
                                                    inscritos: ["s1", "s2"])
        XCTAssertTrue(destinos.isEmpty)
    }

    /// Snapshot é do estado inteiro: interessa a todos, e cada um se acha nele
    /// (é o que o `SessionDetailViewModel` já faz em `:80-84`).
    func testSnapshotVaiParaTodos() {
        let destinos = LiveHubRouting.destinatarios(de: .snapshot([sessao("s1")]),
                                                    inscritos: ["s1", "s2"])
        XCTAssertEqual(destinos, ["s1", "s2"])
    }

    /// A regra que evita o bug silencioso: tipo de mensagem que este roteador não
    /// conhece vai para TODOS, e quem recebe filtra — igual ao `default: break`
    /// que o ViewModel já tem. Roteador que descarta o desconhecido faria uma
    /// mensagem nova do hub simplesmente nunca chegar, sem erro nenhum.
    ///
    /// Escreva UM caso por membro do enum que não seja `sessionUpdated` nem
    /// `outputChunk`. Para saber quais são, antes de escrever o teste:
    ///     grep -n "enum WSMessage" -A 20 app/CutuqueApp/Models.swift
    /// Se o enum tiver só `snapshot` além dos dois, o teste acima já cobre e este
    /// aqui sai — não deixe um teste que repete a assertiva do vizinho fingindo
    /// cobrir outra coisa.
    func testCadaOutroTipoDeMensagemVaiParaTodos() {
        for mensagem in outrosTiposDeMensagem {
            XCTAssertEqual(LiveHubRouting.destinatarios(de: mensagem, inscritos: ["s1", "s2"]),
                           ["s1", "s2"], "\(mensagem)")
        }
    }

    /// Preencher com os membros do enum descobertos pelo grep acima.
    private var outrosTiposDeMensagem: [WSMessage] { [.snapshot([])] }
}
````

Ajuste os construtores de `Session` e os casos de `WSMessage` ao que existe de fato
(`Models.swift`): se `Session.init` exigir mais parâmetros, passe-os — **não** relaxe o
tipo nem crie um init de conveniência só para o teste.

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/LiveHubTests -quiet`
Expected: FAIL — `cannot find 'LiveHubRouting' in scope`.

- [ ] **Step 3: Implementar `LiveHub.swift`**

````swift
import Foundation

/// Para quem vai cada mensagem do `/ws`. Função pura, separada da conexão, para
/// ter teste sem hub no ar.
enum LiveHubRouting {
    static func destinatarios(de mensagem: WSMessage, inscritos: [String]) -> [String] {
        switch mensagem {
        case .sessionUpdated(let s):
            return inscritos.filter { $0 == s.id }
        case .outputChunk(let sessionID, _, _):
            return inscritos.filter { $0 == sessionID }
        default:
            // Snapshot e qualquer tipo novo vão para todos; quem recebe filtra.
            // Descartar o desconhecido aqui criaria um bug sem sintoma.
            return inscritos
        }
    }
}

/// Uma conexão ao `/ws` para todas as abas abertas.
///
/// Antes de 08/2026 cada `SessionDetailViewModel` abria a sua e filtrava do lado
/// do cliente. Com o teto de 6 painéis montados (D3) isso viraram seis conexões
/// fazendo o mesmo trabalho, cada uma com backoff próprio. O hub aqui recebe uma
/// vez e distribui — a filtragem que estava em cada ViewModel virou
/// `LiveHubRouting`, testável.
@MainActor
final class LiveHub: ObservableObject {
    private let api: CutuqueAPI
    private var inscritos: [String: AsyncStream<WSMessage>.Continuation] = [:]
    private var upstream: Task<Void, Never>?

    init(api: CutuqueAPI) { self.api = api }

    /// Assina as mensagens de uma sessão. A conexão sobe na primeira assinatura
    /// e desce quando a última sai — nada de socket aberto sem ninguém ouvindo.
    func inscrever(sessionID: String) -> AsyncStream<WSMessage> {
        AsyncStream { continuation in
            inscritos[sessionID] = continuation
            continuation.onTermination = { [weak self] _ in
                Task { @MainActor in self?.cancelar(sessionID: sessionID) }
            }
            garantirConexao()
        }
    }

    func cancelar(sessionID: String) {
        inscritos[sessionID]?.finish()
        inscritos[sessionID] = nil
        if inscritos.isEmpty {
            upstream?.cancel()
            upstream = nil
        }
    }

    private func garantirConexao() {
        guard upstream == nil else { return }
        upstream = Task { [weak self] in
            guard let stream = self?.api.liveUpdates() else { return }
            for await mensagem in stream {
                guard let self else { break }
                let destinos = LiveHubRouting.destinatarios(de: mensagem,
                                                           inscritos: Array(self.inscritos.keys))
                for id in destinos { self.inscritos[id]?.yield(mensagem) }
            }
        }
    }
}
````

- [ ] **Step 4: Rodar e ver passar**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -only-testing:CutuqueAppTests/LiveHubTests -quiet`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/LiveHub.swift app/CutuqueAppTests/LiveHubTests.swift
git commit -m "feat(app): LiveHub distribui uma conexao ws para varias abas"
```

### Task H2: o detalhe da sessão passa a usar o hub

**Files:**
- Modify: `app/CutuqueApp/SessionDetailView.swift:56-92` (`startLiveUpdates` e `stop`)
- Modify: `app/CutuqueApp/CutuqueApp.swift` (injetar o `LiveHub`)

**Interfaces:**
- Consumes: `LiveHub.inscrever(sessionID:)` (H1).
- Produces: nada — troca de fonte, mesmo comportamento.

- [ ] **Step 1: Trocar a fonte do stream**

`startLiveUpdates` deixa de chamar `api.liveUpdates()` e passa a chamar
`hub.inscrever(sessionID: session.id)`. **O `switch` de dentro do loop não muda** — as
guardas `where updated.id == self.session.id` ficam onde estão. Elas viraram redundantes
para as duas mensagens que o roteador já filtra, e é assim que se quer: a defesa em
profundidade custa uma comparação de string e protege de um erro de roteamento.

`stop()` passa a chamar também `hub.cancelar(sessionID: session.id)` — é o que derruba a
conexão quando a última aba fecha.

O `[weak self]` e o desembrulho a cada iteração **continuam**: o comentário de `:59-62`
documenta um vazamento real encontrado em review (_"evita ciclo self→task→self que
vazaria a conexão WS — review F2, achado #2"_). Preserve o comentário e a data dele.

- [ ] **Step 2: Injetar o hub**

Em `CutuqueApp.swift`, criar o `LiveHub(api:)` junto do `CutuqueAPI` e passá-lo por
`.environmentObject`. O `SessionDetailViewModel` recebe o hub no init, do mesmo jeito que
já recebe a `api`.

- [ ] **Step 3: Rodar a suíte**

Run: `cd app && xcodegen generate && xcodebuild test -project CutuqueApp.xcodeproj -scheme CutuqueApp -destination 'platform=iOS Simulator,name=Cutuque iPad 13' -quiet`
Expected: PASS.

- [ ] **Step 4: Verificar que é UMA conexão**

Com três abas de chat abertas, no hub:

```bash
ssh macmini 'lsof -i :8787 | grep -c ESTABLISHED'
```

Antes desta task: uma por aba (mais a da lista). Depois: uma só. Se o número não cair,
alguma view ainda chama `api.liveUpdates()` direto — procure com
`grep -rn "liveUpdates()" app/CutuqueApp` e confira cada chamada (a lista de sessões tem
a sua, e ela pode continuar; o que não pode é uma por painel).

- [ ] **Step 5: Commit**

```bash
cd /Users/vanessa/Desktop/coding/personal/cutuque
git add app/CutuqueApp/SessionDetailView.swift app/CutuqueApp/CutuqueApp.swift
git commit -m "refactor(app): detalhe da sessao consome o LiveHub"
```

---

## Testes: o que cada suíte cobre

| suíte | task | o que garante |
|---|---|---|
| `tmux_script_test.go` + `tmux_screens_test.go` | A1 | o harness `estadoDaTela` e as telas reais de 12/08 como fixture |
| `tmux_state_opencode_test.go` | A2 | `esc interrupt` (sem "to"), e a tela ociosa com `· 3.2s` **não** virando `running` |
| `tmux_state_codex_test.go` | A3, A4 | portão de confiança → `waiting`; `• Working (29s • esc to interrupt)` → `running` |
| `tmux_shell_test.go` | A5 | pane sem agente sai marcado como shell, com estado vazio |
| `tmux_test.go` | B1 | `Kind` atravessa o parse e o `TmuxPaneAsDiscovered` |
| `tmux_new_test.go` | C1 | validação de nome/pasta, montagem do comando, `parseNovaSessao` |
| `launch_test.go` | C4 | a rota: 200, 400 de nome inválido, 404 de máquina, 502 de falha |
| `TerminalPaneStateTests` | D1 | os três estados e a `TerminalResizeKey` |
| `TerminalCenaLogicTests` | D3 | background devolve, active reaplica, `.inactive` não |
| `LivePaneLogicTests` | E1 | agrupamento por grupo, servidores por máquina, rótulo de encerrar |
| `NomeTmuxTests` | F1 | alfabeto e filtragem enquanto digita |
| `NovoTerminalFormTests` | F2, F3 | `podeCriar`, sugestões de grupo, `kind` tolerante |
| `OpenTabsTests` | G1–G4 | aba de passagem, foco sem duplicar, teto de 6, fixar/fechar, persistência, aba morta |
| `NavigationStateTests` | G7 | duas colunas em qualquer orientação |
| `LiveHubTests` | H1 | roteamento, incluindo mensagem desconhecida indo para todos |

**O que nenhum teste cobre, e por isso tem passo de verificação manual:** a largura de
verdade no tmux (D2 Step 5, G6 Step 6), o `resize-window` chegando na máquina certa, a
criação de ponta a ponta (F3 Step 6), e a contagem de conexões (H2 Step 4). Todos os
quatro são "o outro lado respondeu como se esperava", e o outro lado é uma máquina de
verdade.

## Verificação final, com tudo mergeado

- [ ] `cd hub && go test ./...` — verde
- [ ] `cd app && xcodebuild test ... -destination 'platform=iOS Simulator,name=Cutuque iPad 13'` — verde, e a contagem de testes **subiu** em relação aos 132 de `versao-ipad`
- [ ] Hub redeployado no macmini, e a lista ao vivo mostrando bolinha de estado em sessão de **codex** (era o sintoma que abriu a chain A)
- [ ] iPad: abrir sessão de três grupos diferentes em abas, trocar entre elas, fechar uma, e conferir no Mac que só as vivas estão em `window-size manual`
- [ ] iPad: criar um terminal vazio num grupo que não existia e ver a sessão aparecer na lista, marcada como shell
- [ ] iPhone: nada mudou — a pilha de navegação, o `✕` e a lista continuam como estavam (nenhuma task desta lista mexeu no caminho do iPhone de propósito)










