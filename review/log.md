# Log de Revisões — Cutuque

Append-only. Entrada mais recente no topo.

## 2026-07-06 — adapter do Codex (`codex exec --json`) + extração da plataforma compartilhada `adapter/agent`
**Risco:** R2 (Alto — nova integração de agente que compartilha o executor de comandos remoto via SSH com o Claude Code (superfície de RCE já auditada, SEC-101), e generaliza `Handle`/`Runner` para os dois agentes. Sem RCE novo: o caminho SSH do Codex reusa corretamente o idioma seguro do SEC-101. Mas a generalização expôs uma diferença estrutural entre os dois agentes — Claude é bidirecional (stdin sempre aberto), Codex é one-shot (stdin sempre `nil`) — que o `Launcher` compartilhado não modela, causando um panic de nil-pointer alcançável por uso normal do app, e um caminho de retomada (`resume`) sem timeout que já era frágil para o Claude e vira rotina para o Codex.)
**Tipo:** T1 (nova funcionalidade: suporte ao Codex como agente) + T3 (refactor: extração de `hub/internal/adapter/agent` como plataforma compartilhada de execução, reusada por `claudecode` via type aliases)
**Escopo:** hub: `adapter/agent/{handle.go, runner.go, target.go}` (novos), `adapter/codex/{target.go, parser.go, transcript.go, discover.go}` (novos), `adapter/claudecode/target.go` (assinatura de `Start` ganhou `sandbox`/`prompt`; tipos agora são aliases de `agent.*`), `launcher/launcher.go` (roteamento agent-aware, `map[machine]map[agent]Target`), `session/session.go` (campo `Agent` em `Discovered`), `cmd/hub/main.go` (`buildTargets`/`agentMap`, wiring de `codex.NewLocalTarget`/`NewSSHTarget`). Commits `646fb21^..HEAD`.

### Bloqueantes
1. **issue (correctness/concurrency, blocking, maior severidade):** `Launcher.SendText` (`hub/internal/launcher/launcher.go:632-653`) e `Reply` (linhas 655-668) chamam `h.SendUserMessage(text)` → `Handle.WriteJSON` (`hub/internal/adapter/agent/handle.go:121-131`, linha 129: `h.Stdin.Write(b)`) em QUALQUER handle presente em `l.handles[id]` ("vivo"), sem checar se o agente daquele handle aceita input pelo stdin. Para o Codex, TODO `Handle` nasce com `Stdin = nil` (`hub/internal/adapter/codex/target.go:144-157`, `startCodex`, comentário explícito: "Codex é one-shot... stdin fechado evita que ele pendure esperando input") — `h.Stdin.Write` nessa condição é um nil pointer dereference, panic em runtime.

   Isso NÃO é uma corrida rara de janela estreita: uma sessão Codex fica com handle "vivo" em `l.handles[id]` durante TODO o intervalo entre `thread.started` e `turn.completed` — ou seja, o tempo inteiro de inferência de cada turno (tipicamente vários segundos). E o app (`app/CutuqueApp/SessionDetailView.swift:352-358`, `isLive`/`canSend`) habilita ativamente o envio de texto e as respostas rápidas sempre que `state == .running || state == .needsYou`, sem NENHUMA distinção por agente — o placeholder do campo de texto inclusive convida a digitar ("Responda ao agente…") exatamente nesse estado. Ou seja: sempre que a usuária manda uma segunda mensagem ou toca um quick-reply enquanto um turno do Codex ainda está rodando (cenário absolutamente normal de uso — "deixa eu adicionar mais um detalhe" enquanto o agente ainda está pensando), a request derruba com panic. `Reply` tem o mesmo problema por outra porta: como o Codex nunca preenche `Session.Pane` (não integra com tmux), `Reply` sempre cai em `SendText` para sessões Codex, nunca em `TmuxSend`.

   Efeito para a usuária: o eco da mensagem (`event.Event{Kind: KindUser, Data: text}`) já é aplicado ANTES da escrita falhar (linha ~640, antes do `SendUserMessage`) — a mensagem aparece na conversa como "enviada" mesmo sendo descartada sem nunca chegar ao Codex. `net/http` recupera o panic por conexão (não derruba o processo do hub), mas a request HTTP aborta sem corpo JSON estruturado — o app recebe um erro de rede genérico em vez do 409 `no_live_session` que **já existe** para exatamente este cenário: `launcher.ErrNoHandle` (`launcher.go:37`) já está definido, já mapeado para 409 em `InputHandler` (`hub/internal/server/launch.go:529`), e já tem teste (`launch_test.go:276`) — só nunca é retornado por `SendText`, em nenhum branch.

   Nenhum teste em `launcher_test.go` cobre isso: o fake `scriptTarget` usado em todos os testes de `SendText`/`resume` sempre cria um `Handle` com `Stdin` real (`io.Pipe`), simulando só o Claude.

   - **Por que importa:** contradiz a premissa com que esta própria revisão foi pedida ("confirmar que o launcher nunca chama `SendUserMessage` num handle Codex") — a resposta é que chama, e é alcançável por uso normal do app, não uma corrida de laboratório. É uma falha de disponibilidade/correção 100% determinística sempre que a segunda condição de corrida (segunda mensagem durante turno em andamento) ocorrer, e ela ocorre com frequência em uso real.
   - **Sugestão concreta:** dar ao `Handle` (ou ao `Target`, já que é uma propriedade do agente, não da instância) uma forma explícita de expressar a capacidade, ex. `func (h *Handle) AcceptsInput() bool { return h.Stdin != nil }`. `SendText` checa essa capacidade ANTES de chamar `SendUserMessage`; se `false`, retorna `launcher.ErrNoHandle` (o mapeamento HTTP 409 já existe, é só passar a ser alcançado). Adicionalmente: não aplicar o eco da mensagem do usuário antes de confirmar que ela tem como ser entregue (ou aplicar só depois que `SendText` decidir o caminho real — stdin vivo ou `resume`). Regressão: um fake `Target` cujo `Handle.Stdin` é `nil` (espelhando `codex.startCodex`) em `launcher_test.go`, confirmando que `SendText` num handle "vivo" desse tipo devolve `ErrNoHandle` em vez de panicar.
   - `[→ security.md#SEC-107]`

2. **issue (correctness, blocking):** `Launcher.resume` (`hub/internal/launcher/launcher.go:670-711`) — o caminho usado por `SendText` quando NÃO há handle vivo, ou seja, para o Codex, praticamente TODA mensagem além da primeira do turno (o Codex é one-shot: cada resposta é um processo novo que nasce, roda, e morre) — não tem timeout nem sinal de falha quando o processo spawnado morre sem produzir NENHUMA saída, ao contrário de `Launch` (que tem `launchTimeout = 20s` + `ErrLaunchTimeout`). `resume` ecoa a mensagem do usuário e retorna 200 imediatamente, sem esperar por confirmação de que o processo vai de fato responder.

   `agent.Runner.Run` (`hub/internal/adapter/agent/runner.go:104-107`) só sintetiza um `Errored` no EOF quando `sessionID != ""` — isto é, só se pelo menos um evento com id (tipicamente `thread.started`) chegou a ser visto no stream. Se o processo do Codex sair ANTES de imprimir qualquer linha JSON (cenário plausível via SSH: binário `codex` fora do PATH do shell não-interativo, erro de auth/quota impresso só no stderr, crash imediato), nenhum evento é aplicado — a sessão fica congelada no estado anterior (tipicamente `done`), sem nenhum sinal de erro, e a mensagem da usuária fica "enviada" para sempre sem resposta nem erro visível.

   - **Por que importa:** como o Codex depende de `resume` para praticamente toda continuação de conversa (diferente do Claude, que só usa `resume` quando a sessão já encerrou de vez), esta é uma fragilidade pré-existente que passa de caso extremo raro para caminho crítico rotineiro nesta leva. Confirmado também que `SetRemoteCodexCmd` nunca é chamado a partir da config (`cmd/hub/main.go`, `buildTargets`/`agentMap` — só `SetRemoteClaudeCmd` é wired a partir do formato de 3 campos de `CUTUQUE_SSH_TARGETS`), o que torna o cenário "binário fora do PATH remoto" ainda mais fácil de acontecer sem a operadora perceber que configurou algo errado.
   - **Sugestão concreta:** dar a `resume` proteção equivalente à de `Launch` — um timeout curto esperando algum sinal de vida do processo (não precisa ser `session_started` novo, já que o id é reusado; basta o primeiro evento do stream OU um teto de tempo), aplicando `Errored` explícito na sessão se estourar, em vez de deixá-la congelada. Alternativa mais barata: propagar ao `Runner` (via `Meta`) que a chamada é um resume com id já conhecido, e sintetizar `Errored` no EOF mesmo com `sessionID == ""` nesse caso. Regressão: fixture com `Target.Start` cujo processo sai imediatamente sem stdout, verificando que a sessão retomada termina em `error` dentro de um teto razoável, não fica congelada.
   - `[→ security.md#SEC-108]`

### Non-bloqueantes
- suggestion (observability, if-minor): `Launcher.Discover` (`launcher.go:237-273`) só retorna erro se TODOS os agentes falharem numa máquina (`anyOK == false`); um único agente persistentemente quebrado numa máquina (ex.: Codex sem `python3`/permissão errada) degrada silenciosamente para sempre, sem log. É uma escolha de UX razoável (um agente quebrado não deveria esconder o resultado do outro) — mas vale um `slog.Warn` por agente que falhar, para dar sinal operacional sem mudar o comportamento do merge.
- suggestion (testability): `codex/discover.go` e `codex/transcript.go` (scripts Python embutidos, parseando `~/.codex/sessions/*/*/*/rollout-*.jsonl`) não têm nenhum teste dedicado (`discover_test.go`/`transcript_test.go` não existem para o pacote `codex`) — inconsistente com o precedente do próprio projeto: um gap análogo em `claudecode/discover.go` já foi tratado como BLOQUEANTE numa revisão anterior (entrada de 2026-07-03, "feature de descoberta") e corrigido com testes de fixture/fake ssh. Vale o mesmo tratamento aqui, especialmente para a lógica de dedup de mensagem entre `event_msg`/`response_item` que o parser do transcript reivindica fazer.
- nitpick (doc): comentário de `startCodex` sobre `Stdin = nil` está correto e bem documentado — só faltou o comentário equivalente do lado do `Launcher`/`Handle` deixando explícito que nem todo Handle aceita `SendUserMessage`; teria sido o sinal que faltou para pegar o bloqueante #1 antes.

### Confirmações positivas (respondendo aos pontos específicos pedidos)
- **Ponto 1 (escape SSH do Codex):** confirmado SEGURO. `remoteCodexCommand`/`codexArgs` (`codex/target.go`) reusam o MESMO idioma estrutural do fix do SEC-101 (`bash -lc 'exec "$0" "$@"' <argv...>`, cada arg individualmente single-quoted via `agent.SingleQuote`, um único nível de shell) — literalmente o mesmo padrão do `claudecode`, agora extraído e compartilhado. `resumeID` que chega ali é sempre `""`, um id já validado por `sessionIDPattern` (via `Adopt`), ou um `thread_id` gerado pelo próprio processo `codex` — nunca input direto de cliente sem validação prévia.
- **Ponto 2 (allowlist de sandbox/model/effort):** confirmado. `sandbox`/`effort` só entram em `-c chave="valor"` depois de passar por allowlists estritas (`validSandbox`/`validEffort`, exact-match); `model` passa por `modelNamePattern` (regex) antes de virar `-m <model>` — e mesmo que o regex falhasse, o valor ainda seria isolado corretamente pelo quoting estrutural por-argumento (defesa em profundidade real, não só decorativa).
- **Ponto 3 (concorrência: `Handle.Close`/`SendUserMessage` em handle Codex):** `Close()` confirmado nil-safe e idempotente (`sync.Once` + checagem `if h.Stdin != nil` antes de fechar). Mas a premissa de que o launcher "nunca chama `SendUserMessage` num handle Codex" está INCORRETA — ver bloqueante #1.
- **Ponto 4 (EOF/terminal do `Runner` genérico):** confirmado que a lógica literal perguntada (`turn.completed → Finished` sintetizando corretamente o fim) está correta e não deixa uma sessão presa em "running" NESSE mecanismo específico. Mas existe um gap adjacente na mesma função quando `sessionID` nunca chega a ser definido — ver bloqueante #2.
- **Ponto 5 (parser do Codex — unknown items, `turn.failed`, dedup):** confirmado correto para o caminho de stream ao vivo (`codex/parser.go`): itens desconhecidos geram um resumo genérico visível (não são descartados silenciosamente); `turn.failed`/`error`/`thread.error`/`stream.error` mapeiam para `Errored` com texto extraído via `errText()`; bem coberto por `parser_test.go`/`live_smoke_test.go`. O dedup entre `event_msg`/`response_item` (transcript.go) vs. `item.completed` (parser ao vivo) é arquiteturalmente plausível pela leitura do código, mas sem nenhum teste dedicado — ver suggestion non-blocking acima.
- **Ponto 6 (merge do `Discover`):** a semântica "só erra se TODOS os agentes falharem" é aceitável como escolha de UX, mas fica sem observabilidade quando degrada parcialmente — ver suggestion non-blocking acima.

### Padrões detectados
- candidato NOVO (1x, abaixo do critério de promoção de 2x): "abstração-compartilhada-esconde-capacidade-heterogênea-do-backend" — a plataforma `agent.Handle`/`Runner` foi generalizada para dois agentes com modelos de execução fundamentalmente diferentes (bidirecional vs. one-shot) sem expor essa diferença como uma capacidade checável pelo código que decide se pode escrever no handle; o código chamador (`Launcher.SendText`) assume a capacidade do caso mais permissivo (Claude) para todo handle "vivo". Se esse tipo de generalização aparecer de novo (ex. um terceiro agente com outro modelo de execução), promover a entrada completa aqui.
- candidato NOVO (1x, abaixo do critério de promoção de 2x): "caminho-secundário-sem-a-mesma-proteção-do-caminho-primário" — `resume()` nunca teve o timeout/sinal-de-falha que `Launch()` tem, mas isso era um caso extremo raro enquanto todo agente era bidirecional (Claude quase sempre reusa o handle vivo); a chegada de um agente one-shot faz o caminho "secundário" virar rotina sem que a proteção tenha acompanhado.

### Especialistas envolvidos
- Nenhum necessário para os dois fixes (são locais a `launcher.go`/`agent/handle.go`, uma checagem de capacidade + um timeout). Se a solução do bloqueante #1 evoluir para um modelo mais rico de "capacidades do agente" no `Target`/`Handle` (além de só stdin — ex. suporte a cancelamento de turno em andamento, que o Codex também não tem hoje), vale trazer **Marcus** para desenhar essa extensão do contrato de forma que sirva a um terceiro agente futuro sem precisar generalizar apressadamente de novo.

## 2026-07-03 — classificação Notification ociosa vs. bloqueio, persistência de devices, `Resolve`/swipe-to-conclude
**Risco:** R2 (Alto — mexe na semântica do state machine para o hook `Notification`, na atomicidade de criação de sessão do Engine, e introduz persistência em disco para os device tokens de push + um novo endpoint mutável `POST /sessions/{id}/resolve`. Sem RCE novo. Confirma que os 3 bloqueantes da leva anterior — SEC-103, `EnsureRegistered` não-atômico, e hijack de pane same-machine — foram corrigidos; mas a correção do 3º introduziu uma lacuna nova (multi-máquina) e o padrão do 2º reaparece num caminho irmão não coberto pelo fix anterior.)
**Tipo:** T2 (correção de bug: Notification ociosa virando needs_you incorretamente) + T1 (feature nova: `Resolve`/swipe "Concluir", persistência de devices em disco) + T3 (doc/comentário sobre a semântica de push)
**Escopo:** hub: `server/hooks.go` (`notificationBlocks`), `notifier/notifier.go` (comentário sobre push por done/error + gate `!s.External` no re-cutucão), `devices/store.go` (persistência JSON em disco, novo), `engine/engine.go` (`Reclaim` em `ensureRunning`), `registry/registry.go` (`AddIfAllowed`, `Reclaim`, eviction em `SetPane`), `launcher/launcher.go` (`Resolve`), `server/launch.go`+`server.go` (`ResolveHandler`, rota), `cmd/hub/main.go` (wiring de `CUTUQUE_DEVICES_PATH`). app: `SessionListView.swift` (dedup Ao vivo/Precisa de você/Sessões, swipe "Concluir"), `Models.swift` (`pane`/`external`/`cwd`), `APIClient.swift` (`resolve`), `TerminalMirrorView.swift` (reorder trivial de tecla).

### Bloqueantes
1. **issue (correctness, blocking):** o comentário novo em `notifier.go:196-205` afirma "um push por tarefa concluída, não por turno interno" — essa invariante é FALSA para qualquer sessão usada interativamente (exatamente o caso de sessões `External`/tmux, que é o alvo desta correção). Trace do ciclo completo: `UserPromptSubmit→running` (sem push) → ... → `Stop→done` (push #1, correto) → `Notification` ocioso 60s depois → `Finished` de novo, mas `Apply` já faz no-op porque `cur.State == target` (`engine.go:68-69`) — sem push, correto, é o que os testes novos cobrem. MAS: se a usuária mandar uma NOVA mensagem na mesma sessão tmux (`UserPromptSubmit→running`, transição real, `n.states[id]` grava `running`) e o Claude responder (`Stop→done`, transição real de `running` para `done`) — isso passa no dedup de `handle()` (`prev=running != done`) e dispara PUSH #2. Cada turno subsequente da MESMA conversa interativa repete esse ciclo e gera OUTRO push "concluiu". Para uma sessão de pareamento longa no terminal (o caso de uso que `External` existe para servir) isso é um push por resposta do Claude, não um push pela tarefa toda — exatamente o "risco real de push repetido" que a revisão pediu para escrutinar, e o oposto do que o comentário reivindica.
   - **Por que importa:** é o comportamento que esta leva inteira se propõe a consertar (ruído de notificação em sessões tmux/externas); ele permanece para o padrão de uso mais comum de uma sessão `External` (conversa interativa de ida-e-volta), só que agora documentado como resolvido — o que pode impedir alguém de notar/reabrir o problema depois.
   - **Sugestão concreta:** não deixar o `Stop` isolado disparar PUSH diretamente para sessões que continuam vivas para responder; usar a Notification ociosa ("waiting for your input", já classificada corretamente por `notificationBlocks`) como o sinal real de "ninguém está mais dirigindo isso agora", e tratar `Stop` como só uma atualização de estado da UI (pode virar `done` no Registry imediatamente, ok) sem necessariamente dela sozinha disparar a chamada de `buildPush`+`fanout`. Alternativa mais simples e menos invasiva: um cooldown mínimo por sessão em `Notifier.handle` (ex.: não repetir push de `done` para o mesmo id dentro de N segundos do último `done` empurrado), que amortece o caso de conversa acelerada sem mexer na máquina de estados.
   - **Observação relacionada (não confirmada com teste, só leitura de código/documentação pública do SDK):** o mesmo padrão de `Finished` por `"result"` de CADA turno (`parser.go: resultEvent`) provavelmente também afeta sessões NÃO-`External` mantidas vivas via `SendText` no mesmo handle (`launcher.go`) — o `Runner.Run` não distingue "resultado de turno" de "resultado de tarefa" nesse caminho também. Vale um teste de fixture com duas linhas `result` em sequência no mesmo stream para confirmar antes de decidir o escopo do fix.

2. **issue (correctness/concurrency, blocking — 5ª ocorrência do padrão `reivindicação-não-atômica-de-recurso-compartilhado`):** o fix do bloqueante #2 da leva anterior trocou `EnsureRegistered` para usar `AddIfAllowed` (atômico) — correto — mas `Engine.ensureRunning` (`engine.go:86-102`), o OUTRO caminho que também cria sessão do zero (usado tanto pelo `Runner` quanto pelo dispatch de hook `SessionStart`/`UserPromptSubmit`), continua fazendo `Get` seguido de `Add` bruto como duas aquisições de lock separadas, em vez de `AddIfAbsent` (já disponível e usado em `Adopt`/`AddIfAllowed`). Cenário: hook e Runner recebem o `SessionStarted` de uma sessão NUNCA vista por nenhum dos dois, quase simultaneamente (exatamente a premissa do SEC-103 original). Se o `Get` de um dos dois lados retornar `exists=false` e, ANTES do seu `Add` bruto rodar, o outro lado tiver inserido E JÁ AVANÇADO o estado (ex.: o hook processou seu próprio `EnsureRegistered`+`Apply(SessionStarted)` e, em seguida, um `Notification` de permissão real chegou primeiro, levando a sessão a `needs_you` com `PendingPrompt` preenchido) — o `Add` bruto do outro lado SUBSTITUI o registro inteiro por um `session.Session{}` novo em `StateRunning`, apagando silenciosamente o `needs_you`/`PendingPrompt`/`Pane` que já existiam. Diferente do SEC-103 original (sessão fica visivelmente travada em `needs_you` sem botão), aqui o resultado é PIOR em silêncio: a sessão volta a aparecer como `running`, sem badge `needs_you` nenhum, enquanto o processo `claude` real continua bloqueado esperando resposta no stdin — nenhum sinal visível de que algo está errado. A janela é mais estreita que a do SEC-103 original (exige 3 eventos entrelaçados, não 2), mas o dano pior compensa a menor probabilidade.
   - **Por que importa:** reabre a mesma classe de corrida que acabou de ser fechada em `EnsureRegistered`, por um caminho irmão que faz exatamente a mesma coisa (criar sessão do zero) sem a mesma primitiva atômica.
   - **Sugestão concreta:** trocar o branch de criação de `ensureRunning` para usar `e.reg.AddIfAbsent(novaSessao)`; se `added==false`, cair no mesmo bloco de reconciliação (`Reclaim`/`UpdateState`/`SetPane`) já existente para o caso "já existia" — mesmo padrão que fechou o bloqueante #2 anterior.
   - `[→ review/patterns.md#reivindicação-não-atômica-de-recurso-compartilhado]` `[→ security.md#SEC-106]`
   - **Status: RESOLVIDO (confirmado 2026-07-06)** — `ensureRunning` já usa `AddIfAbsent`; ver nota em `security.md#SEC-106`.

3. **issue (correctness, blocking):** a correção do hijack de pane same-machine (bloqueante #3 da leva anterior, `Registry.SetPane`, `registry.go:189-230`) fechou o caso reportado mas introduziu uma lacuna nova: o loop de eviction (`registry.go:206-217`, `if oid != id && os.Pane == pane`) compara SÓ a string do pane, sem checar `os.Machine == s.Machine`. O alvo composto (`"<socket>\t<pane>"`, montado em `hookPayload.paneTarget()`, `hooks.go:32-43`) não inclui a máquina em NENHUM lugar do código (hub ou app). Como tmux usa por padrão o socket "default" e a primeira pane sempre é "%0", duas máquinas diferentes rodando uma sessão tmux default cada uma colidem na MESMA string de pane com bastante probabilidade na prática (não é um edge case exótico). Consequência no hub: uma sessão `needs_you` na Máquina A pode ser evictada (pane limpo, estado forçado para `done`) por uma sessão nova que só coincide na string de pane na Máquina B — dado de OUTRO usuário/máquina interferindo silenciosamente no estado de uma sessão não relacionada. No app, o mesmo problema aparece em `SessionListView.swift`: `livePaneIDs`/`needsYouPaneIDs` (linhas 202, 204) são `Set<String>` construídos a partir de TODAS as máquinas configuradas (`targets`, poll de `/machines/{m}/live`) sem incluir `machine` na chave, e `liveState(_:)` (linha 435-436) faz `model.sessions.first(where: { $0.tmuxTarget == entry.id })` — sem filtrar por máquina, então uma linha "Ao vivo" da Máquina B pode herdar o estado/dedup de uma sessão da Máquina A que colide na string de pane.
   - **Por que importa:** o mesmo bug de "roubo de identidade de sessão pela pane" que a leva anterior corrigiu para o caso same-machine reaparece para o caso cross-machine, exatamente a configuração multi-máquina que o Cutuque já suporta nativamente (`Launcher.targets`/`SSHTarget`).
   - **Sugestão concreta:** patch mínimo imediato — adicionar `&& os.Machine == s.Machine` à condição de eviction em `SetPane`. Fix durável — incluir `machine` na própria chave composta (`"<machine>\t<socket>\t<pane>"`) em TODO lugar onde ela é construída/comparada (`hooks.go: paneTarget()`, `registry.go: SetPane`, e no app: `LiveEntry`/`tmuxTarget`/dedup sets), para que qualquer código futuro que compare panes não precise lembrar de checar `machine` separadamente — a ambiguidade fica embutida na string hoje, o que é frágil.
   - `[→ security.md#SEC-104]`

4. **issue (correctness/concurrency, blocking):** `devices/store.go: persist()` (linhas 78-91) é chamado FORA do lock de mutação em `Upsert`/`Remove`, e nada serializa chamadas concorrentes de `persist()` entre si — o `path+".tmp"` é um caminho COMPARTILHADO entre todas as chamadas. Duas goroutines concorrentes (ex.: um `Upsert` do app reabrindo, concorrente com um `Remove` disparado por um 410 Unregistered no fanout de push) podem interlaçar leitura de `List()` + `json.Marshal` + `os.WriteFile(tmp,...)` + `os.Rename` de forma que a chamada que TERMINA por último vence, independente de qual mutação era logicamente mais recente — o arquivo em disco pode silenciosamente reverter para um snapshot mais antigo do que o estado em memória (`byToken`, que É protegido corretamente por `sync.RWMutex` e nunca fica inconsistente sozinho). Não é pego por `go test -race` (é uma corrida de I/O de arquivo do SO entre duas chamadas de `os.WriteFile`/`os.Rename`, invisível ao detector de raça do Go, que só instrumenta memória gerenciada pelo runtime) — coerente com o fato de a suíte inteira passar com `-race` sem detectar isto.
   - **Por que importa:** um device removido (410) pode "voltar" no disco se seu `Remove` perder a corrida contra um `Upsert` mais antigo que ainda não tinha terminado de persistir — na prática, reenviar push para um token morto (inofensivo pro usuário, mas desperdiça budget de APNs e pode gerar ruído de log) ou, no sentido oposto, perder um `Upsert` recém-chegado após restart do hub.
   - **Sugestão concreta:** um `persistMu sync.Mutex` dedicado, adquirido no INÍCIO de `persist()` e liberado no fim, cobrindo a sequência `List()`+`Marshal`+`WriteFile`+`Rename` inteira — o último a adquirir o lock sempre lê o `List()` mais fresco no momento em que roda, então o resultado final em disco sempre reflete o estado mais recente, não o mais lento a terminar.
   - `[→ review/patterns.md#persistência-em-disco-fora-da-seção-crítica]` `[→ security.md#SEC-105]`

### Non-bloqueantes
- suggestion (observability, if-minor): `persist()`/`load()` (`devices/store.go:54-91`) engolem TODOS os erros de I/O/marshal silenciosamente (`return` sem log) — inconsistente com o resto do hub (`notifier.go` loga falha de push com `slog.Warn`). Um disco cheio ou erro de permissão pararia a persistência sem nenhum sinal operacional. Vale ao menos um `slog.Warn` nos `return` de erro.
- suggestion (testability): nenhum teste novo de `devices/store_test.go` exercita `Upsert`/`Remove` concorrentes (`TestPersistenceRoundTrip`/`TestPersistenceRemove` são sequenciais) — um teste com N goroutines concorrentes + `-race` + verificação de que o conteúdo final em disco bate com o estado final em memória cobriria o achado #4 acima (e falharia hoje, sem o `persistMu`).
- suggestion (correctness, non-blocking, baixa severidade): `Launcher.Resolve` (`launcher.go:414-419`) força o estado para `done` incondicionalmente, sem checar se o estado atual ainda é `needsYou` — uma corrida estreita entre o swipe "Concluir" no app e uma transição legítima concorrente (ex.: novo prompt enviado no terminal levando a `running` quase no mesmo instante) pode fazer o `Resolve` sobrescrever esse `running` de volta para `done`. Considerar `Resolve` ser no-op (ou retornar um erro de conflito) se o estado atual não for `needsYou` no momento da chamada.
- nitpick (doc): `docs/09-configurar-hooks.md` está desatualizado — não documenta os campos `pane`/`tmux_socket`/`machine` que o forwarder de hook agora envia, nem a distinção Notification ociosa vs. bloqueio real. Vale atualizar junto, para quem configurar hooks numa máquina nova ter referência correta.

### Confirmações positivas (respondendo aos pontos específicos pedidos)
- **Semântica ocioso→done não engole pedido real (ponto d):** confirmado correto. `notificationBlocks` (`hooks.go:109-116`) só classifica como NÃO-bloqueio as substrings "waiting for your input"/"waiting for input"; qualquer outra mensagem — incluindo "Claude needs your permission...", qualquer prompt de aprovação de plano, e qualquer mensagem customizada/desconhecida — cai no default (bloqueia, vira `needs_you`). O default conservador é explícito e documentado no próprio comentário da função, e as duas mensagens reais do Claude Code (permissão vs. ociosidade) são tratadas de forma distinta e testada (`TestHookNotificationPermissionBlocks`, `TestHookIdleNotificationBecomesDone`, `TestHookIdleDoesNotResurrectDone`).
- **Edge cases de correlação pane→estado / roteamento no app (ponto c):** "sessão ainda não carregada" é tratado corretamente — `resolveDeepLink()` é re-executado via `.onChange(of: model.sessions)` (`SessionListView.swift:364`), então um push tocado antes do carregamento inicial da REST terminar resolve corretamente assim que as sessões chegam, sem ficar preso. "pane ausente" também é seguro — `tmuxTarget` retorna `nil` para pane vazio, e todo o dedup usa `compactMap`/`?? false`, então uma sessão sem pane nunca casa falsamente com nenhum Set de dedup. O dedup entre "Ao vivo"/"Precisa de você"/"Sessões" funciona corretamente dentro de UMA máquina (confirmado por trace de código) — mas quebra entre máquinas diferentes, ver bloqueante #3.
- **Concorrência da persistência (ponto b):** confirmado com defeito real — ver bloqueante #4. O padrão tmp+rename em si está correto (atômico contra o processo morrer no meio); o problema é a ausência de serialização ENTRE chamadas concorrentes de `persist()`, uma classe de bug diferente da que `go test -race` consegue pegar.
- **Raciocínio anti-spam do notifier (ponto a):** o comentário que afirma "um push por tarefa concluída, não por turno" está incorreto para sessões usadas interativamente — ver bloqueante #1 com o trace completo do ciclo `UserPromptSubmit→running→Stop→done` repetido.

### Padrões detectados
- `reivindicação-não-atômica-de-recurso-compartilhado` (5ª ocorrência — `Engine.ensureRunning`, criação de sessão nova) `[→ review/patterns.md]`
- candidato NOVO (1x, abaixo do critério de promoção de 2x): "chave-composta-sem-dimensão-de-desambiguação" — o alvo tmux composto (`socket\tpane`) omite `machine`, causando colisão cross-máquina tanto no hub (`SetPane`) quanto no app (dedup de seções). `[→ review/patterns.md]`
- candidato NOVO (1x, abaixo do critério de promoção de 2x): "persistência-em-disco-fora-da-seção-crítica" — mutação em memória atômica seguida de um efeito colateral em disco (write+rename) chamado FORA do lock e não serializado contra si mesmo. `[→ review/patterns.md]`

### Especialistas envolvidos
- **Marcus:** decidir o desenho correto da semântica turno-vs-tarefa no state machine (bloqueante #1) — é uma decisão de modelo de estado do backend, não um fix puramente local; e revisar se `ensureRunning`/`AddIfAbsent` (bloqueante #2) deveria virar o único padrão de criação de sessão em todo o Engine (unificar os dois caminhos de criação hoje existentes).
- **Dinah** (opcional): se a decisão for incluir `machine` na chave composta do pane (bloqueante #3) de forma abrangente, vale uma revisão de boundary/contrato entre hub e app para esse formato, já que toca o payload do hook, o Registry e o modelo do app simultaneamente.

## 2026-07-03 — conserto do "hook de aviso": pane composto (socket\tpane), dismissed-set, External no notifier/app
**Risco:** R2 (Alto — corrige a correlação sessão↔pane do tmux e introduz roteamento de terminal ao vivo por pane no app. Sem RCE novo: `tmux.go` mantém as defesas do SEC-101 mesmo estendendo para 4 operações novas sobre múltiplos servidores tmux. Mas duas corridas de concorrência podem travar permanentemente o gate de aprovação de uma sessão lançada pelo próprio hub, e uma lacuna de dedup pode desviar teclado/leitura para a sessão errada.)
**Tipo:** T2 (correção de bug: sessões fantasma / push infinito) + T3 (refactor: pane composto, dedup por pane no app)
**Escopo:** hub: `adapter/claudecode/tmux.go` (novo), `server/hooks.go`, `engine/engine.go`, `registry/registry.go`, `notifier/notifier.go`, `session/session.go`, `event/event.go`. app: `Models.swift`, `SessionListView.swift`, `SessionDetailView.swift`, `TerminalMirrorView.swift`.

### Bloqueantes
1. **issue (correctness/concurrency, blocking):** corrida entre o hook (`HookHandler`/`EnsureRegistered`, `hooks.go:113-125` + `engine.go:117-146`) e o `Runner` do próprio hub (`ensureRunning`, `engine.go:86-107`) para o MESMO `session_id`, quando o hub lança um `claude -p` LOCAL (o hook `~/.cutuque/hook.sh` dispara para "TODA sessão claude do Mac, inclusive as -p lançadas pelo hub" — premissa confirmada pelo próprio pedido de revisão). Se o hook processar seu POST HTTP (`SessionStart`/`UserPromptSubmit`) antes de o `Runner.Run` (`adapter/claudecode/runner.go:49-94`) processar a primeira linha `session_started` do stream-json (corrida de IPC vs. HTTP loopback, sem contrato de ordem entre as duas fontes), `EnsureRegistered` cria a sessão com `External: true` e `Title`/`Machine` vindos do hook (`niceNameFromCwd`, não o prompt real). Quando o `session_started` REAL chega depois, `ensureRunning` cai no branch `exists=true` (linha 103-106) — que só ajusta `State`/`Pane`, NUNCA reverte `External` para `false` nem corrige `Title`/`Machine`/`Agent`. Resultado permanente: uma sessão 100% controlada pelo hub (com `ControlID` pendente de aprovação real via stdin, guardado em `Launcher.pending`) fica marcada `External=true` para sempre. No app, `SessionDetailView.swift:181-187` (`permissionPrompt`) é gated a `!isExternal` — a sessão chega a `needs_you` SEM os botões aprovar/negar; como é processo `-p` headless (sem TTY), a usuária fica SEM NENHUM canal para responder, e o `claude` fica bloqueado no `control_request` indefinidamente. Nenhum teste cobre esta ordem de eventos (`engine_test.go`/`hooks_test.go` só testam cada caminho isoladamente).
   - **Por que importa:** quebra o invariante de segurança do docs/03 ("nunca aprovar às cegas") pelo lado oposto — a sessão não é aprovada às cegas, mas fica IMPOSSÍVEL de aprovar, travando um agente que pode estar no meio de uma operação.
   - **Sugestão concreta:** dar ao `Runner`/`Launcher` uma forma de "reclamar" a sessão como hub-owned de forma que sempre vença, independente da ordem: ou (a) adicionar uma marca de origem ao `event.Event` (ex. `Source: "runner"`) e fazer `ensureRunning` SEMPRE corrigir `Title`/`Machine`/`Agent`/`External=false` quando `Source=="runner"`, mesmo se a sessão já existir; ou (b) separar `ensureRunning` (só chamado pelo Launcher) de um `ensureRunningFromHook` dedicado, e nunca deixar o caminho do hook sobrescrever campos depois que o Launcher "reclamar" a sessão.
   - `[→ security.md#SEC-103]`
   - **Status: RESOLVIDO** — `Registry.Reclaim` foi introduzido e é chamado por `Engine.ensureRunning` sempre que `!ev.External && cur.External`, corrigindo `External`/`Title`/`Machine`/`Agent` a partir do evento autoritativo do Runner. Ver detalhe/ressalva em `security.md#SEC-103`.

2. **issue (correctness/concurrency, blocking — 4ª ocorrência do padrão `reivindicação-não-atômica-de-recurso-compartilhado`):** `Engine.EnsureRegistered` (`engine.go:117-146`) faz `Get` + `Dismissed` + `Add` como TRÊS aquisições de lock separadas do Registry, não uma seção crítica única — apesar de o Registry já expor `AddIfAbsent` (atômico, `registry.go:86-96`) para exatamente este propósito, já usado por `Launcher.Adopt` (`launcher.go:377`). Isso reabre a mesma classe de bug catalogada 3x no projeto (a última em `Adopt`, já corrigida com `AddIfAbsent`) por um caminho NOVO que essa correção não cobriu. Colisão concreta e demonstrável: `Launcher.Adopt` (`launcher.go:350-388`) chama `l.reg.Undismiss(id)` (linha 361) e só DEPOIS `l.reg.AddIfAbsent(s)` (linha 377) — duas operações de lock separadas, com a montagem do `session.Session{}` no meio. Se um hook concorrente para o MESMO `id` (plausível: a usuária está literalmente reabrindo/continuando uma sessão do Claude que ainda está viva no tmux, gerando eventos de hook, exatamente o cenário que "Continuar sessão do Mac" existe para servir) executar `EnsureRegistered` na janela entre o `Undismiss` e o `AddIfAbsent` do Adopt, o hook vence a corrida: `Get(id)`→false, `Dismissed(id)`→false (acabou de ser limpo pelo Undismiss), `Add(...)` insere uma versão hook-flavored (título de `niceNameFromCwd`, sem o `title` que a usuária escolheu ao adotar). Quando o `Adopt` chega no seu próprio `AddIfAbsent(s)`, encontra o id já existente (`added=false`) e **pula o `importTranscript`** (linha 378-380: `if !added { return existing, nil }`) — a sessão reaparece sem o histórico importado, e com metadados errados. É o mesmo efeito visível para a usuária (histórico "sumido") que motivou a correção anterior deste padrão, só que pela porta que ficou destravada.
   - **Por que importa:** o padrão já mordeu o projeto 3x antes; desta vez reabre um bug que JÁ tinha sido fechado (Adopt/importTranscript) através de um caminho de entrada novo introduzido por esta mesma feature.
   - **Sugestão concreta:** trocar o trio `Get`/`Dismissed`/`Add` de `EnsureRegistered` por uma única operação atômica no Registry, ex. `func (r *Registry) EnsureRegisteredIfNotDismissed(s session.Session) (added bool)` que faz a checagem de existência + dismissed + insert sob o MESMO lock (mesmo padrão de `AddIfAbsent`, só que também consultando `dismissed` dentro da seção crítica).
   - `[→ review/patterns.md#reivindicação-não-atômica-de-recurso-compartilhado]`
   - **Status: RESOLVIDO nesta leva** — `EnsureRegistered` (`engine.go:124-149`) agora usa `Registry.AddIfAllowed` (`registry.go:84-98`), que faz `dismissed`+existência+insert sob o MESMO lock. Confirmado via leitura de código; ver, porém, o achado #2 da entrada acima (`ensureRunning` reabre o mesmo padrão por um caminho irmão).

3. **issue (correctness, blocking):** dedup por pane no app (`SessionListView.swift`) não detecta um pane REUTILIZADO por uma sessão diferente. `Registry.SetPane`/`ensureRunning` gravam `Session.Pane` uma vez e nunca o limpam (`registry.go:142-156`: no-op se `pane==""`); não existe nenhuma reconciliação no hub que detecte "este pane agora pertence a outra sessão" (confirmado: nenhum mecanismo de cleanup/TTL/GC no hub — `grep` não encontrou nada). Cenário: sessão A trava em `needs_you` (ex.: processo morto com `kill -9`/Ctrl+C antes de o `Stop` hook disparar — comum quando a usuária desiste de um pedido de permissão assustador) e permanece nesse estado para sempre no registry, com `Pane = "sock\t%3"`. A usuária inicia um NOVO `claude` na MESMA pane %3 (fluxo normal de reuso de terminal) → hook registra a sessão B com o MESMO pane. Na lista (`SessionListView.swift:196-209`), `needsYou` (linha 197) não filtra por pane — a entrada STALE de A continua aparecendo em "Precisa de você" indefinidamente. Ao tocar nela, `needsYouRow` (linhas 416-431) abre `LiveDetailView`/`TerminalMirrorView` usando `target = session.pane` — que hoje é a pane de B, não de A. O app mostra o `pendingPrompt` STALE de A (ex.: "permitir rm -rf X?") mas ao tocar/digitar, a usuária interage com o terminal AO VIVO de B (capture-pane/send-keys operam só sobre a pane, sem verificar dono lógico) — risco real de digitar uma resposta pensando estar respondendo a A quando na verdade está indo para o processo de B.
   - **Por que importa:** pode levar a ação não intencional no processo errado (mesmo que a operação em si — send-keys — não seja privilegiada, o contexto do que a usuária pensa estar aprovando/respondendo está errado).
   - **Sugestão concreta:** no hub, quando `EnsureRegistered`/`ensureRunning`/hook `SetPane` atribuir um pane a uma sessão NOVA, verificar se alguma OUTRA sessão viva no registry já tem o MESMO `Pane` e, se tiver, transicioná-la para `done`/`error` (o pane mudou de dono, a sessão antiga não é mais alcançável por ali) antes de aceitar o novo dono. Mitigação mais barata no app: `needsYouRow` só oferecer o roteamento ao vivo se `session.tmuxTarget` também estiver em `livePaneIDs` (confirma que a pane está viva agora) — reduz, mas não elimina (não distingue "viva com B" de "viva com A").
   - **Status: RESOLVIDO nesta leva** — `Registry.SetPane` (`registry.go:189-230`) agora faz a eviction descrita na sugestão: ao mudar de pane, varre o registry por outra sessão com o mesmo pane, limpa o pane dela e a força para `done` se estava em `needs_you`. Confirmado via leitura de código + `TestSetPaneEvictsStaleSession`. Porém a comparação não inclui `machine` — ver bloqueante #3 da entrada acima (colisão cross-máquina).

### Non-bloqueantes
- suggestion (perf/memory, if-minor): `Registry.dismissed` (`registry.go:41`, `226`) cresce sem limite nem TTL — cada `Remove` adiciona uma entrada permanente, nunca removida (exceto `Undismiss` explícito). Para um hub de uso pessoal rodando por semanas/meses sem restart (ex. ZimaOS), isso é memória não-liberada, embora de baixo impacto prático (string curta por entrada). Vale um teto (LRU) ou expiração por tempo se o padrão de uso envolver deletar muitas sessões ao longo de meses.
- thought: `Registry.Remove` (`registry.go:220-233`) marca `dismissed[id]=true` incondicionalmente, mesmo quando a sessão não existia (`hadSession=false`) — não é usado hoje de forma perigosa, mas é uma pequena inconsistência com o padrão "atomicidade combinada" que o resto do arquivo persegue.
- nitpick (baixa severidade): `TmuxResizeHandler`/`TmuxResize` (`launch.go:67-94`, `tmux.go:209-224`) não clampam `cols`/`rows` — valores negativos caem no branch de "restaurar automático" (inofensivo), mas um valor absurdamente grande (ex. `999999999`) é repassado direto ao `tmux resize-window`; não é injeção (via `strconv.Itoa` de um `int`), só falta de validação de faixa razoável.

### Confirmações positivas (respondendo aos pontos específicos pedidos)
- **Ponto #1 (injeção via alvo composto no tmux.go):** NÃO há injeção. `parseTarget` (`tmux.go:120-134`) valida pane (`^%[0-9]+$`, sem espaço/especiais) e socket (`^/[A-Za-z0-9._/ -]+$`, sem aspas/`;`/`$()`/backtick) ANTES de qualquer uso; todo valor validado passa por `singleQuote` (`target.go:389-391`, idioma `'\''` correto) em CADA call site (`tmuxBase`, `TmuxCapture/Key/Resize/Send`); e — diferente do SEC-101 original — o comando remoto aqui roda em UM SÓ nível de shell (`runSSHTmux` passa a string inteira como UM argv para `ssh dest "<cmd>"`, sshd invoca o login shell UMA vez; não há `bash -lc` aninhado por dentro). `TmuxKey` também restringe `key` a um allowlist fixo (`tmuxAllowedKeys`) antes de concatenar sem quote. `TmuxSend` usa `-l --` (literal + fim-de-opções) no `send-keys`, protegendo contra texto começando com `-`. Espaço permitido no socket não é problema: sempre embrulhado em `singleQuote`, vira um único argumento do jeito que já seria com ou sem espaço.
- **Ponto #4 (parcial):** `Registry.SetPane` é no-op se o pane não mudou (`registry.go:142-156`) — não gera rajada de broadcast a cada hook (o pane de uma sessão só muda uma vez, na prática). A parte real do ponto #4 é o crescimento do `dismissed` set (ver non-bloqueante acima).
- **Ponto #5:** o decode funciona corretamente. `convertFromSnakeCase` transforma a CHAVE do JSON recebido ("created_at" → "createdAt") no nível do `KeyedDecodingContainer`, independente de o `init(from:)` ser sintetizado ou customizado — então `c.decode(Date.self, forKey: .createdAt)` (CodingKeys já em camelCase) casa certo com `created_at`/`pending_prompt` do wire. `Equatable`/`Hashable` sintetizados não são afetados por um `init(from:)` customizado (são conformidades independentes de `Decodable`, calculadas sobre as stored properties) — `Session` synthesiza normalmente (todas as properties são `Hashable`), então `path: [Session]`/`.navigationDestination(for: Session.self)` continuam corretos.
- **Ponto #6 (parcial):** `SetPane`, `AddIfAbsent`, `UpdateState`, `SetPendingPrompt`, `ClearPendingPrompt` são cada um uma seção crítica única (lock→mutação→unlock→broadcast fora do lock) — sem deadlock, `go test -race` verde é coerente com isso. `Remove`/`Unsubscribe`/`broadcastRemoved` têm raciocínio explícito e correto contra send-on-closed-channel. O buraco real de atomicidade é especificamente `Engine.EnsureRegistered` (não é um método do Registry, é do Engine, e compõe 3 chamadas do Registry sem lock único) — ver bloqueante #2.

### Padrões detectados
- `reivindicação-não-atômica-de-recurso-compartilhado` (4ª ocorrência — `Engine.EnsureRegistered`, ainda ABERTA) `[→ review/patterns.md]`
- candidato NOVO (1x, abaixo do critério de promoção de 2x): "duas-fontes-de-verdade-sem-marca-de-propriedade" — duas fontes independentes (hook HTTP e Runner in-process/stream-json) podem criar/mutar o MESMO registro (`Engine`/`Registry`) para o mesmo id sem nenhum marcador de qual fonte é autoritativa, e quem chega primeiro "vence" permanentemente mesmo quando semanticamente errado (ver bloqueante #1). Se reaparecer (ex. outro adapter com dupla fonte de eventos), promover a entrada completa em `patterns.md`.

### Especialistas envolvidos
- **Marcus:** desenhar o mecanismo de "ownership" do Registry (hub-launched vs. hook-detected) que resolve o bloqueante #1 de forma robusta a qualquer ordem de chegada, e avaliar a reconciliação de pane reutilizado (bloqueante #3) — ambos tocam o modelo de estado central do backend, não são fixes puramente locais.

## 2026-07-03 — histórico ao adotar, sessões LIVE no Mac, supressão de push por foreground, preview + markdown
**Risco:** R2 (Alto — novos endpoints públicos (`/machines/{m}/live`, `/app/foreground`), importação de histórico de conversa via SSH para máquinas remotas, e uma mudança em lógica de segurança de notificação (supressão de push). Sem RCE novo — SEC-101 não regrediu.)
**Tipo:** T1 (feature nova) + T3 (refactor: `discoveryLikeHandler` compartilhado)
**Escopo:** hub: `adapter/claudecode/{transcript.go (novo), live.go (novo), discover.go, target.go}`, `launcher/launcher.go` (`Adopt`/`importTranscript`/`Live`), `registry/output.go` (teto 200→500), `notifier/notifier.go` (`SetForeground`/TTL), `server/{settings.go (novo), launch.go, server.go}`, `session/session.go`, `cmd/hub/main.go`. app: `MarkdownText.swift` (novo), `ForegroundReporter.swift` (novo), `APIClient.swift`, `Models.swift`, `CutuqueApp.swift`, `DiscoverSessionsView.swift`, `SessionListView.swift`, `SessionDetailView.swift`.

### Bloqueantes
1. **issue (correctness/concurrency, blocking, impacto de segurança):** corrida entre o heartbeat de foreground (`ForegroundReporter.swift:36-43`, manda `active:true` a cada 60s) e o aviso de background (`ForegroundReporter.swift:45-50`, manda `active:false` na hora) — são duas requisições HTTP independentes, sem nenhuma ordenação/sequência entre elas. `Notifier.SetForeground` (`notifier.go:92-98`) só faz `atomic.Int64.Store`, que garante atomicidade do acesso à variável, mas NÃO garante que as chamadas sejam aplicadas na ordem em que os eventos aconteceram no cliente — só na ordem de CHEGADA ao hub. Cenário concreto: qualquer transição para `.inactive` (Control Center, app switcher, chamada entrando, Face ID) já dispara `stopHeartbeat()` → `active:false` imediato; ao voltar para `.active`, `startHeartbeat()` dispara `active:true` imediato, sem debounce. Duas requests saem em sequência rápida sem esperar a anterior terminar; se a rede entregar fora de ordem (ou a `true` simplesmente responder mais devagar que uma `false` mandada depois), o hub fica com `foregroundUntil` setado para +150s mesmo com o app em background — suprimindo silenciosamente pushes de `needs_you`/`done`/`error` por até 150s exatamente quando ninguém está olhando o app. Não é um edge case raro: qualquer gesto de Control Center/app switcher já é suficiente para gerar o par de requests concorrentes.
   - **Por que importa:** o objetivo declarado da feature #3 é "nunca perder o push crítico quando o app não está aberto"; essa corrida inverte exatamente essa garantia.
   - **Sugestão concreta:** incluir um timestamp/sequência monotônica no corpo (`{"active": bool, "at": <ms epoch do cliente>}`) e o hub só aplicar a atualização se `at` for mais recente que a última aceita (`notifier.go` guarda `lastForegroundAt int64` ao lado de `foregroundUntil`, ignora updates com `at` mais antigo). Resolve o problema de raiz (ordem lógica, não ordem de chegada na rede) sem exigir sincronização perfeita no cliente.
   - `[→ security.md#SEC-102]`
   - **Status: RESOLVIDO nesta leva** — `Notifier.SetForeground` (`notifier.go:94-111`) agora recebe `at int64` e usa `fgMu`/`fgLastAt` para descartar updates fora de ordem, exatamente como recomendado. Confirmado via leitura de código.

2. **issue (correctness, blocking):** teto de chunks de output no cliente ficou desatualizado em relação ao hub. `registry/output.go:7` subiu `maxOutputChunks` de 200 para 500 justamente para caber o histórico importado (`Launcher.Adopt` → `importTranscript`). O app, porém, ainda usa um teto fixo de 200 só no caminho de chunk AO VIVO (`SessionDetailView.swift:55-56`, comentário "espelhando o teto de 200 do hub" — não é mais verdade) e NENHUM teto no carregamento inicial (`SessionDetailView.swift:29-30`, `chunks = history` sem `.suffix`). Resultado: ao adotar uma sessão com histórico grande (até 500 chunks importados) e continuar a conversa, o PRIMEIRO chunk ao vivo que chega dispara `removeFirst(count - 200)` (linha 56) e descarta de uma vez até ~300 mensagens do histórico que a usuária acabou de importar — bem no momento em que ela está engajada na conversa. Reprodutível de forma determinística (não depende de corrida), sempre que o histórico importado exceder 200 chunks.
   - **Por que importa:** anula na prática o valor da feature #1 (importar histórico) assim que a sessão volta a ficar viva.
   - **Sugestão concreta:** subir o teto do cliente para 500 (idealmente uma constante única referenciada nos dois lados, ou documentada em ambos os comentários) e aplicar o MESMO teto também no carregamento inicial (`chunks = Array(history.suffix(500))`), não só no caminho de append ao vivo.

3. **issue (concurrency, blocking — 3ª ocorrência de um padrão já catalogado):** `Launcher.Adopt` (`launcher.go:262-283`) faz `reg.Get(id)` e, mais adiante, `reg.Add(s)` + `importTranscript` sem uma seção crítica única cobrindo check+ação — mesma classe de bug do padrão `reivindicação-não-atômica-de-recurso-compartilhado` já catalogado 2x em `hub/review/patterns.md` (ambas as ocorrências anteriores corrigidas). Na revisão da feature de discover/adopt (2026-07-03) isso já tinha sido apontado como non-blocking, porque um `Add` duplicado do mesmo `Session{}` struct é inofensivo (idempotente). Esta leva muda o cálculo: agora `importTranscript` roda incondicionalmente após o `Add`, e `Registry.AppendOutput` (`registry/output.go:27`) só faz `append` puro, sem chave de idempotência. Duas chamadas concorrentes de `Adopt` para o MESMO id novo (plausível com o app aberto em dois dispositivos apontando pro mesmo hub, ou um retry de cliente) passam ambas pelo `Get` (ambas veem `ok=false`), ambas chamam `importTranscript`, e o histórico inteiro da sessão aparece DUPLICADO no chat.
   - **Por que importa:** o mesmo padrão já mordeu o projeto 2x antes (`Handle.Close`, `respond()`); desta vez o efeito é visível para a usuária (mensagens repetidas), não mais "inofensivo".
   - **Sugestão concreta:** `Registry.AddIfAbsent(s) (existing session.Session, added bool)` fazendo lookup+insert sob o mesmo lock; `Adopt` só chama `importTranscript` quando `added == true`.
   - `[→ hub/review/patterns.md#reivindicação-não-atômica-de-recurso-compartilhado]`

### Non-bloqueantes
- suggestion (perf, if-minor): `importTranscript` roda de forma síncrona dentro do handler de `POST /adopt`, com teto de 15s (`launcher.go:295`, `discoverTimeout`). É uma escolha deliberada e documentada (`launcher.go:279-281`: garante que o `GET /output` logo após o adopt já tenha histórico) e o teto evita travar para sempre — aceitável hoje. Vale reconsiderar para assíncrono (201 imediato + import em background + WS) se popular sessões com transcript grande via SSH remoto se mostrar perceptivelmente lento na prática.
- suggestion (design, recurring): execução de script remoto via SSH sem login shell (`ssh <opts> -- dest "python3 -"`) aparece agora em 3 lugares (`discover.go:104`, `live.go:116`, `transcript.go:125`) com o mesmo risco de PATH restrito em sessão ssh não-interativa já apontado (non-blocking) para `discover.go` na revisão anterior. Vale corrigir uma vez só no nível do helper compartilhado (`runDiscoverScript`/equivalente para `runTranscript`) em vez de manter a mesma decisão replicada em 3 arquivos. `[→ review/patterns.md#ssh-remote-script-sem-login-shell]`
- suggestion (correctness/UX, fail-open, baixa severidade): `CutuqueApp.swift:23-24` — `.onChange(of: scenePhase)` não usa `initial: true`, então `ForegroundReporter.shared.update(.active)` nunca roda no cold launch; o hub só aprende "foreground=true" na primeira transição background→active. Entre o cold launch e essa primeira transição, um push de `needs_you` pode disparar mesmo com o app aberto — direção de falha é "notifica a mais", não "suprime indevidamente", por isso non-blocking.
- thought (design): heurística `newest_sid(cwd)` do `liveScript` (`live.go:68-71`) mapeia um cwd para UM candidato (arquivo `.jsonl` de mtime mais recente). Duas sessões `claude` vivas simultaneamente no MESMO cwd (ex.: duas abas de terminal no mesmo projeto) colapsam no mesmo candidato — a outra sessão viva fica invisível naquele poll e pode "piscar" entre polls de 15s conforme a ordem de escrita dos dois processos. Não é crash nem segurança; já é um fallback (o sinal forte é `--session-id`/`.jsonl` no argv, `live.go:62-67`). Vale documentar a limitação ou, se incomodar na prática, trocar por `lsof -p <pid>` filtrando descritores `.jsonl` abertos de fato pelo processo, em vez de cwd+mtime.
- nitpick: `sid_from_cmd` (`live.go:62-67`) reconhece `--session-id <uuid>` e `<uuid>.jsonl` no argv, mas não `--resume <uuid>` — que é exatamente a flag usada pelo próprio `Launcher.resume` (`launcher.go:425`) para retomar sessões adotadas/continuadas pelo Cutuque. Na prática cai no fallback de cwd sem problema visível (o arquivo da sessão retomada quase sempre é o mais recente no seu próprio cwd), mas um `--resume\s+(UUID)` a mais no regex tornaria o match forte também para o próprio tráfego do hub.
- nitpick (perf, irrelevante na escala atual): `importTranscript` chama `AppendOutput` uma vez por chunk em loop (até 500 vezes), cada chamada faz lock/unlock + tentativa de broadcast (`registry/output.go:27-41`) — para uma sessão recém-adotada isso é uma rajada de até 500 eventos `output_chunk` na conexão WS geral (que ninguém ainda está filtrando para essa sessão, então são descartados sem efeito). Não é bug (o `Output(id)` usado pelo `GET /output` lê o array persistido, não depende do broadcast), só uma rajada desnecessária de mensagens.

### Confirmações positivas (respondendo aos pontos específicos pedidos)
- **Ponto #6 (SEC-101):** NÃO regrediu. `SSHTarget.Transcript` (`transcript.go:124-127`) reusa `singleQuote`/um único nível de shell (sem `bash -lc` aninhado) e depende de `Launcher.Adopt` validar `id` contra `sessionIDPattern` (`launcher.go:259-261`, `^[0-9a-fA-F-]{8,64}$`) antes de qualquer chamada — único call site confirmado via grep. `Live`/`live.go` não recebe nenhum input do cliente (script é uma constante; `machine` só é usado para indexar o mapa fixo de targets).
- **Ponto #2:** `importTranscript` bloqueia o handler de `/adopt` por até 15s no pior caso, mas com teto (`discoverTimeout`) e degradação graciosa (erro → retorna silenciosamente, sessão fica adotada sem histórico). Ver suggestion non-blocking acima.
- **Ponto #4:** `livePollTask` (`SessionListView.swift:88-102`) não vaza — `guard let self else { return }` está DENTRO do `while` (reavaliado a cada iteração), diferente do bug já catalogado `weak-self-fora-do-loop-em-task-de-stream`; `stopLivePolling()` cancela e zera a referência, chamado em `.onDisappear`. Classe é `@MainActor`, então toda escrita em `@Published` (`refreshLive`, `liveSessions =`) roda isolada na main actor — nenhuma escrita fora da main encontrada.
- **Ponto #5:** `MarkdownText.swift` não apresenta loop infinito nem crash nos casos testados (fence sem fechamento, heading sem espaço, tabela malformada, índices fora de faixa) — todo `while` interno usa `i < lines.count` como curto-circuito antes de indexar, e o parser inline cai em `Text(s)` puro via `try?` quando `AttributedString(markdown:)` falha.
- **Ponto #1:** `liveScript` não usa `shell=True` em nenhum subprocess (`ps`/`lsof` chamados com lista de args), e o script é uma constante Go sem interpolação de input externo — sem risco de injeção. Risco de misattribution é real mas limitado (ver thought/nitpick acima), mitigado pela janela de recência de 180s.

### Padrões detectados
- `reivindicação-não-atômica-de-recurso-compartilhado` (3ª ocorrência, ainda ABERTA nesta) `[→ hub/review/patterns.md]`
- `ssh-remote-script-sem-login-shell` (3x: discover.go, live.go, transcript.go) `[→ review/patterns.md, NOVO]`

### Especialistas envolvidos
- Nenhum necessário para os 3 bloqueantes (são locais: `notifier.go`+`ForegroundReporter.swift`, `SessionDetailView.swift`+`registry/output.go`, `launcher.go`+`registry.go`). Se o achado #1 (corrida de foreground) precisar de uma solução mais robusta que timestamp lógico (ex.: multi-dispositivo com estados de foreground independentes por device), vale chamar **Marcus** para desenhar o modelo de estado por device.

## 2026-07-03 — resolução dos bloqueantes da feature de descoberta
Todos os 3 bloqueantes corrigidos e verificados; hub redeployado no ZimaOS.
- **#1 (RCE/SEC-101):** `remoteClaudeCommand` agora usa `bash -lc 'exec "$0" "$@"' <argv...>` (cada arg single-quoted) — sem reparse aninhado. Validação de `id` (`^[0-9a-fA-F-]{8,64}$`) em `Adopt` → `ErrInvalidSessionID`/`400`. Regressão: `TestRemoteClaudeCommandNeutralizesInjection` (5 payloads) + `TestAdoptRejectsInvalidID` + `TestAdoptInvalidSessionID`. Verificado no hub live (400 + sem artefato). [security.md#SEC-101 → RESOLVIDO]
- **#2 (testes):** `discover_test.go` criado — `parseDiscovered` (válido/vazio/malformado), `runDiscover`/`LocalTarget.Discover` e `SSHTarget.Discover` via fake program, e o comportamento do `discoverScript` (descarta títulos sintéticos, normaliza espaços).
- **#3 (mapeamento de erro):** `ErrDiscoverFailed` tipado; `Launcher.Discover` distingue máquina desconhecida de falha real e aplica timeout de 15s; `DiscoverHandler` → `502 discover_failed`. Testes: `TestDiscoverFailedReturns502`, `TestDiscoverUnknownMachine`.
- Non-bloqueante adotado junto: timeout em `Discover`. Nitpick do título já estava resolvido (função `clean()` no script). Comentário de `singleQuote` atualizado.
- Ainda em aberto (non-bloqueantes, próxima iteração): `reg.AddIfAbsent` no Adopt (TOCTOU), `bash -lc` no `SSHTarget.Discover` para PATH, e diferenciar erro de vazio no `APIClient.discover` do app.

## 2026-07-03 — feature "descobrir e adotar sessões ativas do Claude Code no Mac"
**Risco:** R1 (novo endpoint que aceita `id`/`cwd` do cliente e os injeta num `bash -lc` remoto via SSH — RCE confirmado por PoC)
**Escopo:** hub/internal/adapter/claudecode/{discover.go (novo), target.go}, hub/internal/launcher/launcher.go (Discover/Adopt/resume), hub/internal/session/session.go, hub/internal/server/{launch.go,server.go}, app/CutuqueApp/{APIClient.swift, Models.swift, DiscoverSessionsView.swift (novo), SessionListView.swift}

### Bloqueantes
- issue (security, blocking): OS command injection via `id` do Adopt no caminho SSH — `remoteClaudeCommand` junta `resumeID` numa string que é reparseada por um `bash -lc` ANINHADO (double-shell), então `singleQuote` só protege o nível externo. PoC local confirmou RCE (`; touch <arquivo>` executado). [→ security.md#SEC-101]
- issue (testability, blocking): `hub/internal/adapter/claudecode/discover.go` é um arquivo NOVO inteiro sem nenhum teste (`discover_test.go` não existe) — `parseDiscovered`, `runDiscover` e os dois `Discover` não têm cobertura alguma; um teste de integração com fake ssh (no espírito de `TestSSHTargetRunnerProcessesFixtureViaFakeProgram`) teria pego o achado acima.
- issue (correctness, blocking): `DiscoverHandler`/`Launcher.Discover` mapeiam QUALQUER erro (ssh caiu, python3 ausente, script crashou, JSON corrompido) para 404 `unknown_machine`, indistinguível de "máquina não configurada". Quebra o padrão já usado em `LaunchHandler` (dispatch por `errors.Is`).

### Non-bloqueantes
- suggestion (perf/reliability): `Discover` roda sobre `l.baseCtx` sem timeout próprio (diferente de `launchTimeout` do Launch) — uma ssh/python3 pendurada trava o request e o processo até o Shutdown do hub.
- suggestion (concurrency, if-minor): `Adopt` faz `reg.Get` + `reg.Add` sem lock atômico (TOCTOU) — inconsistente com o cuidado explícito de `respond()` (reivindicação atômica do pending). Risco prático baixo (exige colisão de id), mas vale um `reg.AddIfAbsent`.
- suggestion: `SSHTarget.Discover` roda `python3 -` direto (sem `bash -lc`), diferente do padrão usado pro `claude` — pode falhar em máquinas com PATH restrito na sessão ssh não-interativa.
- suggestion (UX): `APIClient.discover` engole QUALQUER erro em `[]`, indistinguível de "sem sessões" — diferente de `adopt`, que propaga erro pra `errorMessage`.
- nitpick: `discoverScript` não colapsa espaços/quebras de linha do título (`title=c.strip()[:100]`) — títulos multi-linha ficam poluídos na lista.
- thought: comentário de `singleQuote` ("não pretende ser escapador genérico para input externo") ficou desatualizado — é exatamente o que a feature faz agora; vale atualizar junto do fix.
- praise: `parseDiscovered` + o script python tratam saída vazia/arquivo corrompido com graça (nunca quebra a lista inteira por causa de uma sessão ruim) — bom design defensivo.
- praise: no app, `DiscoverSessionsView` desabilita TODAS as linhas durante uma adoção em voo e `SessionListView` guarda contra push duplicado no path (`if path.last?.id != session.id`) — detalhe de concorrência de UI bem pensado.

### Padrões detectados
- `shell-command-por-string-reparseada-em-shell-aninhado` (1x) — primeira ocorrência, ainda não registrado em patterns.md (critério é 2x).

### Especialistas envolvidos
- Nenhum necessário para o fix pontual do achado #1 (é local a `target.go`/validação de input). Se o padrão se repetir em outro adapter, vale chamar Marcus para revisão de padrão de invocação de comando remoto.
</content>
